# 本地 Docker Compose 栈的环境变量模板（T-003）。
#
#     cp infra/compose/.env.example infra/compose/.env
#     docker compose -f infra/compose/docker-compose.base.yml up -d
#
# 本文件与 compose 文件同目录**不是随手放的**：compose 的 project directory 默认取
# `-f` 的第一个文件所在目录，于是 `infra/compose/.env` 会被自动加载，本地与
# prod/staging 叠加链读到的是同一份，不需要 --env-file，也不会出现两条命令读了
# 不同 .env 却因为 ${VAR:-default} 兜底而静默跑起来的情况。
#
# ⚠️ `.env` **绝不入库**（§14）—— 根 .gitignore 一刀切忽略 .env 与 .env.*，
# 只对 .env.example 开了口（该规则不限目录，在本目录同样生效）。
# 本文件里的全部值都是**本地开发占位值**，不具备任何安全意义。
#
# staging / production 的真实值由 sops(age) 加密后随 Ansible 下发（T-012），
# 在目标主机上解密成容器环境变量。它们不经过本文件，也不进 CI。
#
# 与 backend/.env 的分工：
#   本文件        compose 起容器时注入的值（谁监听什么端口、用什么口令）
#   backend/.env  Symfony 在**裸机**上跑（composer test / bin/console）时的默认值
# 容器里以本文件为准 —— 真实环境变量优先于 Symfony 的 dotenv 文件。

# --- Symfony ---------------------------------------------------------------
APP_ENV=dev
APP_SECRET=dev-only-not-a-secret

# --- 宿主机端口 -------------------------------------------------------------
# caddy 是**唯一**映射宿主机端口的服务。postgres / redis / vault 不映射（§7.4）。
# 80 被占用时改这里，别去给后端服务加 ports。
HTTP_PORT=80

# Caddy 的站点地址。本地明文 localhost；staging/prod 由 Ansible 渲染成真实域名
# （**不带 scheme** —— 裸域名才会触发自动 TLS 与 80→443 重定向），
# 那时 Caddy 会自动申请证书并发 HSTS。
CADDY_SITE_ADDRESS=http://localhost

# T-106：App Links 域（`/l/magic/*` 落地页与 `/.well-known/assetlinks.json`）。
# 与上面的 API 域是两个站点块，同一个 Caddy 进程 —— 那个块只吐静态文件，
# 不反代 app:8080（理由见 Caddyfile 里那一大段与 ADR-0016）。
#
# ⚠️ 本地必须与 backend/.env 的 APP_PUBLIC_BASE_URL 指向同一个地方，
# 否则本地 Mailpit 收到的信里那个链接点开是 404（或者更糟：指向生产）。
CADDY_APP_SITE_ADDRESS=http://localhost:8081
APP_SITE_HTTP_PORT=8081

# ACME 账户邮箱与目录（T-012）。本地是明文 http://localhost，Caddy 根本不走 ACME，
# 这两个值对本地毫无影响 —— 列在这里是因为 Caddyfile 引用了它们，
# 而一个「compose 会读、模板里却找不到」的变量是下一个人排查时最费时间的东西。
#
# staging/prod 的真值由 Ansible 渲染（infra/ansible/roles/ncards_stack/templates/env.j2）。
# ACME_CA 排练时指向 Let's Encrypt 的 staging 目录以避开速率限制，
# 验收时必须是生产目录。
ACME_EMAIL=ops@n-cards.de
ACME_CA=https://acme-v02.api.letsencrypt.org/directory

# --- 部署镜像与日志（staging/prod 才用到，T-012）--------------------------
# 本地 app 是 build 出来的，用不到 APP_IMAGE；prod/staging 叠加层从 GHCR 拉，
# 由部署流程写入 commit sha。默认值只是让 `docker compose config` 不报错。
APP_IMAGE=ghcr.io/ht-lin/n-cards-backend:main

# staging 相对 prod 的唯一放松（见 docker-compose.staging.yml 的头注释）。
# monolog 的实际接线是 T-405 的事，这里先把变量占住。
LOG_LEVEL=debug

# --- Postgres --------------------------------------------------------------
# app 容器的 DATABASE_URL 由这三个值拼出来，见 docker-compose.base.yml。
POSTGRES_DB=ncards
POSTGRES_USER=ncards
POSTGRES_PASSWORD=ncards

# --- Vault -----------------------------------------------------------------
# 本地 Vault 跑 dev 模式，这是它的固定 root token。
# app 容器与 vault-init 都用它（T-005）。
#
# 生产是 file 后端 + **人工 unseal**（Q6 / ADR-0004），unseal key 离线保管、绝不进 CI ——
# 所以生产没有与此对应的变量。见 infra/vault/vault.hcl。
VAULT_DEV_ROOT_TOKEN_ID=dev-only-root-token

# 生产/staging 用 AppRole 认证 Vault（§7.4 / §17.4），**不用** root token。
# 这两个变量只在 .prod.yml 里被引用，且是**必填**（未配置时 compose 会直接报错退出）。
# 真实值由 sops(age) 加密后随 Ansible 下发到目标主机的 .env（T-012）。
#
# ⚠️ VAULT_SECRET_ID 是**凭据**：绝不入库、绝不进 CI、绝不进聊天工具。
# role_id 不是秘密（单独拿着它登录不了），bootstrap.sh 跑完会打印。
# 本地不需要这两个 —— 留空即回落到 VAULT_DEV_ROOT_TOKEN_ID。
#VAULT_ROLE_ID=
#VAULT_SECRET_ID=

# --- 邮件通道（T-102 / §3.1 / §3.2 / §8.3）---------------------------------
# 发信 DSN。**换服务商就是改这一行** —— §3.2 允许一期单通道、无双活，
# 代价是「未来 2 人日能补回来」，而它成立的前提就是这个变量是唯一的耦合点。
# R1（发信域名被拉黑 / 通道故障）的处置动作也是它：
# 改 DSN → 重启 worker，见 docs/runbooks/email-dns.md。
#
# 本地默认 null://null：**不发信，直接丢弃**。想在本地肉眼看信：
#   docker compose --profile dev up -d mailpit
#   MAILER_DSN=smtp://mailpit:1025      # 然后重启 worker，UI 在 http://localhost:8025
#
# ⚠️ staging/prod 的真实 DSN **含 SMTP 口令，是凭据**，与 VAULT_SECRET_ID 同一口径：
# 由 sops(age) 加密后随 Ansible 下发到目标主机的 .env（T-012），绝不入库、绝不进 CI。
# .prod.yml 里它是**必填**（`:?`）—— 缺失时 compose 直接拒绝启动，
# 因为回落到 null:// 的症状是「所有人都登不进去，但日志全绿」。
MAILER_DSN=null://null

# 发件人。域名来自 ADR-0002。SPF / DKIM / DMARC 必须配在**这个地址的域**上，
# 否则几乎必进垃圾箱（§3.2）—— 记录模板见 docs/runbooks/email-dns.md。
MAIL_FROM_ADDRESS=no-reply@n-cards.de
MAIL_FROM_NAME=N-Cards

# §14.4 `email_send_total{provider,template,result}` 的 provider 标签。
# Q3 已决（../../docs/adr/0013-mail-via-domain-mailbox.md）：n-cards.de 的域名邮箱，
# 托管方 dogado GmbH（德国），不采购专业 ESP。
# 本地起 Mailpit 看信时可以改成 `mailpit`，免得本机的计数混进 dogado 那条曲线。
MAIL_PROVIDER=dogado

# mailpit UI 的宿主机端口（仅 dev profile）。8025 被占用时改这里。
#MAILPIT_UI_PORT=8025

# ---------------------------------------------------------------------------
# 客户端版本与维护窗口（T-112 / §6.1 / §9.2）
# ---------------------------------------------------------------------------
# 两者都由 `GET /v1/config` 下发给客户端；MIN_SUPPORTED_CLIENT 同时是
# ClientVersionListener 的 426 client_too_old 门槛（同一个值，两个读者）。
#
# 格式 `platform/x.y.z (build)`，与 `X-Client` 同一形态。配错时是 500（而不是一个
# 指着客户端说「你的 header 不对」的 400）—— 服务端配置错误该以 5xx 示人。
#
# ⚠️ 两者的爆炸半径不同，排查时别混：
#   MIN 配坏    → **每个**请求 500（含 /health/*），compose healthcheck 与
#                 Ansible 部署当场失败。判定在 ClientVersionListener 的构造期，
#                 而 kernel.request 的监听器在任何请求处理之前就被实例化了。
#   LATEST 配坏 → 只有 /v1/config 500，/health/ready 照样 200，部署照过。
#   （LATEST < MIN 同此）唯一的安全网是 §14.4 的 5xx 告警。
#
# ⚠️ 基线保守是刻意的：App 还没发布，把任何客户端挡在门外都是纯粹的误伤。
# 真要抬高它是一次部署动作（改 group_vars + 跑 Ansible），不需要改代码。
MIN_SUPPORTED_CLIENT=android/0.0.0 (1)

# 商店上最新的版本。消费者是**仍在支持范围内**的客户端（「有新版可用」软提示），
# 不是强制墙 —— 强制墙的信号是 426 本身。对齐 android 侧 versionName/versionCode。
LATEST_CLIENT=android/0.1.0 (1)

# §9.2 的计划维护窗口：每周二 03:00–04:00 CET，提前 24 小时通过 /v1/config 下发，
# 客户端展示横幅（维护期间离线读卡不受影响）。
#
# ⚠️ 两头**必须成对**填写，RFC 3339 且 **offset 必填**。都留空即「无窗口」，
# 这是常态。只配一头 → 500（而不是静默当成无窗口：漏填 end 的那次部署，
# 运维以为公告发出去了而客户端什么也没收到）。
#   MAINTENANCE_WINDOW_START=2026-09-15T03:00:00+02:00
#   MAINTENANCE_WINDOW_END=2026-09-15T04:00:00+02:00
MAINTENANCE_WINDOW_START=
MAINTENANCE_WINDOW_END=
