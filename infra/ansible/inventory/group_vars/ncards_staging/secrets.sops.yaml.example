---
# secrets.sops.yaml 的**明文模板**（T-012）。
#
# ============================================================================
# 这个文件不会被 Ansible 读取。它是给人看的模板。
# ============================================================================
#
# 真正被读取的是同目录下的 `secrets.sops.yaml`（sops/age 密文，**入库**），
# 由 community.sops 的 vars plugin 在 playbook 运行时自动解密进变量空间。
#
# 怎么从本模板做出那个文件（首启第 6 步，见 docs/runbooks/staging-first-boot.md）：
#
#     cd infra/ansible/inventory/group_vars/ncards_staging
#     cp secrets.sops.yaml.example secrets.sops.yaml
#     # 把下面四个值改成真值（生成方法见每个变量的注释）
#     sops -e -i secrets.sops.yaml       # 就地加密，用仓库根 .sops.yaml 里的 age 公钥
#     git add secrets.sops.yaml          # 密文入库是刻意的
#
# 之后要改某个值：`sops secrets.sops.yaml` 会解密到编辑器里，存盘时自动重新加密。
# **任何时候都不要 `sops -d` 到一个文件里** —— 根 .gitignore 挡了 *.decrypted，
# 但别去赌那个。
#
# ⚠️ 本模板里的值全是 `dev-only-` 前缀的占位值，不具备任何安全意义。
# 这个前缀是仓库的约定（见 .gitleaks.toml）：提交进来的假凭据必须这样开头，
# 于是「忘了替换成真值」和「不小心提交了真凭据」在扫描器眼里是两件不同的事。

# Symfony 的 APP_SECRET。生成：openssl rand -hex 32
APP_SECRET: dev-only-replace-with-openssl-rand-hex-32

# Postgres 口令。生成：openssl rand -base64 33 | tr -d '/+=' | head -c 40
# ⚠️ 首启之后再改这个值不会自动生效 —— 口令是 initdb 时写进数据卷的，
# 改了 .env 只会让 app 连不上。要改得走 ALTER ROLE，见 deploy-and-rollback.md。
POSTGRES_PASSWORD: dev-only-replace-with-a-real-random-password

# Vault AppRole 凭据（§7.4 / §17.4）。
#
# ⚠️ 首启时这两个值填占位符 `pending-bootstrap-see-runbook` —— 那时 Vault 还没
# init，凭据还不存在，而 docker-compose.prod.yml 用 ${VAR:?} 把它们设成了必填，
# 留空会让 compose 直接报错退不起来。
#
# 占位值能让整个栈先起来：VaultHealthCheck 只打**免认证**的 sys/health，
# 所以 unseal 之后 /health/ready 照样 200，只有真正的涉密请求才 503。
# 首启第 9 步拿到真值后回来 `sops` 改掉。
#
# role_id 不是秘密（单独拿着它登录不了），bootstrap.sh 跑完会打印。
# secret_id **是**凭据：绝不入库明文、绝不进 CI 日志、绝不进聊天工具。
VAULT_ROLE_ID: pending-bootstrap-see-runbook
VAULT_SECRET_ID: pending-bootstrap-see-runbook

# Vault policy 下发身份的 AppRole 凭据（T-114 / §17.4）。
#
# 与上面那一对是**两个不同的身份，用途完全不重叠**：
#   ncards-app     应用运行时。加解密、HMAC、读 JWT 签名密钥。
#   ncards-policy  部署流水线。只读写 sys/policies/acl/*，让仓库里的 *.hcl
#                  真的到得了长期运行的环境（T-114 的缘起）。**没有任何 transit** ——
#                  它与 ncards-ops（轮换/rewrap）是刻意分开的两个身份。
#
# ⚠️ **这一对不会进主机的 infra/compose/.env**（env.j2 里刻意没有它们）。
# .env 是 app 与 worker 容器的环境来源，把「改写 ncards-app policy」的能力
# 放进那两个容器的 `docker inspect` 里，正好抵消掉 ncards-app.hcl 整份清单。
# ansible 只在 vault-policy 那一条任务里把它们传进一个跑完就退出的容器。
#
# 同样是首启时填占位符（那时 Vault 还没 init），第 9 步拿到真值后回来 sops 改掉。
# ⚠️ **已经首启过、root token 已吊销的环境**要补这一对，得走一次
# `vault operator generate-root`（3 位 unseal key 持有人到场）——
# 见 docs/runbooks/staging-first-boot.md 的「已经首启过的环境怎么补上」。
VAULT_POLICY_ROLE_ID: pending-bootstrap-see-runbook
VAULT_POLICY_SECRET_ID: pending-bootstrap-see-runbook

# 发信 DSN（T-102 / §3.2 / §8.3）。
#
# ⚠️ **是凭据** —— 含邮件服务商的 SMTP 用户名与口令。与 VAULT_SECRET_ID 同一口径：
# 绝不入明文库、绝不进 CI 日志、绝不进聊天工具。
#
# Q3 已决（docs/adr/0013-mail-via-domain-mailbox.md）：发信走 n-cards.de 的
# **域名邮箱**，托管方 dogado GmbH（德国）。形态：
#
#   smtp://<邮箱账号>:<口令>@web246.dogado.net:587?require_tls=true
#
# ⚠️ **端口是 587，不是面板给的 465。** Hetzner 封了 465（连 25 一起）出站，
#    2026-09-06 实测，症状是连接超时而非拒绝。改回 465 = 邮件全部发不出去。
#    完整判据与实测数字见 docs/runbooks/email-dns.md §2.4。
# ⚠️ **`?require_tls=true` 不能省，它是这条 DSN 的安全性所在。** 587 是明文起步 +
#    STARTTLS 升级，而 Symfony 的 require_tls 默认 false —— 服务器不通告 STARTTLS 时
#    它不报错，而是继续用**明文 AUTH** 把口令发上公网，日志全绿。加上这个参数，
#    TLS 没起来就抛异常，且那条检查在 handleAuth() 之前，口令不会被送出去。
# ⚠️ scheme 是 **smtp** 不是 smtps。smtps 强制 implicit TLS，套在 587 上握手失败。
# ⚠️ 主机名**从 dogado 面板抄**，别凭印象写 —— 写错的症状是 worker 起不来。
# ⚠️ 账号通常就是完整邮箱地址 no-reply@n-cards.de，那个 `@` **必须** percent-encode
#    成 %40，否则 DSN 解析会在一个非常不显眼的地方出错（用户名被截断 →
#    认证失败 → 全部邮件进 email_failed）。口令里的 @ : / 同理。
#
#   smtp://no-reply%40n-cards.de:<口令>@web246.dogado.net:587?require_tls=true
#
# ⚠️ 三条硬要求（§8.3，合规底线，**不可放宽**）：
#   ① 数据处理在 EU/EEA 内   ② 可签 AVV/DPA   ③ 支持自定义域 SPF/DKIM
# dogado 满足①③（德国；SPF/DKIM 已自动写入我们的 zone，selector `cloudpit`，
# 2026-09-06 实测），②的 AVV **仍须实际签署并归档**。
# 见 docs/runbooks/email-dns.md §2.2。
# **不得**改用个人/消费级邮箱（Gmail/GMX 个人账户）或美国 SaaS 的免费层 ——
# 那是无 DPA 的第三国传输，属于合规硬伤，与「要不要做双活」是两回事。
#
# 首启时可以先填下面这个占位值让栈起来 —— 但注意此时**邮件功能是坏的**
# （DSN 解析失败 → worker 起不来）。拿到真实口令后回来 `sops` 改成真值。
# 只想让栈先跑起来而不发信，就填 `null://null`。
MAILER_DSN: dev-only-replace-with-dogado-smtp-dsn
