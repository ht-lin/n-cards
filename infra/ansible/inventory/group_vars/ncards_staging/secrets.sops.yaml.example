---
# secrets.sops.yaml 的**明文模板**（T-012）。
#
# ============================================================================
# 这个文件不会被 Ansible 读取。它是给人看的模板。
# ============================================================================
#
# 真正被读取的是同目录下的 `secrets.sops.yaml`（sops/age 密文，**入库**），
# 由 community.sops 的 vars plugin 在 playbook 运行时自动解密进变量空间。
#
# 怎么从本模板做出那个文件（首启第 6 步，见 docs/runbooks/staging-first-boot.md）：
#
#     cd infra/ansible/inventory/group_vars/ncards_staging
#     cp secrets.sops.yaml.example secrets.sops.yaml
#     # 把下面四个值改成真值（生成方法见每个变量的注释）
#     sops -e -i secrets.sops.yaml       # 就地加密，用仓库根 .sops.yaml 里的 age 公钥
#     git add secrets.sops.yaml          # 密文入库是刻意的
#
# 之后要改某个值：`sops secrets.sops.yaml` 会解密到编辑器里，存盘时自动重新加密。
# **任何时候都不要 `sops -d` 到一个文件里** —— 根 .gitignore 挡了 *.decrypted，
# 但别去赌那个。
#
# ⚠️ 本模板里的值全是 `dev-only-` 前缀的占位值，不具备任何安全意义。
# 这个前缀是仓库的约定（见 .gitleaks.toml）：提交进来的假凭据必须这样开头，
# 于是「忘了替换成真值」和「不小心提交了真凭据」在扫描器眼里是两件不同的事。

# Symfony 的 APP_SECRET。生成：openssl rand -hex 32
APP_SECRET: dev-only-replace-with-openssl-rand-hex-32

# Postgres 口令。生成：openssl rand -base64 33 | tr -d '/+=' | head -c 40
# ⚠️ 首启之后再改这个值不会自动生效 —— 口令是 initdb 时写进数据卷的，
# 改了 .env 只会让 app 连不上。要改得走 ALTER ROLE，见 deploy-and-rollback.md。
POSTGRES_PASSWORD: dev-only-replace-with-a-real-random-password

# Vault AppRole 凭据（§7.4 / §17.4）。
#
# ⚠️ 首启时这两个值填占位符 `pending-bootstrap-see-runbook` —— 那时 Vault 还没
# init，凭据还不存在，而 docker-compose.prod.yml 用 ${VAR:?} 把它们设成了必填，
# 留空会让 compose 直接报错退不起来。
#
# 占位值能让整个栈先起来：VaultHealthCheck 只打**免认证**的 sys/health，
# 所以 unseal 之后 /health/ready 照样 200，只有真正的涉密请求才 503。
# 首启第 9 步拿到真值后回来 `sops` 改掉。
#
# role_id 不是秘密（单独拿着它登录不了），bootstrap.sh 跑完会打印。
# secret_id **是**凭据：绝不入库明文、绝不进 CI 日志、绝不进聊天工具。
VAULT_ROLE_ID: pending-bootstrap-see-runbook
VAULT_SECRET_ID: pending-bootstrap-see-runbook
