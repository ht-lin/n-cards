/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package de.ncards.core.network.api.model


import kotlinx.serialization.Serializable
import kotlinx.serialization.SerialName
import kotlinx.serialization.Contextual
import kotlinx.serialization.KSerializer
import kotlinx.serialization.Serializer
import kotlinx.serialization.builtins.serializer
import kotlinx.serialization.encoding.Decoder
import kotlinx.serialization.encoding.Encoder

/**
 * 计划维护窗口的公告（§9.2）。窗口固定为**每周二 03:00–04:00 CET**， 提前 24 小时通过本字段下发，客户端展示横幅。  ⚠️ 维护期间**离线读卡不受影响**——横幅文案要体现这一点（T-158）。 
 *
 * @param active 当前是否处于维护窗口内。
 * @param messageKey **本地化键，不是文案**（§11.1：面向用户的德语文案由客户端生成）。  - `maintenance.scheduled` —— 窗口将在 24 小时内开始，`active` 仍为 `false`； - `maintenance.in_progress` —— 正在窗口内，`active` 为 `true`； - `null` —— 无公告（无窗口、窗口在 24 小时以外、或窗口已结束）。  取值与 `App\\Shared\\Domain\\Config\\MaintenanceMessageKey` 逐项一致， 由 `backend/tests/Api/OpenApiDocumentTest` 对账。 
 * @param retryAfter 距窗口结束的秒数。**仅在 `active` 为 `true` 时有值**，否则恒为 `null`。  ⚠️ 提前公告阶段**刻意不给**这个数字：本字段是 §6.1 里 `503` 那个 `Retry-After` 的同义物，「多久以后可以重试」对一个还没开始的窗口 没有意义。横幅需要的时间写在客户端文案里（窗口由 §9.2 固定）。 
 */
@Serializable

data class Maintenance (

    /* 当前是否处于维护窗口内。 */
    @SerialName(value = "active")
    val active: kotlin.Boolean,

    /* **本地化键，不是文案**（§11.1：面向用户的德语文案由客户端生成）。  - `maintenance.scheduled` —— 窗口将在 24 小时内开始，`active` 仍为 `false`； - `maintenance.in_progress` —— 正在窗口内，`active` 为 `true`； - `null` —— 无公告（无窗口、窗口在 24 小时以外、或窗口已结束）。  取值与 `App\\Shared\\Domain\\Config\\MaintenanceMessageKey` 逐项一致， 由 `backend/tests/Api/OpenApiDocumentTest` 对账。  */
    @SerialName(value = "message_key")
    val messageKey: Maintenance.MessageKey?,

    /* 距窗口结束的秒数。**仅在 `active` 为 `true` 时有值**，否则恒为 `null`。  ⚠️ 提前公告阶段**刻意不给**这个数字：本字段是 §6.1 里 `503` 那个 `Retry-After` 的同义物，「多久以后可以重试」对一个还没开始的窗口 没有意义。横幅需要的时间写在客户端文案里（窗口由 §9.2 固定）。  */
    @SerialName(value = "retry_after")
    val retryAfter: kotlin.Int?

) {

    /**
     * **本地化键，不是文案**（§11.1：面向用户的德语文案由客户端生成）。  - `maintenance.scheduled` —— 窗口将在 24 小时内开始，`active` 仍为 `false`； - `maintenance.in_progress` —— 正在窗口内，`active` 为 `true`； - `null` —— 无公告（无窗口、窗口在 24 小时以外、或窗口已结束）。  取值与 `App\\Shared\\Domain\\Config\\MaintenanceMessageKey` 逐项一致， 由 `backend/tests/Api/OpenApiDocumentTest` 对账。 
     *
     * Values: maintenancePeriodScheduled,maintenancePeriodIn_progress,unknown_default_open_api
     */
    @Serializable(with = MessageKeySerializer::class)
    enum class MessageKey(val value: kotlin.String) {
        @SerialName(value = "maintenance.scheduled") maintenancePeriodScheduled("maintenance.scheduled"),
        @SerialName(value = "maintenance.in_progress") maintenancePeriodIn_progress("maintenance.in_progress"),
        @SerialName(value = "unknown_default_open_api") unknown_default_open_api("unknown_default_open_api");
    }

    internal object MessageKeySerializer : KSerializer<MessageKey> {
        override val descriptor = kotlin.String.serializer().descriptor

        override fun deserialize(decoder: Decoder): MessageKey {
            val value = decoder.decodeSerializableValue(kotlin.String.serializer())
            return MessageKey.entries.firstOrNull { it.value == value }
                ?: MessageKey.unknown_default_open_api
        }

        override fun serialize(encoder: Encoder, value: MessageKey) {
            encoder.encodeSerializableValue(kotlin.String.serializer(), value.value)
        }
    }

}

