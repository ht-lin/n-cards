/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package de.ncards.core.network.api.model


import kotlinx.serialization.Serializable
import kotlinx.serialization.SerialName
import kotlinx.serialization.Contextual
import kotlinx.serialization.KSerializer
import kotlinx.serialization.Serializer
import kotlinx.serialization.builtins.serializer
import kotlinx.serialization.encoding.Decoder
import kotlinx.serialization.encoding.Encoder

/**
 * `POST /me/username` 的请求体。**唯一**的字段。  ⚠️ 服务端会再做一次 `trim` + 小写化 —— 客户端的归一化是为了让用户在 输入框里就看到最终形态，不是服务端可以省掉那一步的理由（§3.8： 「客户端与服务端均对输入 `trim` + `toLowerCase`」）。  ⚠️ `additionalProperties: true` 是全仓库的约定（§13.6 允许新增字段）， 但这个端点在**运行时**会把未知字段拒成 `400 validation_failed` + `errors[].code = unknown_field`：请求体只有一个字段，多出来的东西 只可能是客户端 bug，而在一个一生只调一次的端点上静默忽略它， 意味着那个 bug 永远不会被发现。 
 *
 * @param username 全局唯一、**不可变**、小写。是添加好友的**唯一**检索键（§3.8）。  客户端发送前必须做 `trim` + `lowercase(Locale.ROOT)` 归一化—— `Anna_B ` 与 `anna_b` 是同一个名字。  不可变是靠「**没有修改端点**」保证的：只有一次性的 `POST /me/username`， 没有 `PATCH` / `PUT` 对应端点。 
 */
@Serializable

data class UsernameAssignment (

    /* 全局唯一、**不可变**、小写。是添加好友的**唯一**检索键（§3.8）。  客户端发送前必须做 `trim` + `lowercase(Locale.ROOT)` 归一化—— `Anna_B ` 与 `anna_b` 是同一个名字。  不可变是靠「**没有修改端点**」保证的：只有一次性的 `POST /me/username`， 没有 `PATCH` / `PUT` 对应端点。  */
    @SerialName(value = "username")
    val username: kotlin.String

) {


}

