/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package de.ncards.core.network.api.model


import kotlinx.serialization.Serializable
import kotlinx.serialization.SerialName
import kotlinx.serialization.Contextual
import kotlinx.serialization.KSerializer
import kotlinx.serialization.Serializer
import kotlinx.serialization.builtins.serializer
import kotlinx.serialization.encoding.Decoder
import kotlinx.serialization.encoding.Encoder

/**
 * `PATCH /me` 的请求体。  ⚠️ **没有 `username`**，而且这不是遗漏：出现该字段的请求返回 `409 username_immutable`（§6.2，**不静默忽略**）。不可变性靠「没有修改 端点」保证，而这个端点是那条不变量最容易被绕过的地方 —— 往下面的 `properties` 里加一行 `username`，等于把它删掉。  ⚠️ **没有 `display_name`**（v1.1 C10 移除）。  `required: [locale]` 与运行时一致：`locale` 是目前唯一的字段， 因此空对象 `{}` 是 `400 malformed_request` 而不是 200 no-op。 通知偏好（§6.2 提到、但一期没有任何数据模型，见 T-108 落地记录）落地时， 这里会放宽成「至少给一个字段」—— §13.6 允许放宽，禁止的是收紧。 
 *
 * @param locale 界面与邮件语言。值域闭合的两项，发别的值是**客户端 bug** —— 服务端返回 `400 validation_failed` + `errors[].code = invalid_format`， 不是 422（422 留给「请求合法但这个值用户不能用」，例如 `username_invalid`）。 
 */
@Serializable

data class MeUpdate (

    /* 界面与邮件语言。值域闭合的两项，发别的值是**客户端 bug** —— 服务端返回 `400 validation_failed` + `errors[].code = invalid_format`， 不是 422（422 留给「请求合法但这个值用户不能用」，例如 `username_invalid`）。  */
    @SerialName(value = "locale")
    val locale: MeUpdate.Locale

) {

    /**
     * 界面与邮件语言。值域闭合的两项，发别的值是**客户端 bug** —— 服务端返回 `400 validation_failed` + `errors[].code = invalid_format`， 不是 422（422 留给「请求合法但这个值用户不能用」，例如 `username_invalid`）。 
     *
     * Values: de,en,unknown_default_open_api
     */
    @Serializable(with = LocaleSerializer::class)
    enum class Locale(val value: kotlin.String) {
        @SerialName(value = "de") de("de"),
        @SerialName(value = "en") en("en"),
        @SerialName(value = "unknown_default_open_api") unknown_default_open_api("unknown_default_open_api");
    }

    internal object LocaleSerializer : KSerializer<Locale> {
        override val descriptor = kotlin.String.serializer().descriptor

        override fun deserialize(decoder: Decoder): Locale {
            val value = decoder.decodeSerializableValue(kotlin.String.serializer())
            return Locale.entries.firstOrNull { it.value == value }
                ?: Locale.unknown_default_open_api
        }

        override fun serialize(encoder: Encoder, value: Locale) {
            encoder.encodeSerializableValue(kotlin.String.serializer(), value.value)
        }
    }

}

