// Room + SQLCipher，entities / DAO / migrations。**由 T-009 填充**（§3.4）。

plugins {
    id("ncards.android.library")
    id("ncards.android.room")
}

android {
    namespace = "de.ncards.core.database"
}
