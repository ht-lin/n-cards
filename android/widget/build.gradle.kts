// Glance AppWidget + TileService + Shortcuts。**由 T-254/255 填充**（§10.2）。

plugins {
    id("ncards.android.library")
    id("ncards.android.compose")
    id("ncards.android.hilt")
}

android {
    namespace = "de.ncards.widget"
}
