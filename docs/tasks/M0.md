# M0 — 地基（第 1 周）

**出口条件**：一次 PR 能自动跑完 CI 并部署到 staging。

| ID | 任务 | 角色 | 预估 | 依赖 |
|---|---|---|---|---|
| T-001 | 仓库骨架与协作规范 | 全员 | 0.5 | — |
| T-002 | 后端 Symfony 骨架与质量门禁 | BE | 1 | T-001 |
| T-003 | 本地 Docker Compose 栈 | BE/OPS | 1 | T-002 |
| T-004 | Shared 内核（HTTP 横切层） | BE | 1.5 | T-002 |
| T-005 | Vault Transit 与加密门面 | BE | 2 | T-003 |
| T-006 | 限额与限流基础设施 | BE | 1 | T-003, T-004 |
| T-007 | OpenAPI 契约底座与 Spectral | 全员 | 1 | T-001 |
| T-008 | Android 工程骨架与 Convention Plugins | AND | 1.5 | T-001 |
| T-009 | Room + SQLCipher 与 core:crypto | AND | 2 | T-008 |
| T-010 | core:network 与代码生成打通 | AND | 1 | T-007, T-008 |
| T-011 | CI 流水线（GitHub Actions） | 全员 | 1.5 | T-002, T-008, T-007 |
| T-012 | Hetzner staging + Ansible 自动部署 | OPS/BE | 2 | T-003, T-011 |

---

## T-001 仓库骨架与协作规范

**角色** 全员 · **预估** 0.5 人日 · **依赖** —

**交付物**
- `n-cards/` 目录树：`docs/{adr,runbooks,api}`、`backend/`、`android/`、`infra/{compose,ansible,caddy,vault,monitoring}`、`.github/workflows/`
- `README.md`（如何起本地栈、如何跑测试）
- `.github/PULL_REQUEST_TEMPLATE.md`：变更说明 / 契约是否变更 / 迁移是否向后兼容 / 测试说明 / UI 截图
- `docs/adr/0001-record-architecture-decisions.md`（ADR 模板：Context / Decision / Consequences / Alternatives / Status）
- `commitlint` 配置（Conventional Commits）
- `main` 分支保护：禁直推、禁 force push、要求线性历史、1 approve + CI 全绿

**验收标准** 目录树与 §12.1 一致；PR 模板生效；一次不合规范的 commit message 能被 commitlint 拒绝。

**规格引用** §12.1、§13.2、§13.7

---

## T-002 后端 Symfony 骨架与质量门禁

**角色** BE · **预估** 1 人日 · **依赖** T-001

**交付物**
- Symfony 7.x / PHP 8.3+ 骨架，目录严格按 §12.2：`src/Shared/{Domain,Application,Infrastructure,Http}`、`src/Module/{Identity,Wallet,Sharing,Social,Sync,Notification,Compliance}` 的空壳
- `phpstan.neon`（level **8**，`src/` 全量）、`.php-cs-fixer.dist.php`（`@Symfony` + `@PHP83Migration`）、`phpunit.xml.dist`
- `deptrac.yaml`：按 §4.2 的 5 条规则配置层与模块边界

**实现要点**
- Deptrac 必须同时强制**两个维度**：① 模块间（不得引用他模块的 `Domain`/`Infrastructure`，只能经 `Application\Port\*`）；② 层间（`Http → Application → Domain`，`Domain` 不得 import 任何框架类型）。
- 为 `Sync\Infrastructure\Doctrine\SyncReadModel` 预留显式豁免槽位并加注释说明（唯一允许的跨模块只读查询入口）。
- 覆盖率阈值先配上：整体 ≥ 70%，`Module/*/Domain` 与 `Application` ≥ 85%（此刻还没代码，配置先就位）。

**验收标准** `phpstan`/`cs-fixer`/`deptrac` 三件套本地与 CI 均可跑；故意写一个跨模块 `Domain` 引用能让 deptrac 报 violation。

**规格引用** §4.2、§12.2、§13.3

---

## T-003 本地 Docker Compose 栈

**角色** BE/OPS · **预估** 1 人日 · **依赖** T-002

**交付物**
- `infra/compose/docker-compose.base.yml` + `.prod.yml` + `.staging.yml`
- 服务：`caddy`、`app`（FrankenPHP + Symfony）、`postgres:16-alpine`、`redis:7-alpine`（`appendonly yes`）、`vault:1.x`
- `bin/console app:seed` 种子数据命令的空壳
- `/health/live`、`/health/ready` 端点（**不在 `/v1` 下**，不暴露内部细节）

**实现要点**
- Postgres / Redis / Vault **不映射宿主机端口**，仅 Docker 内网可达（§7.4）。
- `app` 容器：非 root 用户、`read_only: true` + tmpfs 挂 `/tmp` 与 `var/cache`。
- Vault 使用**独立数据卷**（与 app 不共卷）。本地开发可用 dev 模式，但 compose 文件必须为 prod 留出人工 unseal 的形态。

**验收标准** `docker compose up` 一键起全栈；`/health/ready` 返回 200 且在 PG 不可用时返回 503。

**规格引用** §4.1、§7.4、§14.2

---

## T-004 Shared 内核（HTTP 横切层）

**角色** BE · **预估** 1.5 人日 · **依赖** T-002

**交付物**
- `Shared/Domain`：`Uuid`（UUIDv7 生成与校验）、`Clock`、`DomainEvent`、`DomainException`
- `Shared/Application`：`CommandBusInterface`、`EventBusInterface`
- `Shared/Infrastructure/Http`：
  - `ApiProblemExceptionListener` → RFC 9457 Problem Details（含 `code`、`request_id`、`errors[]`、`current`）
  - `RequestIdListener`（`X-Request-Id` 透传/生成，写入所有日志）
  - `ClientVersionListener`（解析 `X-Client: android/1.4.0 (26)`，缺失即 400）
  - `IdempotencyMiddleware`（`Idempotency-Key` header，Redis 存 24h，覆盖所有 POST）
- `Shared/Http`：`AbstractApiController`、`CursorPaginator`（游标式，默认 50 / 最大 200，**禁止 offset**）
- `Shared/Infrastructure/Doctrine`：`UuidType`、`TransactionalRunner`

**实现要点**
- ⚠️ **`ClientVersionListener` 必须把 `/health/*` 排除在外**。T-003 已交付的
  `/health/live` 与 `/health/ready` 的调用方是 Docker healthcheck、Caddy 与
  §14.3 部署流程里的 Ansible，它们**不带** `X-Client`。漏掉这条豁免，两个探活
  端点会在本任务合入当天集体变 400，连带 compose 起栈与 staging 部署的健康检查
  一起失效。同样的豁免逻辑将来还要覆盖 T-007 之后的任何非 `/v1` 端点。
- §6.1 的**统一错误码表**要落成一个 PHP enum（`validation_failed` / `token_expired` / `insufficient_role` / `username_required` / `revision_conflict` / `full_resync_required` / `username_taken` / `username_immutable` / `limit_exceeded` / `client_too_old` / `rate_limited` …），客户端只对 `code` 分支。
- `detail` 只写英文开发者文案，**服务端绝不返回可展示的德语文案**。

**验收标准** 单测覆盖每个错误码的响应形状；契约测试能对 Problem Details schema 校验通过。

**规格引用** §6.1、§12.2

---

## T-005 Vault Transit 与加密门面

**角色** BE · **预估** 2 人日 · **依赖** T-003（Q6 需先决策）

**交付物**
- Vault 初始化脚本：`transit/ncards-card`、`transit/ncards-pii`（AES-256-GCM96）、`transit/ncards-hmac`（HMAC-SHA256，**不轮换**）、JWT 签名密钥存 KV
- AppRole 认证（policy 最小化：仅对指定 key 的 `encrypt|decrypt|rewrap`；**不授予** `keys/*`、`rotate`、`rewrap` 管理权），token TTL 1h 自动续期
- `Shared/Infrastructure/Crypto`：`CryptoServiceInterface`、`VaultTransitCrypto`、`HmacHasher`、`BatchDecryptor`
- `infra/vault/` 策略文件（§17.4）+ `docs/runbooks/vault-unseal.md` 初版

**实现要点**
- **批量解密是硬要求**：`BatchDecryptor` 用 Transit 的 `batch_input`，**禁止在循环里逐条调 Vault**。目标 200 条 P95 < 80 ms。
- **不缓存明文**。若压测不达标，只允许单个 HTTP 请求生命周期内的进程内 memo，请求结束即销毁。
- auto-unseal **关闭**，unseal key（Shamir 3-of-5）离线保管，**绝不进 CI**。

**验收标准** 集成测试（真实 Vault 容器）：加密→解密往返一致；200 条 batch decrypt 有基准测试并记录耗时；deptrac 确认无模块直接 import `VaultTransitCrypto`（只经接口）。

**规格引用** §3.3、§5.3、§7.4、§17.4

---

## T-006 限额与限流基础设施

**角色** BE · **预估** 1 人日 · **依赖** T-003, T-004

**交付物**
- `config/packages/ncards_limits.yaml`：§7.5 全部限额常量（卡数 500、每卡成员 20、好友 500、payload 1024 B、note 2000 字符、title 100 字符、username 3–20、好友请求 50/日、共享邀请 100/日）
- `Shared/Infrastructure/RateLimit/LimitEnforcer`：超限抛出 `422 limit_exceeded`
- `config/packages/rate_limiter.yaml`：Symfony RateLimiter + Redis 滑动窗口，按 §7.5 速率表配置（端点 × 维度）
- 限流响应**必须**带 `Retry-After` 与 `X-RateLimit-Remaining`；多维度同时触发时返回**更长的** `Retry-After`

**实现要点** 明确区分两类：**系统限额**（超限 → `422 limit_exceeded`）与**速率限制**（超限 → `429 rate_limited`）。不建 `plan`/`quota` 表（ADR-11）。

- 复用 T-004 已交付的三样东西，**不要另起炉灶**：
  `Shared\Infrastructure\Redis\RedisConnectionFactory`（全仓库唯一的 Redis 连接构造点）、
  `Shared\Domain\Http\ApiSurface`（只对 `/v1/*` 生效的正向白名单）、
  `Shared\Domain\Error\ErrorCode`（`limit_exceeded` / `rate_limited` 都已在表里）。
- ⚠️ **Redis 不可用时限流必须 fail-CLOSED**，与 T-004 的幂等**相反**（那个是 fail-open）。
  限流 fail-open 等于在故障期间关掉 §7.5 的 OTP 与 username 枚举防线 ——
  那是安全控制，不是便利功能。这个不对称是刻意的，理由见 ADR-0003；
  在代码里也要注释引用，否则 reviewer 会当成不一致顺手「修」掉。
- ⚠️ 按 IP 的限流依赖 `framework.trusted_proxies`（T-004 已配）。真要确认它还在生效，
  看 `tests/Api/TrustedProxyTest` —— 那条没了的话，§7.5 的 IP 20/h 会把全世界算作一个 IP。

**验收标准** 单测覆盖每个限额常量的边界值；限流集成测试验证滑动窗口与 header。

**规格引用** §3.1、§7.5

---

## T-007 OpenAPI 契约底座与 Spectral

**角色** 全员 · **预估** 1 人日 · **依赖** T-001

**交付物**
- `docs/api/openapi.yaml`（OpenAPI **3.1**）v0：Auth + Cards 端点骨架 + 通用组件（Problem Details、Cursor 分页参数、`X-Client` header）
- Spectral 自定义规则集：必须有 `operationId`、必须有错误响应、必须有示例、所有 schema `additionalProperties: true`
- 后端契约测试底座：`tests/Api` + `league/openapi-psr7-validator`

**⚠️ 与 T-004 的衔接**
- `components/schemas/Problem` 必须用 `$ref` **指向** T-004 已交付的
  `docs/api/schemas/problem-details.schema.json`，**不要复制一份** —— 两份 `code`
  枚举必然会漂，而 Android 的 `ApiError` 是照它生成的。
- 契约里的 `code` 枚举与 `App\Shared\Domain\Error\ErrorCode` 的一致性已由
  `tests/Unit/Shared/Domain/Error/ProblemDetailsSchemaTest` 逐项断言，沿用即可。
- 通用组件里的 Cursor 分页参数用 T-004 已定的信封：`items` / `next_cursor` / `has_more`
  （见 `docs/api/README.md`）。
- `X-Client` header 的格式与 `Idempotency-Key` / `Idempotency-Replayed` 两个 header
  也要进契约 —— 后者是 T-004 自定义的，标准里没有。

**实现要点** **禁止**用 API Platform / NelmioApiDocBundle 从实现生成契约——方向反了，会让契约跟着实现漂移。

- ⚠️ **`league/openapi-psr7-validator` 的 README 明写只支持 OpenAPI 3.0.x，但它能跑 3.1**。
  0.23 起底层解析器从 `cebe/php-openapi` 换成了支持 3.1 的
  `devizzent/cebe-php-openapi`，文档没跟上而已。落地前先花半小时实测四件事
  （3.1 版本号、跨文件 `$ref`、`type: [string, "null"]`、坏响应确实抛异常）——
  四条全过才动手写契约。**结论：0.24 四条全过**，另需 `symfony/psr-http-message-bridge`
  + `nyholm/psr7` 把 Symfony 的 `Response` 转成 PSR-7。
  - ⚠️ `nyholm/psr7` 的 Symfony recipe 会生成
    `config/packages/nyholm_psr7.yaml`，把 PSR-17 服务注册进**生产**容器 ——
    而它是 `require-dev`。**必须删掉那个文件**，否则 `composer install --no-dev`
    之后容器编译会找不到类。契约测试自己 `new Psr17Factory()` 就够了。

- ⚠️ **`servers[].url` 带 `/v1`，所以 `paths` 下不带**（§6.1 的基址、§17.2 的参考片段
  都是这个形态），而后端路由是 `/v1/cards`。这个落差由校验器的 `PathFinder` 自动消化，
  但**三处必须同时对**：servers 带、paths 不带、测试传真实路径。
  `OpenApiDocumentTest::testServersCarryTheVersionPrefixAndPathsDoNot()` 钉着前两处。

- **契约首版只写 Auth + Cards**，其余 §6.2 端点由各自的任务在「先改契约」时补入。
  提前把 schema 全摆好只会得到一堆没人对照实现校对过的定义 ——
  连 §17.2 里的 `PublicUser` 都没放进去（它属于 T-302 的 `/users/lookup`）。

- **验收标准的后半条不要靠真实端点来满足**。契约里的十个端点一个都还没实现
  （Auth 是 T-103/104，Cards 是 T-109），但「校验器会咬人」必须**当天**就被证明，
  否则中间三个任务都是在没有契约门禁的情况下写的。做法是拿契约**自己的 `example`**
  当夹具：正向全过、改坏必拒。顺带把 `example` 从文档装饰变成了可执行断言。
  **不要**另建 `tests/Fixture/Contract/*.json` —— 那是第二个真相源，必然会漂。

- ⚠️ **`.github/workflows/backend.yml` 的 `paths` 过滤要补 `docs/api/**`**。
  契约文件在 `docs/` 下、校验它的测试在 `backend/` 下，不补的话「只改 openapi.yaml」
  的 PR 根本不触发后端流水线，漂移当场溜过去。

**交付产物（落地后）**

- `docs/api/openapi.yaml`：Auth 5 + Cards 6 个操作，`components` 里 6 个参数、
  4 个响应头、12 个可复用错误响应、17 个 schema。
- `.spectral.yaml` + `.spectral/functions/{hasErrorResponse,additionalPropertiesTrue,requiresHeaderParameter}.js`：
  任务书点名的四条 + 守既有约定的四条（`X-Client` 必填、错误响应必须
  `problem+json`、禁 offset 参数、v1.1 已删定义与 `editor` 角色不得复活）。
- `backend/tests/Api/Support/OpenApiContract.php`：`assertResponseMatchesContract()`
  与 `assertResponseViolatesContract()`，整份契约进程内只解析一次。
- `backend/tests/Api/OpenApiContractHarnessTest.php`：正反夹具。
- `backend/tests/Api/OpenApiDocumentTest.php`：文档守卫，含
  **`testEveryProductApiRouteIsDeclaredInTheContract()` —— §13.1 在 CI 里的强制点**，
  是 `RouteInventoryTest` 的对偶（那条守「谁可以不在 `/v1` 下」，这条守
  「在 `/v1` 下的都必须在契约里」）。今天空过，从 T-103 起开始咬。
- `.github/workflows/contract.yml`（T-011 并进 `shared` 流水线）、`npm run lint:api`。

**留给 T-010 的一条**（已结）：跨文件 `$ref` 已验证 Spectral 与 PHP 校验器都能解，
但 `openapi-generator` 没验过。**T-010 实测能解**，`npm run bundle:api` 没有用上。
不过生成器在**另一件事**上需要一份派生输入（`additionalProperties: true` 让它把
每个对象都当成了 Map），做法与那条预案同形：派生产物只供生成器消费，
契约一个字没动。见 `docs/api/README.md`。

**验收标准** `spectral lint` 通过；一个故意与契约不符的响应能让契约测试失败。

**规格引用** §13.1

---

## T-008 Android 工程骨架与 Convention Plugins

**角色** AND · **预估** 1.5 人日 · **依赖** T-001

**交付物**
- `android/` 按 §12.3 建模块：`app`、`core/{model,common,designsystem,ui,database,datastore,crypto,network,barcode,testing}`、`data/{auth,card,friend,sharing,sync}`、`sync`、`feature/*`、`widget`、`benchmark`（先建空模块）
- `gradle/libs.versions.toml`（**唯一**依赖声明处）
- `build-logic/convention/`：`ncards.android.{application,library,feature,hilt,room}`、`ncards.kotlin.serialization`
- `core:designsystem`：Material 3 Theme、Color tokens、Typography
- `ktlint` + `detekt` + Android Lint 配置（`HardcodedText`、`MissingTranslation`、`ContentDescription` 提升为 **error**）
- `minSdk 26`，德语为**默认** `values/`，英语为 `values-en/`

**实现要点** Gradle 强制模块依赖规则：`app → feature:* → data:* → core:*`；`feature:*` **之间禁止互相依赖**；`core:*` 不得依赖 `data:*`/`feature:*`；`sync` 不得被 `feature:*` 直接依赖。

**验收标准** `assembleDebug` 成功；故意在两个 feature 间加依赖会构建失败；德语为默认资源目录。

**规格引用** §4.3、§11.1、§12.3、§13.3

**交付产物（落地后）**

- 工具链：AGP **9.3.2** / Gradle **9.7.1** / Kotlin **2.4.10** / KSP 2.3.11，
  `compileSdk 37`（minor 1）· `targetSdk 36` · `minSdk 26`。
  wrapper 带 `distributionSha256Sum`。
- `android/gradle/libs.versions.toml`：唯一依赖声明处，只声明**已在用**的坐标，
  每条注明归属任务。
- `build-logic/convention/`：任务书点名的六个插件 + 四个内部插件
  （`ncards.module.graph` / `ncards.quality` / `ncards.android.compose` /
  `ncards.jvm.library`），每个的存在理由写在实现类头部。
- §12.3 的 30 个模块全建；`app` 与 `core:designsystem` 有实质内容，其余空壳。
- `.github/workflows/android.yml`（最小版，T-011 并进三条并行流水线）。

**⚠️ 三个 AGP 9 的坑**（写任何 convention plugin 之前先读 `android/README.md` 的同名段）

1. **不要应用 `org.jetbrains.kotlin.android`** —— AGP 9 自带 Kotlin 支持，应用它会被
   直接拒绝。jvmTarget 由 `compileOptions` 推导（已验证产物 class file major 61）。
2. **`CommonExtension` 不再带类型参数** —— AGP 8 的 `CommonExtension<*, *, *, *, *, *>`
   编译不过。
3. **不要写 `extension.apply { … }`** —— 它会绑到 `ExtensionAware` 的 Gradle 重载而非
   Kotlin 作用域函数，块内成员集体变成 "Unresolved reference"，报错完全不指向原因。

**⚠️ §13.3 点名的 `HardcodedText` 对本项目是空转的**

三条 lint 规则都按要求提升为 error 了，但按验收标准做反向验证时发现：
`HardcodedText` 检查的是 **XML 布局属性**（`android:text="…"`），而 §4.3 的选型是
「全 Compose，**无 XML 布局**」—— 往 `MainActivity` 里塞一个 `Text("hardcoded")`
再跑 `:app:lintDebug`，**结果是 BUILD SUCCESSFUL**。一条永远不会响的门禁比没有门禁
更糟，因为它让人以为这件事有人管着。

补法：`./gradlew checkComposeHardcodedText`（`build-logic` 的 `ComposeHardcodedText.kt`）。
`lint.xml` 里那三条**保留** —— Glance widget（T-254）与 RemoteViews 仍是 XML。
`ContentDescription` 有同样的局限，但 Compose 侧 Material3 把它做成了必填参数，
由编译器保证「有没有传」；「该给说明的图标传了 null」只能人工评审（T-452）。

**模块依赖规则的落地形态**（见 [ADR-0006](../adr/0006-android-module-graph-enforcement.md)）

§12.3 原文建议「Gradle 模块可见性 + 自定义 lint 规则」，两条都不成立：Gradle 没有
模块可见性机制，而 lint 要等到 `lintDebug` —— 验收标准要的是**构建失败**。
实际做法是 `build-logic` 的一张规则表 + 配置期检查，`./gradlew help` 即报错。
两道自检缺一不可：`./gradlew -p build-logic test`（规则表对不对）与
`android/tools/module-graph-selftest.sh`（规则真的接在构建上，注入 4 条违规）。

**留给后续任务的三条**

- **T-009**：`AndroidManifest` 的 `allowBackup="false"` / `dataExtractionRules` /
  `networkSecurityConfig` 有意**未加** —— 它们与 Room + SQLCipher 是同一件事的两面，
  分开做会出现「本地库加密了但允许云备份」的中间态。
- **T-010**（已结）：`openapi-generator` 能否解析 `openapi.yaml` 里指向
  `schemas/problem-details.schema.json` 的**跨文件 `$ref`** 仍未验证（T-007 只验了
  Spectral 与 PHP 校验器）。**T-010 实测 7.25.0 能解**，26 个 `code` 全部解出来，
  所以没有加 bundle 步骤。真正卡住的是 `additionalProperties: true`，
  出路同形（派生一份只喂生成器的副本）—— 见 T-010 的「交付产物」段。
- **T-011**（已结）：Kover 的 70% 阈值当时已配好但 CI 里没开。**T-011 打开了**，
  并纠正了这里的判断 —— 空壳模块**是**直接通过的，红的是「有源码、没单测」的模块。
  instrumentation（GMD，api 26 + 34）、`assembleRelease` 与 APK 大小回归同样已落地，
  详见下面 T-011 的「交付产物」。生成代码 diff 由 T-010 落地。

---

## T-009 Room + SQLCipher 与 core:crypto

**角色** AND · **预估** 2 人日 · **依赖** T-008

**交付物**
- `core:crypto`：32 字节随机 passphrase → Android Keystore AES-GCM 包裹 → 存 `EncryptedSharedPreferences`；`DbPassphraseProvider`
- `core:database`：Room + `net.zetetic:sqlcipher-android`，`cards` / `card_members` / `sync_outbox` / `sync_meta` 等表的初版 entity 与 DAO
- `AndroidManifest`：`android:allowBackup="false"`、`dataExtractionRules` 排除全部数据
- `networkSecurityConfig`：`cleartextTrafficPermitted="false"`

**实现要点**
- Keystore 密钥**不得**设 `setUserAuthenticationRequired(true)`——Widget 与 FCM 后台同步需要在无用户交互时读写数据库。此取舍写入代码注释并链接 §3.4。
- 令牌只存 `EncryptedSharedPreferences`，**绝不**明文写 SharedPreferences 或 Room。
- 生物识别应用锁只锁 UI，**不锁数据库**（M4 的 T-450 实现 UI 部分，此处只留接口）。

**交付产物（落地后）**

- `core:crypto`：`KeyWrapper`/`KeystoreAesGcmKeyWrapper`（AndroidKeyStore 的
  AES-256-GCM，别名 `ncards_db_master_v1`，密文 `[版本][12B IV][密文||tag]`）、
  `SecretStore`/`KeystoreSecretStore`、`DbPassphraseProvider` + `Passphrase`、
  `CryptoModule`（全 `@Binds`，实现一律 `internal`）。
- `core:database`：四张表的 entity + DAO、`NcardsDatabase`（version 1，schema JSON 入库）、
  `SqlCipher`、`DatabaseModule`；`WalletCard` 投影。
- `:app`：`allowBackup="false"` + `res/xml/data_extraction_rules.xml`（`cloud-backup`
  **与** `device-transfer` 两段全排除）+ `res/xml/network_security_config.xml`。
- 测试：`core:crypto` 7 个单测（首次 / 复用 / 恢复三条分支）+ 14 个仪器测试
  （真 Keystore 上的往返、IV 不重复、篡改/截断/换密钥必须失败、
  `isUserAuthenticationRequired == false`、passphrase 持久化与恢复）；
  `core:database` 17 个仪器测试（三条验收标准 + 四张表的 DAO 行为）。

**三处与任务书字面的偏差，都有记录**

1. **不引入 `androidx.security:security-crypto`**（[ADR-0007](../adr/0007-android-secret-storage-without-jetpack-security.md)）：
   该库已被 Google 停止维护，而它在本设计里只是「Keystore 包裹」之外的第二层容器 ——
   两层锚在同一个 Keystore 上，不提升防护强度。§3.4 与 §7.3 已加注指向 ADR。
2. **`sync_meta` 装的是三路合并的 base 快照，不是同步游标。** §5.4.1 与 T-250 明确
   游标存 Proto DataStore；而 §5.4.3 的字段级三路合并需要 base 落盘才能跨进程重启
   存活。两者不重叠。
3. **`cards` 上没有 `sync_state` 列**，由 `sync_outbox` 派生（`CardDao.observeWallet`）。
   一份状态存两处迟早漂移成两份，而漂移时没人知道该信哪个。

**四张表以外的没建**：`friendships` / `share_invitations` 是 M3（T-301+），
`users` 要等 T-107 的 username —— 现在建等于凭空猜列，而 §13.5 的 expand–contract
意味着猜错要付迁移代价。

**留给后续任务的四条**（详见 [`android/README.md`](../../android/README.md) 的「已知事项」）

- **T-011**（已结）：Kover 对 `core:database` 失真的问题**选了记豁免**
  （不并入仪器测试覆盖率 —— 那要求 PR 流水线起模拟器，会冲掉 12 分钟预算）。
  同时发现 `core:crypto` 有**一模一样**的问题（实测 27.2%），拿了同一条豁免；
  本文件此前「`core:crypto` 没有这个问题」的说法是错的。
- **T-011**（已结）：APK 大小基线已建，取 T-009 之后的 **release** APK
  = 20,352,900 字节（19.41 MiB），存 `android/app/apk-size-baseline.txt`。
  发布走 AAB 按 ABI 分发，用户实际下载增量约 3.5–5.7 MB。
- **T-011**（已结）：`assembleRelease` 已进 CI，**R8 一次通过**。SQLCipher 的 AAR
  自带 consumer proguard 规则（keep 了 native 方法、构造函数与 `mNativeHandle`），
  `app/proguard-rules.pro` 不需要额外条目 —— 这条结论已被实测证实，别再加一遍。
- **第一次改 schema 的任务**：`MigrationTestHelper` 要把 `schemas/*.json` 放进
  androidTest 的 assets，而常见写法在 AGP 9 上**配置期崩**
  （`DefaultAndroidLibrarySourceSet_Decorated` 无法转型），`getByName` 与 `named { }`
  都一样。细节写在 `core/database/build.gradle.kts` 的注释里。

**验收标准** 仪器测试：写入后用外部工具打开 db 文件无法读出明文；进程重启后能用 Keystore 解出 passphrase；卸载重装后为全新空库。

**验证情况**：31 个仪器测试已在 API 34 模拟器上跑绿（`core:crypto` 14/14、
`core:database` 17/17），三条验收标准逐条对应。

> ⚠️ 后两条只做到「重建全部对象」这一步 —— 测试自己也活在被杀的进程里，
> 真·重启与真·卸载重装是原理上做不到的。补齐要靠手工 `adb`，而那段**现在还跑不了**：
> `:app` 里没有任何人注入 `NcardsDatabase`，运行时不建库（实测 `databases/` 不存在），
> 依赖那两个模块只是为了让 Hilt 在 DI 根上看见 `@Module`。
> **第一个真实消费者是 T-153**，从那时起手工步骤才有意义 ——
> 步骤见 [`android/README.md`](../../android/README.md) 的「本地库加密」。

**规格引用** §3.4、§5.2、§5.4.3、§7.2（T08）、§7.3、§12.3

---

## T-010 core:network 与代码生成打通

**角色** AND · **预估** 1 人日 · **依赖** T-007, T-008

**交付物**
- `core:network:api`：`openapi-generator`（`kotlin` + `retrofit2` + `kotlinx-serialization`）产物，**提交入库但禁止手改**
- `core:network:impl`：OkHttp 配置、`X-Client` / `X-Request-Id` 拦截器、错误映射（HTTP + `code` → sealed `ApiError`）、重试策略
- Gradle task：重新生成并 diff，不一致即失败

**实现要点** `Json { ignoreUnknownKeys = true }` 是**硬要求**（前向兼容）；未知枚举值必须有 `UNKNOWN` 兜底分支，在生成器配置中保证。

**验收标准** 改动 `openapi.yaml` 后不重新生成 → CI 失败；`ApiError` 覆盖 §6.1 错误码表全部条目。

**规格引用** §3.10、§13.1、§13.6

**交付产物（落地后）**

- **`core:network:api`**：36 个文件的 `openapi-generator` 7.25.0 产物（`kotlin` +
  `jvm-retrofit2` + `kotlinx_serialization`），落在 `generated/` 下并提交入库。
  `AuthApi`（5 个 operation）/ `CardsApi`（6 个）+ 17 个 model + `infrastructure/`
  的序列化 adapter。方法名取自 `operationId`，与契约逐一对应。
- **build-logic 的 `ncards.openapi`**（第 11 个 convention plugin）：四个任务 ——
  `prepareOpenApiGeneratorInput` / `generateApiClient` /
  `generateApiClientForVerification` / `checkApiClientUpToDate`（挂在 `check` 上）。
  手写 `JavaExec` 而不是用 `org.openapi.generator` 插件，理由是配置缓存
  （仓库全局开着，赌第三方插件的兼容性没有退路）。
- **`core:network:impl`**：`NetworkConfig`、`ClientHeaderInterceptor` /
  `RequestIdInterceptor` / `RetryInterceptor`、`ApiError`（26 个 code 一码一类型 +
  3 个传输层）、`FieldError`（9 项词表 + `UNKNOWN`）、`ApiErrorMapper`、
  `ApiResult` + `execute {}`、`NetworkModule`；`AndroidManifest` 声明 `INTERNET`。
- **`:app`**：`AppNetworkModule` 绑 `NetworkConfig`（`BuildConfig.API_BASE_URL` 按
  buildType，debug → staging；`X-Client` 由 `VERSION_NAME` / `VERSION_CODE` 拼出）。
- **CI**：`android.yml` 的 `paths` 加 `docs/api/**`（否则只改契约的 PR 跑不到 diff
  检查，验收标准第 1 条落空），新增一步 `checkApiClientUpToDate`。
- **测试**：`core:network:impl` 36 个单测（错误码全覆盖、problem 映射与四个响应头、
  重试边界 9 条、`ignoreUnknownKeys` 与未知枚举兜底、`NetworkConfig` 校验）。

**验收标准的落实方式**

1. 「改 `openapi.yaml` 不重新生成 → CI 失败」：`checkApiClientUpToDate` 把契约重新
   生成到 `build/` 再与 `generated/` **逐文件比对**（不是 `git diff` —— 后者把
   「产物过期」和「工作区本来就脏」混成同一种失败）。实测：往 `CardPlacement` 加一个
   可选字段 → 该任务红并点名 `CardPlacement.kt`；还原后转绿。
2. 「`ApiError` 覆盖 §6.1 全表」：`Problem.toApiError` 是一个**没有 `else` 的
   `when`**，直接 `when` 生成的 `Problem.Code`。契约新增一个 code → 重新生成 →
   枚举多一项 → 那个 `when` 不再穷举 → **编译失败**。这比运行时断言早，
   也是本任务作为 §6.1 错误码表「第四方」的钉法。`ApiErrorCoverageTest`
   再补一层：两个 code 映射到同一类型（复制粘贴漏改）也会红。

**四处与任务书字面的偏差，都有记录**

1. **契约补了一行 `info.license.identifier`。** `openapi-generator` 的 spec 校验器
   把 OAS 3.1 里**可选**的 `identifier` 当必填，不写就 `Error count: 1` 直接中止。
   出路是 `--skip-validate-spec`（会连同「$ref 指不到」这类真问题一起关掉）或者补上
   这一行。选后者：它是纯元数据，§13.6 的禁止项一条不沾。
   `npm run lint:api` 与 `backend/tests/Api`（264 个）都仍然绿。
2. **给生成器派生了一份剥掉 `additionalProperties: true` 的契约副本**
   （`build/openapi-input/openapi.json`，不入库）。§13.1 第 5 条要求所有 schema 都
   `additionalProperties: true`，而生成器把「有 properties **又**有
   additionalProperties」的 schema 当成 Map，后果是 15 个模型全部继承一个语法非法的
   `HashMap<String, Any>()()`、嵌套模型被打上 `@Contextual`。生成器侧没有开关。
   **契约一个字都没动** —— 这正是 `docs/api/README.md` 结尾预留的那条出路的形态。
   只剥同时声明了 `properties` 的那些：`Problem.current` / `debug` 是真的自由形态
   对象，剥了会从 `Map<String, JsonElement>` 退化成裸 `Any`。
3. **覆写了两个 mustache 模板各一行**（`data_class_{req,opt}_var.mustache` 的
   `{{^isEnumRef}}`）。`$ref` 出去的枚举（`BarcodeFormat` / `CardRole`）会被上游模板
   误判成「需要外部序列化器」而加 `@Contextual`，运行时报
   `Serializer for class 'BarcodeFormat' is not found`。未改动的上游原文一并入库，
   `diff` 应当只有一行差异 —— 见 `core/network/api/templates/README.md`。
4. **跨文件 `$ref` 不需要 bundle。** T-007 留的那条「解不开就加
   `npm run bundle:api`」的预案**没有用上**：`openapi-generator` 7.25.0 能解
   `components/schemas/Problem` → `./schemas/problem-details.schema.json`，
   26 个 `code` 全部解出来。三个消费者至此都验过了。

**留给后续任务的五条**

- **T-011**（已结）：`koverVerify` 已打开。`:core:network:api` 的豁免保留，
  `core:database` / `core:crypto` / `core:designsystem` 各加了一条有理由的豁免。
  另外发现 `:core:network:impl` 被 Dagger/Hilt 的**生成代码**摁在 64.5%，
  `Coverage.kt` 现在把生成代码排除出分母 —— 那比任何豁免都重要。
- **T-011**（已结）：`checkApiClientUpToDate` **仍然挂在 `android.yml`**，没有并进
  `shared`。搬过去要连着 JDK + Android SDK 一起搬，等于在关键路径上多装一遍 SDK，
  而两条 job 都在同一个 `pr-gate` 下，门禁效果相同。见 [ADR-0008](../adr/0008-ci-gate-topology.md)。
- **T-150**：认证的两个插槽都空着 —— `Authorization` 由一个拦截器加，401 静默刷新
  由 OkHttp 的 `Authenticator` 做（不是拦截器，在连接层重发，与现有顺序无关）。
  生成器吐的 `infrastructure/ApiClient.kt` 与 `auth/HttpBearerAuth.kt` **已经被
  `.openapi-generator-ignore` 挡掉**，不要去捡它们。令牌存储用 `core:crypto` 的
  `SecretStore`（那里的 KDoc 早就留好了位）。
- **T-150 / T-153（第一批调用方）**：生成的每个方法都带一个必填的
  `xClient: String` 参数 —— 那是契约逐操作声明 `X-Client` 的必然结果，不是第二个
  开关。传 `NetworkConfig.clientHeader`；真正发出去的值由
  `ClientHeaderInterceptor` 覆盖决定。
- **写 SAM lambda 的人**：AGP 9 的 lint（`ExperimentalDetector`）在分析 `fun interface`
  的 SAM 转换时会崩，整个 `lintAnalyzeDebug` 失败，而报错只说文件名不说行号。
  `NetworkModule.provideSleeper` 因此写成 `object :`，注释里有完整线索。

---

## T-011 CI 流水线（GitHub Actions）

**角色** 全员 · **预估** 1.5 人日 · **依赖** T-002, T-007, T-008

**交付物**
- **PR 流水线**（目标 ≤ 12 分钟，三条并行）：
  - `backend`: cs-fixer → phpstan → deptrac → unit → integration（services: pg/redis/vault）→ api-contract
  - `android`: ktlint → detekt → lint → unit → assembleDebug
  - `shared`: spectral → gitleaks → openapi-codegen-diff → commitlint
- **main 合入流水线**：全量 PR 检查 → Android instrumentation（Gradle Managed Device, api 26 + api 34）→ 构建后端镜像推 GHCR → 部署 staging → 迁移 → 冒烟测试 → 构建 AAB 上传 Play Internal
- 通用门禁：`gitleaks` 0 命中；**敏感日志扫描**（禁 `dump(`、`var_dump`、`Log.d/v/i` 打印实体、禁日志中插值 `barcode_value`/`email`）；`TODO` 必须带 issue 编号
- Doctrine 迁移检查：`doctrine:schema:validate` 通过 + 迁移 `up`/`down` 往返

**验收标准** PR 流水线在空项目上全绿且 ≤ 12 分钟；故意提交一个含 `Log.d(card)` 的 diff 会被阻断。

**规格引用** §13.3、§14.3

**交付产物（落地后）**

- **拓扑**（[ADR-0008](../adr/0008-ci-gate-topology.md)）：`pr.yml` 一个入口 →
  `changes` 过滤 → `backend` / `android` / `android-release` / `shared` 四条并行
  （前三条 reusable workflow）→ **`pr-gate`** 汇总。`pr-gate` 是**唯一**的
  required status check，`scripts/setup-branch-protection.sh` 已改。
  `contract.yml` 与 `commit-conventions.yml` 并入 `shared.yml` 后删除。
- **`shared.yml`**（总是运行）：spectral → gitleaks（钉死 v8.30.1 + sha256）→
  扫描器自检 → 敏感日志扫描 → TODO 检查 → commitlint（分支每条 commit + PR 标题）。
- **通用门禁的四个脚本**（`scripts/ci/`）：`check-gitleaks.sh`、
  `check-sensitive-logs.sh`、`check-todo-issue-refs.sh`、`sensitive-scan-selftest.sh`，
  外加 `check-apk-size.sh`。`.gitleaks.toml` 三条豁免，每条写了理由。
- **`main.yml`**：全量四条 → 仪器测试（GMD，api 26 + 34）+ 后端镜像推 GHCR
  （`ghcr.io/ht-lin/n-cards-backend:{sha,main}`）+ AAB 产物。
- **backend**：`tools/migration-check.sh`（up/down 往返 + `schema:validate`），
  并进 `composer qa`。
- **android**：`koverVerify` 打开；GMD 设备矩阵（`build-logic/ManagedDevices.kt`）；
  `assembleRelease` + APK 大小基线（`app/apk-size-baseline.txt`）。

**实测（PR #17，两趟都全绿）：冷缓存 13m01s，热缓存 8m21s。验收标准 ≤ 12 分钟 —— 达标。**

| job | 冷缓存 | 热缓存 |
|---|---|---|
| `changes` | 5s | 5s |
| `shared` | 21s | 21s |
| `backend` | 1m27s | 1m32s |
| `android-release` | 7m26s | 7m29s |
| **`android`** | **12m42s** | **8m01s** ← 关键路径 |
| `pr-gate` | 3s | 3s |
| **墙钟** | **13m01s** | **8m21s** |

冷缓存那趟 `android` 内部的分布（合计 12m36s）：

```
205s  Android Lint            188s  assembleDebug
101s  模块依赖规则单测         98s  模块依赖规则自检
 51s  单元测试                 28s  ktlint
 26s  koverVerify              25s  Setup Android SDK
```

**第一趟为什么慢**：Gradle 缓存是空的（「Setup Gradle」只花了 2s，正是没找到任何东西
可恢复的样子）。同一条分支上的第二趟就降到 8m01s，`android-release` 与 `backend`
几乎不变 —— 差的那四分半全在 Gradle 那一侧，与流水线结构无关。

⚠️ **别拿冷缓存那个数字去改结构。** 每条新分支的第一个 PR 都会经历一次冷启动，
那是缓存的性质，不是流水线的问题。真需要压时间时，手上现成的杠杆是把
`:app:lintDebug`（205s）拆成第四条并行 job —— 与当初把 `android-release` 拆出去
是同一个理由，能直接从关键路径上砍掉三分半。**现在不需要，别提前拆。**

**四处与任务书字面的偏差，都有记录**

1. **`openapi-codegen-diff` 留在 `android`，没并进 `shared`**（§14.3 划给 shared）。
   `checkApiClientUpToDate` 要 JDK 17 + Android SDK + Gradle，搬过去等于在关键路径上
   再装一遍 Android SDK。两条 job 都挂在同一个 `pr-gate` 下，门禁效果完全相同。
   前提是 `changes` 的 `android` 过滤器含 `docs/api/**` —— 它含。见 ADR-0008 末尾。
2. **`assembleRelease` 进了 PR 流水线**（§14.3 的 PR 流程只列到 `assembleDebug`）。
   §13.3 把它列为 CI 阻断项，只在 main 上跑意味着 R8 断裂与体积膨胀要合入之后才发现，
   而 SQLCipher 的 JNI 反射正是 R8 的经典断裂点。做成**独立并行 job**，不占关键路径。
3. **staging 部署 / 迁移 / 冒烟测试没做** —— 那是 T-012 的交付物（Ansible playbook
   目前只有 README）。`main.yml` 末尾留了接法说明，没有写永远 skip 的空壳 job：
   Actions 界面上「跳过了」和「跑了但什么都没做」分不开。
4. **AAB 只产出不上传 Play** —— 上传要正式签名密钥与 Play service account，属 T-456
   （release 变体现在连 `signingConfig` 都没有）。产出的是**未签名** AAB。

**打开 `koverVerify` 时纠正了两处此前的记载**

- **T-008 说「空壳模块分母为 0，跑 verify 会得到假绿」—— 反了。** 实测 `main` 里没有
  `.kt` 的模块直接通过，而**有源码、没单测**的模块会红在 `0.000000`。所以打开这个
  门禁的成本不是「假绿变真红」，是要为每个有源码的模块给出一个答案。
- **`android/README.md` 说「`core:crypto` 没有这个问题」—— 那句话是错的。** 实测
  27.2%：`KeystoreAesGcmKeyWrapper` / `KeystoreSecretStore` 要真的 AndroidKeyStore，
  14 个测试全在 `androidTest`。它与 `core:database` 是同一个问题，拿同一条豁免。

比豁免更要紧的是**分母**：Dagger/Hilt 的生成代码原本整个计进来，把
`:core:network:impl` 摁在 64.5%（手写的 `RetryInterceptor` 其实是 32/33）。
`Coverage.kt` 现在排除生成代码，该模块随即过线。**四条豁免**（`network:api` /
`database` / `crypto` / `designsystem`）逐条写了理由。

> ⚠️ `core:crypto` 与 `core:database` 选豁免而不是并入仪器测试覆盖率，是因为后者要求
> PR 流水线里起模拟器，会把 12 分钟预算直接冲掉。**这个取舍应在 M1 重新评估** ——
> 那时 `data:*` 有了真实代码，覆盖率的分母才有代表性。

**仪器测试：api 26 这一档第一次跑通**

T-009 的 31 个测试此前只在 api 34 上验过。T-011 在本地 GMD 上跑了两档
（crypto 14 + database 17，共 62 次）**全绿** —— Keystore 与 SQLCipher 在
Android 8.0 上与 api 34 没有行为差异，`minSdk` 这一档的风险到此关闭。

> ⚠️ **上面这段当时只在本地成立，CI 上一个仪器测试都没跑过 —— 门禁是空的。**
> 这一点在交付时没有被识别出来：`main.yml` 的 `instrumentation` job 连挂四次，
> 而每次失败都发生在**模拟器启动阶段**，`:core:crypto` 与 `:core:database` 的任务
> 根本没被执行到。CI 上的仪器测试门禁**直到四层排障全部修完才真正生效**，
> 见下面「CI 上跑通 GMD 又踩了四层」。
> **教训**：「本地全绿」不构成「CI 上也能跑」的证据，别拿它当验收依据。

配置上踩到三个坑，都写进了 `ManagedDevices.kt` 与 `gradle.properties`：
AGP 默认拒绝 API ≤ 26（要 `allowOldApiLevelDevices`）；`testedAbi` 必须显式写
`x86_64`（AGP 10 会改默认，而 x86_64 镜像不支持 NDK translation）；
两个模块 × 两档设备会起**四台**模拟器，16 GB 机器上会把 Gradle daemon 挤死
（`maxConcurrentDevices=1`）。

**CI 上跑通 GMD 又踩了四层（交付后补）**

`main.yml` 的 `instrumentation` job 在交付后连红四次。四层病因**各自独立**，
修好一层才露出下一层，而报错的「形状」几乎不变（都是
`Unable to start Android emulator` + `Deleting unbootable snapshot`）——
差别只在内层那一句。**按形状判断必然绕路**，排查中因此绕了两次。

| 层 | 判据（看这个，不要看形状） | 修法 | PR |
|---|---|---|---|
| 磁盘不足（一） | job 红了但**一行日志都没有** | 清理 dotnet / ghc / boost / CodeQL | #20 → #21 |
| `/dev/kvm` 权限 | `x86_64 emulation currently requires hardware acceleration!` | udev 规则改 0666 | 同上 |
| GPU 模式失效 | `gpuChoiceBasedOnGpuOptions` | `-P…emulator.gpu=swiftshader` | #22 / #23 |
| 磁盘不足（二） | `Error message from emulator process = []`（**零输出**） | 清理加狠到 39 G | #24 / #25 |

**最终验证**（run `33526439179`）：62 个测试（31 × 两档）在 CI 上**首次真正执行**，
`failures=0 errors=0 skipped=0`，`BUILD SUCCESSFUL in 23m 19s`，job 耗时 26 分钟。
磁盘全程最低点 21 G。§13.3 的「关键路径在 GMD 上通过」自此才算成立。

⚠️ **三点值得记住**：

1. **`#20` 曾被强推丢掉。** 修复合入 23 分钟后 `main` 被重置回旧提交，两处修复一并消失，
   于是同一个病因又红了一次。`main` 分支保护当前**服务端未生效**
   （Free 私有仓库，403），见 `CONTRIBUTING.md` §3 的已知风险说明。
2. **`aosp_atd` 镜像被两次误判为病因，两次都错。** 真实差异是**执行顺序**不是镜像 ——
   api 34 排在后面，轮到它建 snapshot 时磁盘已被前面吃光。别去动它。
3. **`instrumentation` 耗时 26 分钟**，只在 `main` 上跑，不受 §14.3 那 12 分钟
   PR 预算约束（§13.3 的编排本就如此）。

排障判据与实测数字写在 `android/README.md`；诊断设施（每 10 秒采一次磁盘余量的
后台采样器）在 `main.yml` 里，**那不是调试残留，别删**。

**留给后续任务的三条**

- ~~**T-012**：`main.yml` 末尾的 `deploy-staging` 注释块。~~ **已接上** ——
  注释块换成了真的 job，调用 reusable 的 `.github/workflows/deploy.yml`。
- **T-456**：`play-internal` 注释块 + release 变体的 `signingConfig`。
- **加新流水线的人**：必须同时加进 `pr.yml` 里 `pr-gate` 的 `needs`。不加，它红了
  PR 照样能合 —— 这是 ADR-0008 记的唯一失效模式。

---

## T-012 Hetzner staging + Ansible 自动部署

**角色** OPS/BE · **预估** 2 人日 · **依赖** T-003, T-011（Q1 需先决策）

**交付物**
- Hetzner（Nürnberg）CCX23 主机；`infra/ansible/` playbook（含 SSH 加固：仅密钥、禁 root、`fail2ban`、非 22 端口）
- Caddy 自动 TLS + 安全响应头：HSTS `max-age=63072000; includeSubDomains; preload`、`X-Content-Type-Options: nosniff`、`Referrer-Policy: no-referrer`、`X-Frame-Options: DENY`、`CSP: default-src 'none'`
- `api.staging.n-cards.de` 部署 hello world，`main` 合入自动部署
- `sops`（age）加密的生产/staging 配置随 Ansible 下发；`.env` 不入库
- 生产发布流水线骨架：手动触发 + approval → 前置数据库快照 → 滚动重启 → 迁移 → 健康检查 → 失败自动回滚上一镜像 tag

**实现要点** staging **只用合成数据**，严禁复制生产数据（哪怕脱敏）。Vault unseal key 绝不进 CI/Ansible secrets。

**验收标准** 一次 `main` 合入能自动部署到 staging 并通过冒烟测试；`curl -I` 能看到全部安全头；`X-Powered-By`/`Server` 版本回显已禁用。

**规格引用** §7.4、§14.1、§14.2、§14.3

---

### 交付产物（落地后）

> ✅ **首启已完成，验收标准已实测**（2026-09-04）。
> [`staging-first-boot.md`](../runbooks/staging-first-boot.md) 的 13 步已走完，
> 实测数字见下面的表。
>
> 首次交付时这里写的是「真实主机上的首启尚未执行」—— 那之后还差一个 bug：
> sops vars plugin 的配置项放错了 section，**凭据从未被解密**（#28）。
> 修掉它之前，`deploy-staging` 是红的。所以「代码与文档都交付了、本地验证全过」
> 与「这条链真的能跑通」之间隔着的不止是人工步骤。
>
> **实测的那一趟**：run
> [`33915013782`](https://github.com/ht-lin/n-cards/actions/runs/33915013782) ——
> `main` 合入 → 四条 PR 检查 + `instrumentation` + `backend-image` →
> `deploy-staging`（Ansible `ok=36 changed=8 failed=0`，健康判定 `ok`：
> `/health/live` 200 + `/health/ready` 200）→ 冒烟 4 组断言全过。
> `/health/ready` 200 是最有信息量的一条：它同时证明了 Postgres 可达、
> Vault 已 init + unseal、AppRole 凭据真的解开了。
>
> ✅ **三段演练已按 [`drill-rollback-and-seal.md`](../runbooks/drill-rollback-and-seal.md)
> 执行完毕**（2026-09-05）：快照路径、封印判定、自动回滚。§13.8 的
> 「需要运维介入的事必须演练过才算交付」至此满足，**T-012 done**。
>
> ⚠️ **但演练的记录做得不好** —— runbook 末尾那张记录表只落下了一个数字
> （封印后 `vault status` rc=**2**）。执行过程与结果符合 runbook 的期望，
> 但耗时、快照大小、unseal 实际人数这些都没记，事后也补不回来。
> 下面的表里凡是写「未记录」的，就是这个原因，不是「没做」。
> 教训写在「留给后续任务」的最后一条：**记录表要在演练时同步填，不是事后回忆。**

**交付物**

- **`infra/ansible/`**：3 个 role（`hardening` / `docker` / `ncards_stack`）、
  3 条 playbook（`site.yml` provision / `deploy.yml` 部署 / `rollback.yml` 回滚）、
  两套 inventory。collection 与 sops 二进制版本全部钉死（sops 还钉了 sha256）。
- **sops(age)**：仓库根 `.sops.yaml`，**staging 与 production 两把不同的密钥** ——
  生产私钥放 `production` Environment 的 environment secret，
  于是「只有过了 approval 的 job 才解得开生产配置」由 GitHub 权限模型强制。
- **CI**：reusable 的 `.github/workflows/deploy.yml`，被 `main.yml` 的
  `deploy-staging` 与 `deploy-manual.yml`（`workflow_dispatch`，含 `guard` 前置校验）
  共用。生产发布骨架与「不等 main 合入就能排练」是同一个文件。
- **门禁**：`scripts/ci/check-sops-encrypted.sh` 挂进 `shared.yml`；
  `.gitleaks.toml` 加一条 `targetRules` + `paths` 双限定的豁免；
  `sensitive-scan-selftest.sh` 加 4 条断言守这个边界。
- **Caddy**：全局块补 `email` / `acme_ca`（env 参数化）。
- **`scripts/ci/smoke-staging.sh`**：把验收标准写成了 12 条可执行断言。
- **文档**：ADR-0009 / ADR-0010；runbook `staging-first-boot.md` 与
  `deploy-and-rollback.md`；`vault-unseal.md` 的路径与 `COMPOSE_FILE` 从
  「以实际为准」改成确定值，`--network` 补成实际的 `ncards_backing`。

**已验证（本地，可复现）**

| 验证项 | 结果 |
|---|---|
| `ansible-playbook --syntax-check` × 3 条 playbook × 2 套 inventory | 全过（ansible-core 2.18.1 + 钉死的 collection） |
| 三段叠加链 `docker compose config` | 通过；`app` image 取到 sha tag、`VAULT_TOKEN` 为空、`vault-init` 被 profiles 排除 |
| 删掉 `VAULT_ROLE_ID` 后 compose | **如期硬失败** —— 证明首启占位值方案是必需的，不是可有可无 |
| `env.j2` 渲染 | 13 个变量全部正确落位 |
| 健康判定 6 种输入 | `ok` / `vault_sealed` ×2 / `broken` ×2 / 首启跳过，全部符合预期 |
| 快照与镜像的保留切片 | 13 份留 10 → 删最旧 3 份；7 个镜像留 3 个 sha tag（排除当前与 `:main`） |
| `caddy validate`（改后的 Caddyfile） | Valid configuration |
| 全部扫描器 + 13 条自检断言 | 全绿，仓库 gitleaks 0 命中 |

**与任务书字面的偏差（7 处，逐条记录）**

1. **机型：实机 2 vCPU / 4 GB / 40 GB，不是 §4.1 假设的 CCX23（4c/16G/160G）。**
   这是最重要的一条。playbook 不假设规格 —— `preflight.yml` 每次实测并落盘
   `/opt/ncards/state/host-facts.json`，断言下限是 2 vCPU / 3400 MB / 15 GB 可用。
   为这台小机器额外做了三件事：**2 GB swapfile**（Hetzner 云镜像默认无 swap，
   部署峰值会触发 OOM killer 杀掉 postgres，而部署流程会把它误读成 `broken`
   然后做一次无意义的回滚）、日志轮转从「该有」变成必需、镜像 prune 只清 dangling
   并保留 3 个 tag。**生产上线时按 §9.3 的扩容路径重新定机型，别直接抄 staging。**
2. **「滚动重启」做不到，实际是几秒的 stop-then-start。** `app` 单副本、无
   `container_name`，Caddy 的 `reverse_proxy app:8080` 是启动时解析一次的静态
   upstream。真滚动需要 `scale 2` + Caddy dynamic upstreams + 健康感知，
   是另一张卡的量。**M0 接受几秒中断**，留给 M1 / T-405 重估。
3. **「hello world」用 `/health/live`，不新增路由。** `backend/tests/Api/RouteInventoryTest.php`
   断言每条路由要么在 `/v1/` 下、要么在显式白名单里；为一句 hello world 去动那张表，
   还要面对 §13.6 的演进约束与 openapi 契约归属，不值。而 `/health/live` 本来就被
   `ApiSurface` 豁免了 `X-Client`，裸 `curl` 可达 —— 它同时就是验收标准本身。
4. **静态法律页（§8.6）移出 T-012。** 本站点的 `CSP: default-src 'none'` 与 HTML
   页面直接冲突（页面自己的 CSS/字体全被拦），且 Impressum 需要真实法律主体、
   Datenschutzerklärung 需要终版子处理者清单。它属于 `n-cards.de` 的站点任务
   （同时承载 Play 要求的站外 `/delete-account`），**不属于 `api.` 子域**。
   已改 `infra/caddy/README.md` 的归属。
5. **部署前快照是简易 `pg_dump`，不是 §7.4/§14.2 的备份体系。** 无异地、无 WAL、
   无客户端加密（ADR-0010）。⚠️ **生产启用前必须先由 T-406 的 age + Storage Box
   方案替换** —— 这句话写在三个地方（ADR、runbook、生产 inventory 的 group_vars），
   因为它很容易被忘掉。
   ⚠️ **而且它只有 Postgres —— `vault_data` 不在里面，也不在任何别的地方。**
   见下面「未缓解的风险」。
6. **没有给 `pr.yml` 加 `infra` 过滤器，也没加新流水线。** T-012 真正需要的 PR 侧
   检查只有一条（sops 文件确实加密了），它挂进了本来就无条件运行的 `shared.yml`：
   零拓扑改动、零 `pr-gate` 改动、`scripts/setup-branch-protection.sh` 一字未改。
   ADR-0008 记的唯一失效模式是「加了新流水线却忘了加进 `pr-gate` 的 `needs`」——
   每加一条就多一次踩坑机会，为一个几十毫秒的文本扫描付这个代价不划算。
   （下一个人一定会问「为什么 infra 没有自己的 job」，答案在这里。）
7. **生产 inventory 是骨架，不指向真实主机。** 任务卡要的是「生产发布流水线
   **骨架**」，流程完整可读，但 `inventory/production.yml` 的 host 是占位的 ——
   对它跑 playbook 会在 DNS 解析那步失败。这是刻意的，比连上一台错的机器好。

**一个已知的、刻意接受的安全边界**

deploy 用户在 `docker` 组里，而 **`docker` 组等价于 root**
（`docker run -v /:/host` 就拿到宿主机根）。既然 root 等价权限在给 sudo 之前
就已经存在，就**给了它 NOPASSWD sudo** —— 不给换不来实质隔离（docker 那条路
照样通），只换来「provision 之后再也改不了这台机器」，因为加固之后 root 的
SSH 登录已经关了。

所以「禁 root 登录」在这里的作用是**缩小攻击面与改善审计**，
**不是权限隔离**；真正的边界是**谁持有 `DEPLOY_SSH_KEY`**。
`authorized_keys` 的 `command=` 能真收紧，但与 Ansible 不兼容 —— 明确不做。
记在 ADR-0009 的负面 Consequences 里。

**⚠️ 未缓解的风险：`vault_data` 没有任何备份**

T-012 交付后，全仓**没有任何地方**备份 `vault_data` 卷 —— 它只出现在 compose 的
卷定义和几条「别 `down -v`」的警告里。而 §9.4 是明确要求的：

> **Vault 数据丢失 = 全部卡数据不可解密**，因此 Vault 存储卷单独每日快照。

它归 T-406（compose 里的 `backup` 服务还是注释），**范围划分没错，但风险从
T-012 落地那天起就存在**，不是等到 T-406 才开始。写在这里是因为交付时漏记了：
ADR-0010 与本段原本只讲了 `pg_dump` 那一半，读起来像是备份缺口只有 Postgres。

- **staging**：合成数据，丢了就是重新 init + bootstrap + 重签 AppRole，不致命。
- **生产**：丢了 = 全部卡数据**永久**不可解密。
  ⚠️ **unseal key 救不了** —— 它解的是 master key，master key 解的是那个卷；
  卷没了，5 把 key 拼齐也没有东西可解。「unseal key 离线存了 ≥2 个物理位置」
  保的是**可用性**，不是**持久性**，两者很容易被混为一谈。

给 T-406 的技术前提：`vault.hcl` 用的是 **`file` 后端不是 raft**，
所以 `vault operator raft snapshot` 用不了；运行期直接 `cp -r /vault/file`
不保证一致性。可行的是「停 Vault 再拷」或「文件系统 / Hetzner 卷级快照」，
且必须配一次真实恢复演练（§9.4：未演练的备份视为不存在）。

**实测数字 · 首启**（run `33915013782`，2026-09-04）

| 项 | 值 |
|---|---|
| `host-facts.json` 实测（vCPU / RAM / 磁盘） | **2 vCPU / 3814 MB / 根分区可用 29.1 GB**；`declared_machine_type: CX22 (2 vCPU / 4 GB / 40 GB)` —— 与上面偏差 1 一致，`preflight` 的下限（2 / 3400 MB / 15 GB）通过 |
| 首次部署耗时 | **`deploy-staging` job 1m36s**，其中 playbook 本体约 64s（20:40:21 → 20:41:25），冒烟 < 1s |
| `main` 合入 → staging 可用的端到端耗时 | **29m35s**（20:11:51 → 20:41:26）—— 与「预计 ≈ 30 分钟」吻合，其中约 24 分钟是 `instrumentation`。这正是下面「留给后续任务」第一条的实测依据 |
| 部署后磁盘占用与余量 | `/dev/sda1` 38G，已用 **6.6G**，可用 **30G**（19%） |
| 后端镜像大小 | **401 MB**（`:main` = `7d56458`，含 T-101 的第一条迁移）；T-012 那三个 sha（`f4adf3c` / `6318292` / `14b8b60`）都是 **390 MB** —— 演练时在主机上补的。⚠️ 这是 `docker images` 报的**解压后本地占用**，不是 GHCR 上的传输体积（后者按层压缩，通常小一半左右）。CX22 只有 40 GB，`prune 只留 3 个 sha tag` 那条保留策略按这个量级算是 ~1.2 GB，不吃紧 |
| 证书首签耗时 | 待测（首次 ACME 签发发生在人工走 runbook 第 7 步时，不在这趟 CI 日志里） |

> 首次部署这一趟里「跳过部署前快照」是**期望行为**（`postgres 没有在运行 ——
> 这台机器上还没有数据库`，见 runbook 第 7 步）。也就是说**快照那条路径在这趟里
> 没有被执行到** —— 它的真实验证是下面的演练 0。

**实测数字 · 演练**（2026-09-05，按 [`drill-rollback-and-seal.md`](../runbooks/drill-rollback-and-seal.md) 在 staging 上执行）

| 项 | 值 |
|---|---|
| **演练 0**：`pg_dump` 快照路径 | ✅ 已执行（`报告快照` 出现，非空断言通过）。快照大小与耗时**未记录** |
| T-101 首次真实迁移 | ✅ 在真机上跑过。耗时**未记录** |
| **演练 1**：封印后 `vault status` 退出码 | **rc = 2**，与 `healthcheck.yml` 把 rc ∈ {1,2} 映射成 `vault_sealed` 的假设一致（此前只有一行「1.18 实测都给 2」的注释支撑）。**这是本次演练唯一被记下来的数字**，也恰好是最该被记的一个：判错的后果是把一次封印当成部署故障，回滚掉一个好镜像，然后仍然 503 |
| **演练 1**：`vault_sealed` 是否触发回滚 | ✅ **否**，与期望一致。`current-image` / `previous-image` / app `StartedAt` 均未变化。unseal 人数与耗时**未记录** |
| **演练 2**：自动回滚 | ✅ 现网退回上一个好镜像，`/health/ready` 恢复 200，且 **Vault 仍保持解封**（验证了 `rollback.yml` 里「只重启 app，刻意不动 vault」那段推理）。总耗时**未记录** |
| 演练中发现并修掉的问题 | **1 个**：`deploy.yml` 里 `include_role` 没有 tag，`--tags health` 在解析期就把它滤掉了，role 根本没被打开 —— 表现是一个**空的 PLAY RECAP**，看起来像「跑完了、没事」。已标 `tags: [always]` 修掉（见该处注释）。⚠️ 这正好是演练的意义：这个 bug 让 `deploy-and-rollback.md` 里「先只看判定，不动现网」那条排障路径**静默失效**，而它在本地 `--syntax-check` 与任何 CI 里都不会暴露 |
| **未覆盖**：`verdict == broken` → 回滚 → 重新判定 | ❌ 本次未验。演练 2 走的是 `rescue`（拉不到镜像），两条回滚入口不同。要补需造一个「容器 healthcheck 绿、外部探针红」的替身镜像 —— 见 runbook 文末。M0 接受不做，**T-405 / 上线前补** |

**留给后续任务**

- **`deploy-staging` 的 `needs` 该不该去掉 `instrumentation`。** 它实测 26 分钟，
  把「合入到 staging 可用」拖到约 30 分钟，而 Android 仪器测试与后端能不能部署
  **没有因果关系**。拿掉能省 20 多分钟。没有顺手改，是因为那是 T-011 已记录在案的
  编排 —— 留给 M1 显式决定。
- **T-405**：Grafana 经 Caddy basic auth 暴露；`LOG_LEVEL` 目前只是占位，
  monolog 的实际接线在那边。
- **T-406（其中「`vault_data` 每日快照」是全部后续任务里最该先做的一件）**：
  ⚠️ **`vault_data` 的每日快照 —— 见上面「未缓解的风险」，生产上这一条不做完
  就等于把「全部卡数据永久不可解密」挂在一个卷上**（§9.4；注意 `file` 后端
  没有 raft snapshot 命令）；
  用 pgBackRest/wal-g + age + Storage Box **替换**本次的 `pg_dump`；
  `restore-from-backup.md`；Vault Agent 把 `secret_id` 换成 24h 自动续期（§7.4）。
- **法律页**：归 `n-cards.de` 站点任务，不在 `api.` 子域。
- **ansible-lint / yamllint 门禁**：本次用 `ansible-playbook --syntax-check` 顶着
  （跑在真正会用到它的地方，零额外依赖）。要加的话记得它同样可以挂进 `shared`。
- **补上 `verdict == broken` 那一档演练**（见上面演练表的最后一行）。它验的是
  「一个能起来但坏掉的版本被自动挡下来」—— 真实发布事故里最常见的形状。
  归 T-405 / 上线前。
- **⚠️ 演练记录要在演练时同步填，不是事后回忆。** 本次三段都跑了，
  但记录表只落下一个 `rc=2`，其余数字永久丢失 —— 于是「回滚需要多久才能恢复」
  这种应急时最想知道的量，下次还得重新演练才能知道。
  §9.4 的口径是「未演练的备份视为不存在」；这次的教训是**未记录的演练只能证明
  它没坏，不能给出任何可用于决策的数字**。T-406 的恢复演练照这个改：
  先把记录表打开，再动手。
