# M1 — 认证与单人钱包（第 2–4 周）

**出口条件（Demo）**：真机上完成 登录 → 设定 username → 扫码加卡 → 从相册图片加卡 → 全屏调出，全部走 staging 后端。

## 后端

| ID | 任务 | 预估 | 依赖 |
|---|---|---|---|
| T-101 | Identity 数据模型与迁移 | 1.5 | T-005 |
| T-102 | 邮件通道与双语模板 | 1.5 | T-003（Q3 ✅ 已决） |
| T-103 | `POST /auth/otp/request`（含 decoy 与限流） | 1.5 | T-101, T-102, T-006 |
| T-104 | `POST /auth/otp/verify` + 注册 + JWT 签发 | 2 | T-103 |
| T-105 | Refresh 轮换与重放检测、logout、设备管理 | 2 | T-104 |
| T-106 | Magic Link（GET 落地页 + POST 消费） | 1 | T-104 |
| T-107 | Username 值对象与一次性设定端点 | 1.5 | T-104, T-006 |
| T-108 | onboarding 拦截器与 `/v1/me` 端点 | 1 | T-107 |
| T-109 | Wallet：cards 模型与 CRUD | 2.5 | T-005, T-104 |
| T-110 | card_members 与 placement 端点 | 1 | T-109 |
| T-111 | 系统限额落地 | 0.5 | T-006, T-109 |
| T-112 | `GET /v1/config`（强制升级 / 维护） | 0.5 | T-004 |
| T-113 | Scheduler 与清理任务 | 1 | T-101, T-107 |

## Android

| ID | 任务 | 预估 | 依赖 |
|---|---|---|---|
| T-150 | 令牌存储与网络认证层 | 1.5 | T-009, T-010 |
| T-151 | Onboarding 流程（邮箱 → OTP → username） | 2.5 | T-150, T-107 |
| T-152 | `core:barcode` 渲染与格式映射 | 1.5 | T-008 |
| T-153 | 钱包列表 | 2 | T-009, T-152 |
| T-154 | 卡详情与全屏条码页 | 2 | T-152, T-153 |
| T-155 | 手动新增/编辑卡 | 1.5 | T-153 |
| T-156 | 相机扫描（CameraX + ML Kit） | 2 | T-152, T-155 |
| T-157 | 从图片录入（Photo Picker） | 2 | T-156 |
| T-158 | 强制升级墙与维护横幅 | 0.5 | T-150, T-112 |

---

## T-101 Identity 数据模型与迁移

**预估** 1.5 人日 · **依赖** T-005

**交付物** `users` / `otp_challenges` / `devices` / `sessions` 四张表的 Doctrine 实体、仓储接口与迁移（DDL 见 §17.1）。

**实现要点**
- `users` **不存明文邮箱列**：`email_hash BYTEA UNIQUE`（HMAC-SHA256 + Vault pepper）用于查找，`email_encrypted TEXT`（Vault Transit）用于发信。
- `users.username TEXT UNIQUE NULL`——**可空是有意的**（首次 OTP 验证即建行，此时尚未设 username），不是遗漏。
- `otp_challenges.is_decoy BOOLEAN`：邮箱不存在时创建的哑挑战。
- `sessions.previous_token_hash` 为轮换重放检测预留。
- 所有表带 `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`；外键显式命名 `fk_<table>_<column>`。

**验收标准** 迁移 `up`/`down` 往返通过；`doctrine:schema:validate` 通过；集成测试断言 `email_hash` 唯一约束与 `username` 唯一（大小写不敏感，存归一化小写值）。

**规格引用** §5.2、§17.1

> ✅ **已交付，三条验收标准全部实测通过**（2026-09-05）。
> `composer migration:check` 三步全绿（up → down 往返 → `schema:validate`
> "The database schema is in sync with the mapping files"）；
> `doctrine:schema:update --dump-sql` 报 "Nothing to update"，即映射与迁移逐字一致。
> `composer qa` 1259 tests / 6588 assertions 全绿，集成用例 **0 skip**；
> 覆盖率整体 89.0%、`Module/Identity/Domain` 95.3%（门槛 85%）。
>
> **落地时冒出来的三条约束，合起来决定了全部实现形态**，详见
> [ADR-0011](../adr/0011-doctrine-orm-xml-mapping-and-module-owned-foreign-keys.md)：
> ① deptrac 的 `Identity.Domain: [Shared.Domain]` 让属性映射结构性不可行 → 映射是 XML；
> ② Doctrine 懒加载要继承实体并回填属性 → 实体不能 `final`、属性不能 `readonly`
> （这破了本仓库 `final readonly` 的惯例，是有据可查的例外）；
> ③ `schema:validate` 会把库里的外键与 ORM 元数据对账 → 外键必须建成 `<many-to-one>`，
> 而且索引、唯一约束与每个 `DEFAULT` 都要在 XML 里与迁移**逐字**重复一遍。
>
> **相对任务卡的三处增补**（都不是范围蔓延，理由随代码注释）：
> - 新增 `Shared\Domain\Crypto\HashDigest` + `HashDigestType` / `CiphertextType`
>   两个 DBAL 类型。六个 `BYTEA` 列若用裸 `string`，「hex 写进摘要列」与
>   「用 `===` 比摘要」都拦不住，而后者是 §7.1 明令要常量时间的地方。
>   T-109 的 `barcode_value_fingerprint` 直接复用。
> - 仓储除接口外**也交付了 Doctrine 实现**。没有实现的话 ORM 的读写路径在本任务里
>   从未真正执行，`schema:validate` 绿了也只证明映射语法对。
> - §17.1 补上了 `otp_challenges` / `devices` / `sessions` 的 DDL 片段
>   （原先只有 §5.2 的列表格）。
>
> ⚠️ **留给 T-109 的雷**：`cards.owner_id → users(id)` 是**跨模块**外键，
> 而按 ③ 它必须建成 ORM 关联才能让 `schema:validate` 通过 ——
> 但 `Wallet.Domain` 的 deptrac 允许列表里没有 `Identity.Domain`。
> 三条出路与各自的代价写在 ADR-0011 的「遗留问题」一节，T-101 刻意没有替它决定。
>
> > **✅ 已拆（2026-09-08，T-109）**：走的是三条之外的第四条 —— 外键照建在库里，
> > 但不进 ORM 元数据，由一个 `postGenerateSchema` 监听器补进生成的 schema。
> > 见 [ADR-0019](../adr/0019-cross-module-foreign-keys-via-post-generate-schema.md)。
> > 也就是说上面的 ③ 只对**模块内部**的外键成立。

---

## T-102 邮件通道与双语模板

**预估** 1.5 人日 · **依赖** T-003（先决 Q3 —— 已于 2026-09-05 定案，[ADR-0013](../adr/0013-mail-via-domain-mailbox.md)）

**交付物**
- `Notification` 模块的 `MailSenderInterface` + Symfony Mailer 实现（DSN 一行可切换通道）
- `templates/email/{de,en}/`：OTP 码、Magic Link、新设备登录提醒、refresh 重放安全提醒
- Messenger `email` transport（异步发送）+ `email_send_total` 指标（标签 `provider`/`template`/`result`）
- 全局日发信量阈值告警 + **自动熔断非关键邮件（保留 OTP）**
- SPF / DKIM / DMARC 配置（`p=quarantine` → `reject`）

**实现要点**
- §3.2 是**有意识的风险接受**：一期单通道、无双活、无送达率监控体系。前提是**把邮箱服务商细节完全收敛在 `MailSenderInterface` 之后**——这是"未来 2 人日能补回来"的唯一保证。业务代码不得出现任何服务商特有类型。
- 服务商必须满足：EU/EEA 处理 + 可签 AVV/DPA + 支持自定义域 SPF/DKIM。**不得**用个人/消费级邮箱或美国 SaaS 免费层（合规硬伤）。
- 外发邮件只有两类：OTP/Magic Link（收件人 = 请求者自己输入的邮箱）与安全提醒（收件人 = 账号自己）。**没有任何路径让用户指定第三方收件人**。

**验收标准** 上线前手工验证：向 Gmail / GMX / Web.de / Outlook 各发一封 OTP，确认不进垃圾箱（4 次手工验证，记录结果）。

**规格引用** §3.1、§3.2、§8.3、§14.4

> ⚠️ **代码与配置已交付；Q3 当日定案，交付物尾巴相应缩短**（2026-09-05）。
> `composer qa` 全绿，容器内 1308 tests / 6737 assertions **0 skip**；
> 覆盖率 `Module/Notification/Application` 98.6%（门槛 85%）、整体 85.1%。
> `composer migration:check` 三步全绿，`schema:update --dump-sql` 报 "Nothing to update"。
>
> **✅ Q3 已决（当日）**：发信走 **`n-cards.de` 的域名邮箱**（托管方 dogado GmbH，
> 德国多特蒙德），标准 SMTP submission，**不采购专业 ESP**。
> `MAIL_PROVIDER` 从 `unset` 改成 `dogado`，`MailSenderInterface` 以上一行代码没动 ——
> 换选型时那道抽象墙的第一次真实检验，它通过了。完整论证与代价见
> [ADR-0013](../adr/0013-mail-via-domain-mailbox.md)。
>
> 这不是对 §3.2 的偏离：§3.2 在 v1.1 里已经把「专业 ESP 强制」撤销成
> 「**普通商业邮箱服务**，仅要求处理地在 EU/EEA」，本次只是把那一档的取值填实。
> §8.3 的三条硬要求（EU/EEA ✅、可签 AVV ✅、自定义域 SPF/DKIM ✅）逐条核对在 ADR 里。
>
> **⛔ 仍然欠着的（2026-09-06 更新：DKIM 与发信地址两条都已解除）**：
> - ~~确认 dogado 是否支持 DKIM 签名并取得 selector~~ → ~~**剩下的是实发一封验证
>   `d=n-cards.de`**~~ → **✅ 整条关闭**（2026-09-06 当日实发）：
>   zone 里 dogado 自动写入的 DKIM 公钥 selector **`cloudpit`**（1024 位 RSA），
>   SPF 也已自动写入（`include:secure-mailgate.com`）；**实发三封 `mailer:test` 到 Gmail，
>   `DKIM-Signature` 全是 `d=n-cards.de`** —— dogado 确实签 SMTP submission 的邮件
>   （不只签 webmail），且用我们的域签，`adkim=s` 严格对齐成立，`p=reject` 的路径实测通了。
>   **DMARC 阶段 ② 的实发前置就此满足，只剩 7 天观察窗口**（起算日仍待确认）。
>   完整信头对照表见 `docs/runbooks/email-dns.md` §2.4。
> - **查实每小时 / 每天发信配额**，回来校准熔断阈值。现在的 **200 / 500** 是
>   保守猜测（原先按专业 ESP 估的 3000 / 10000 已下调），理由见 `ncards_mail.yaml`。
>   **这条现在是本任务最大的未知。**
> - ~~**收紧 SPF 到 `-all`，并发布 DMARC 阶段 ①**~~ → **✅ 两条都已做**（2026-09-06 实测）：
>   SPF 现在是 `v=spf1 a mx include:secure-mailgate.com -all`，
>   `_dmarc.n-cards.de` 是 `v=DMARC1; p=none; rua=mailto:dmarc@n-cards.de; adkim=s; aspf=s`。
>   **三条记录全部在位。** 接下来是看聚合报告 —— ~~⚠️ 阶段 ② 的前置**不只是** 7 天，
>   还有那封实发验证信~~ → 实发验证已通过（见上一条），**阶段 ② 现在只等 7 天窗口**，
>   起算日仍待确认。
> - **§3.2 那 4 次手工送达验证**（Gmail / GMX / Web.de / Outlook）。
>   §15.1 的**上线必需项**，不是可裁剪项。⚠️ 上面那三封 `mailer:test` 发的是 Gmail
>   并且到了，但那是裸信不是 OTP 模板，也没记落收件箱还是垃圾箱 ——
>   **这一条一次都没做，别拿它冲抵**。顺带：那三封没抄 `Authentication-Results:` 那行，
>   做这 4 封时补上。
> - **与 dogado 签署 AVV 并归档**，以及处置 `no-reply@` 那个真实收件箱
>   （域名邮箱是双向的，退信与误回复会积累真实邮箱地址，而 §8.2 的 ROPA 里没有它）。
>   两项归 T-450。⚠️ 套餐确认为 **1 个账号 + 只能收信的别名**，所有别名默认汇进
>   同一个收件箱 —— **法定地址（Impressum / GDPR）的转发目标必须与 `no-reply@` 分开**，
>   否则 30 天 SLA 的请求会被埋在退信里。
> - **`MAIL_FROM_ADDRESS` 的最终取值**（ADR-0013 未解决项 f）。
>   ~~先测「认证账号能否用别名做 `From:`」~~ → **✅ 已测，可以**：同一组实发里
>   `--from` 分别指 `no-reply@` / `dmarc@` / `info@`，三封的 `From:` 都**原样保留**，
>   没被 `550` 拒收、也没被静默改写。「别名不能发信」只指「别名没有独立 SMTP 凭据」。
>   **约束消失，只剩拍板** —— 保持现值 `no-reply@`（理由见 runbook §1），
>   人工回信首选 `Reply-To:`。⚠️ `Return-Path` 不跟 `--from` 走，三次都是
>   `<no-reply@n-cards.de>`（来自 `envelope.sender`）：SPF 域级对齐不受影响，
>   但 `From: info@` 的信被退回时，退信落进**没人看的 `no-reply@` 池**——
>   对有 30 天 SLA 的 GDPR 回信是个盲区，并进 ADR-0013 未解决项 d 一起处置。
>
> **2026-09-06 第一次真发，撞上两个纯基础设施的阻塞，都已修**（代码一行没改）：
> 1. **worker 没有出站网络。** 它只连 `backing`，而那张网 `internal: true` ——
>    连 DNS 都出不去，症状是 `getaddrinfo ... failed: Try again`。
>    修法：新增一张只给 worker 的 `egress` 网络（`docker-compose.base.yml`），
>    `backing` 的 internal 保持不变。本地不会暴露这个 bug，因为本地
>    `MAILER_DSN` 指向同在 `backing` 上的 mailpit，压根不需要出站。
> 2. **Hetzner 封了 465 出站**（连 25 一起，587 不封），症状是连接超时。
>    ADR-0013 定的 `smtps://…:465` 因此**改成 `smtp://…:587?require_tls=true`**。
>    `require_tls` 不能省 —— 它替代了 465 原本提供的「没有明文降级路径」这个保证。
>    465 的解封工单**决定暂不开**。判据、实测数字与两种超时的区分见
>    `docs/runbooks/email-dns.md` §2.4，选型影响见 ADR-0013 负面影响 8。
>
> ⚠️ **顺带记一条 §9.3 层面的天花板**：按 5 万注册推算的日发信量约 1100 封，
> 已经越过典型域名邮箱的配额量级。**这个通道能撑住上线，撑不住 12 个月目标。**
> 触发判据（日累计逼近实测配额 50%）与处置（补做专业 ESP，2 人日）写在
> `docs/runbooks/email-dns.md` §4。
>
> **四条落地决定，合起来决定了全部实现形态**，详见
> [ADR-0012](../adr/0012-mail-channel-topology.md)：
> ① 代码通道无关，`MailSenderInterface` 之后只有 Symfony Mailer、无任何服务商 SDK
>    （原文是「Q3 悬置」，当日被 [ADR-0013](../adr/0013-mail-via-domain-mailbox.md) 取代，
>    但**通道无关这一点本身没变**，也正因为它没变，换选型只改了两个环境变量）；
> ② async transport 选 **Doctrine**（T-004 把这个决定留给了「第一个真实异步消费者」）——
>    丢一封 OTP 信 = 该用户登不进去，而 §14.2 的 Redis 是单容器、无 HA；
> ③ 队列消息体**整体经 Vault Transit 加密**，否则 `messenger_messages.body` 就是一列
>    明文邮箱 + 明文 OTP 码，且会被 T-406 的备份带出主机；
> ④ 新开 `Framework.Mail` / `Framework.Templating` 两个 deptrac 图层，**只给
>    `Notification.Infrastructure`** —— 这是 §3.2「把服务商细节收敛在
>    `MailSenderInterface` 之后」的唯一强制点，由 `deptrac:selftest` 场景 ④ 守着。
>
> **相对任务卡的三处增补**（都不是范围蔓延，理由随代码注释）：
> - `Shared\Application\Metrics\MetricsInterface` + Redis 实现。任务卡把
>   `email_send_total{provider,template,result}` 列为交付物，而一个**带标签的计数器**
>   没法用日志代替 —— §14.4 的「失败率 > 5% 持续 10 min」P1 告警要靠它。
>   **只交付接缝**：`/metrics` 抓取端点与 Alertmanager 规则仍归 T-405。
> - `MailTemplate` 放在 `Application\Dto\` 而不是 `Domain\`。deptrac 的 `_Ports` 策略
>   只给调用方开放 Port + Dto，放 Domain 的话 Port 入参只能退化成 string，
>   把「外发邮件只有这四封」这条约束从类型系统里删掉。
> - compose 的 `worker` 服务（base/prod/staging 三处）与 dev profile 的 Mailpit。
>   前者是 async transport 的必然配套（T-004 的注释里就写着「接上之后再启用」）。
>
> **落地时踩到、值得记住的三个坑**（都写进了对应文件的注释）：
> - `doctrine.dbal.schema_filter` 结尾**不能带 `$`**：`BIGSERIAL` 连带的
>   `messenger_messages_id_seq` 会漏网，`schema:update --dump-sql` 给出一句
>   `DROP SEQUENCE … CASCADE`，真跑下去会连主键默认值一起 CASCADE 掉。
> - `use_notify: false` 是 **transport 选项、不是 DSN 参数**，且必须与 `auto_setup=0`
>   成对出现。漏了不报错，而是**静默的 60 秒投递延迟**。
> - 序列化器必须**装饰** `PhpSerializer` 而不是自己写 JSON 编解码：Messenger 的
>   stamp（含承载重投计数的 `RedeliveryStamp`）全都序列化在 body 里，
>   自己写会静默丢掉它们，症状是 `max_retries: 3` 永远达不到 3。
>
> ⚠️ **留给 T-103/T-104/T-105/T-106 的两条**：
> - 调用方要自己把 `Identity\Domain\ValueObject\Locale` 映射成
>   `Notification\Application\Dto\MailLocale`（一行 `match`）。两个 enum 取值域相同
>   但不能复用，理由见 ADR-0012 的「Consequences」。
> - ~~`config/services.yaml` 的 `when@test` 里那条 `MailSenderInterface` public 别名，
>   在 T-103 有了真实调用方之后可以删。~~ → **✅ T-103 已删**（2026-09-06）。

---

## T-103 `POST /auth/otp/request`

**预估** 1.5 人日 · **依赖** T-101, T-102, T-006

**交付物** 端点实现 → **恒定** `202 {challenge_id, expires_at, resend_after_seconds: 60}`。

**实现要点（防枚举，逐条都是 MUST）**
- 邮箱**不存在**时同样创建 challenge（`is_decoy = true`）并返回一个哑 `challenge_id`，**不发信**；响应体与**耗时**与真实路径不可区分。
- 限流三维：email_hash 1/min、5/h、10/day；IP 20/h；全局熔断。
- 6 位码用 `random_int`（CSPRNG），存 `HMAC-SHA256(code, pepper)`，**不存明文**。
- 新建 challenge 时作废该 email 的旧 challenge（单次登录只允许一个活跃 challenge）。
- 有效期 10 分钟；`attempts` 上限 5。

**验收标准** 集成测试断言：已注册 vs 未注册邮箱的响应体结构、状态码、耗时分布不可区分；限流触发时返回 429 + `Retry-After`。

**规格引用** §3.8、§6.2、§6.3.1、§7.1、§7.5

> ✅ **已交付，两条验收标准都实测通过**（2026-09-06）。
> 容器内 `1394 tests / 7100 assertions` 全绿、**0 skip**；裸机 `composer test`
> 同样全绿（164 skip，都是依赖 compose 栈的既有用例）。
> `deptrac` 0 violation / 0 uncovered，`deptrac:selftest` 四个场景全过，
> PHPStan level 8 无错，`schema:validate` 与迁移 up/down 往返全绿。
> 覆盖率 `Module/Identity/Application` **98.9%**、`Module/Identity/Domain` 95.3%、
> 整体 90.5%（门槛 85 / 85 / 70）。
>
> **契约一个字没改** —— `requestOtp` 早在 T-007 就写好了。真正的变化是
> `OpenApiDocumentTest::testEveryProductApiRouteIsDeclaredInTheContract()`
> **从今天起开始咬**：把路由临时改成 `/v1/auth/otp/requestX` 实测，它当场报
> 「以下 /v1 路由没有出现在 openapi.yaml 里」。§13.1 在 CI 里的强制点第一次生效。
>
> ### ⚠️ 更正（2026-09-06，随 T-104 / [ADR-0014](../adr/0014-otp-always-sends-a-code.md)）
>
> **本任务交付的「未注册邮箱 → decoy challenge + 不发信」是错的，已在 T-104 撤销。**
>
> 它让**注册变得不可能**：新用户永远收不到码 → 永远走不到 `otp/verify`，
> 而 §5.2 / §6.3.1 / 契约三处都规定「首次验证成功即注册」，且全仓库没有第二条注册路径
> （T-151 的 onboarding 也走这条流）。也就是说，本任务交付之后系统里**没有任何用户能注册**，
> 而 M1 的出口条件「真机上完成登录 → 设定 username → 加卡」在第一步就走不通。
>
> 它没有在任何用例里显形，因为既有用例要么只测 request（那里没有注册的概念），
> 要么自己往库里塞一个 user 再断言。**这是本任务真正的教训**：
> 「两条路径不可区分」被测得很细，而「这两条路径合起来能不能完成产品流程」没人测。
>
> T-104 的替代方案更简单也更强：**恒发码**，`RequestOtpService` 不再注入
> `UserRepositoryInterface` —— 防枚举从「配平两条路径的做功」变成「服务端根本没查」。
> 下面那段「三道防线」因此只剩第 ① 与第 ③ 条成立，第 ② 条（做功配平）连同
> 那行「返回值被丢弃的 `encrypt()`」一起删掉了。代价记在 ADR-0014 的 Consequences 里。
>
> **防枚举是怎么落地的**（三道防线，缺一不可，理由写在 `RequestOtpService` 类注释里）：
> - **形状**：两条路径都返回同一个 DTO，控制器里**一个 `if` 都没有**。
> - **做功**：Vault 往返次数配平成 4 : 4。真实路径是
>   `hmac(email/code/ip)` + `EncryptedMailSerializer` 入队时的那次加密；
>   decoy 路径是同样三次 hmac + **一次返回值被丢弃的 `encrypt()`**。
>   ⚠️ 那一行看起来像死代码，删掉就打开时间侧信道，而所有功能测试仍然全绿 ——
>   `RequestOtpServiceTest::testBothPathsDoTheSameAmountOfWork()` 就是钉它的。
>   （↑ **已作废**，见上方更正：ADR-0014 之后没有第二条路径可配平。）
> - **耗时**：新增 `Shared\Application\Timing\TimeEqualizerInterface`，
>   把总耗时填充到固定预算。它兜的是「做功配平」将来配不上的余数
>   （今天是真实路径那条 `messenger_messages` 的 INSERT）。**T-104/T-106 直接复用**：
>   verify 的 decoy 恒 401 也要求耗时相同。
>
> **相对任务卡的三处偏差**（都不是范围蔓延）：
> - **「限流三维」实际只接了两维。** 第三维「全局熔断」T-102 已经交付了，
>   而且它**不在请求路径上** —— 是 `MailCircuitBreaker`，跑在 worker 里、按信件分级丢弃
>   （`RateLimitPolicyCoverageTest` 的 `NOT_RATE_LIMITED` 早就登记了这条豁免：
>   「不是 per-subject 限流」）。本任务只需要传主体，正如 ADR-0005 所写。
> - **`request_ip_hash` 走 Vault HMAC 而不是本地 SHA-256。** IPv4 空间只有 2^32，
>   不带 pepper 的摘要在拿到备份后几秒钟就能全表反查，§8.2 把它当「哈希后的个人数据」
>   就不成立了。代价是每请求多一次 Vault 往返（两条路径都付，所以不破坏配平）。
> - **多了一个 `otp_request_total{result}` 计数器。** §14.4 的「OTP 转化率骤降」P1 告警
>   需要请求侧计数，而 T-102 只给了 worker 侧的 `email_send_total` —— 它看不到 decoy
>   （decoy 压根不入队）。只加计数，`/metrics` 端点仍归 T-405。
>
> **落地时踩到、值得记住的四个坑**：
> - **`Identity.Dto` 碰不到 `Identity.Domain`**（deptrac 里 Dto 的允许列表只有
>   `Shared.Domain`，因为它是给别的模块看的跨模块契约）。于是持有 `Locale` 的
>   `OtpRequestPayload` 只能放 `Application/Otp/` 而不是 `Application/Dto/`。
>   下一个写端点入参的人会撞同一堵墙。
> - **`WebTestCase::createClient()` 要求内核还没启动**，所以
>   `RequiresIdentitySchema`（第一行就是 `bootKernel()`）在 `tests/Api` 里用不了。
>   新开了 `tests/Api/Support/RequiresOtpStack`，顺序反过来：先建 client 再取连接。
> - **端到端用例必须每条请求换一个源 IP**，否则整组会撞上 `otp_request_ip` 的 20/h ——
>   Redis 里的计数**不在**测试事务里、回滚不掉，症状是「单跑绿、全跑红」，
>   而且一小时内重跑还是红。同理，`Idempotency-Key` 也必须每次跑都是新的
>   （幂等记录存 24 h，写死一个 key 的话第二天会拿它配上一个不同的请求体 → 422）。
> - **幂等作用域目前是 IP**（`AnonymousIdempotencyScopeResolver` 返回 null 时回落）。
>   验重放的用例因此必须把两次请求固定在同一个 IP 上 —— 与上一条正好相反。
>
> **手工验证**（本地 compose + Mailpit，2026-09-06）：
> 未注册邮箱 → 202、`is_decoy=t`、`messenger_messages` 0 行（**不发信**）；
> 已注册邮箱 → 202、`is_decoy=f`、队列 body 以 `vault:v1:` 开头且**不含明文邮箱**，
> worker 消费后 Mailpit 收到德语信「150738 ist dein N-Cards-Anmeldecode」；
> 60 秒内重发 → 429 + `Retry-After: 42` + `X-RateLimit-Remaining: 0`，
> `application/problem+json`，且 `detail` 里没有主体。
>
> ⚠️ **留给后续任务的三条**：
> - **`ncards.otp.request_budget_ms` 的 150 是保守猜测，不是实测值。** 它必须大于
>   慢路径的 p99，小于就等于填充**静默失效**（功能测试全绿，泄露悄悄打开）。
>   唯一的信号是 `MonotonicTimeBudget` 越界时那行 `Constant-time budget overrun`
>   warning，T-405 接告警时应该盯它。校准方法写在 `config/packages/ncards_otp.yaml` 里。
> - **`ncards.otp.max_attempts: 5` 已经配好但没有消费者** —— 归 T-104，
>   `OtpChallenge::hasAttemptsLeft()` 已经收一个 `$max` 入参在等它。
> - `config/services.yaml` 的 `when@test` 里那条 `MailSenderInterface` public 别名
>   **已按 T-102 的注释删掉**：本任务提供了生产调用方，服务不再是孤儿，
>   `QueuedMailPipelineTest` 从测试容器照样 get 得到。
>
> ℹ️ 顺带记一条**与本任务无关**的既有问题：dev 容器没有加载任何 php.ini，
> `memory_limit` 是 PHP 默认的 128 M，于是 `docker compose exec app composer stan`
> 会以「PHPStan process crashed」失败（`git stash` 到改动之前同样复现）。
> 宿主机与 CI（setup-php 默认 `memory_limit=-1`）都不受影响，所以没在本 PR 里动它。
> 要在容器里跑就加 `php -d memory_limit=1G vendor/bin/phpstan analyse`。

---

## T-104 `POST /auth/otp/verify` + 注册 + JWT 签发

**预估** 2 人日 · **依赖** T-103

**交付物** 验证端点 → `200 {access_token, expires_in, refresh_token, user}`；首次验证成功即创建 `users` 行（注册）。

**实现要点**
- `hash_equals()` **常量时间**比较；`attempts++`，> 5 或过期 → 401；decoy → **恒 401 且耗时与真实路径相同**。
- 成功后：upsert user → 创建 `device`（客户端生成 id，安装级唯一）+ `session` → 签发 JWT（EdDSA Ed25519，claims `sub, sid, did, jti, iat, exp`，**15 分钟**）+ refresh（32 字节随机，Base64url，不透明，**90 天滑动**）→ 写 `audit_log(login_success)`。
- 新设备登录 → 向该用户所有既有设备发推送 + 向邮箱发提醒信（设备型号、大致时间、粗粒度地区）+ 信中"这不是我"的一键撤销链接（**同样走 POST 确认**）。
- `POST /auth/otp/verify` 限流：challenge_id 5 次总计；IP 60/h。

**验收标准** 单测覆盖常量时间比较与 decoy 路径；集成测试断言首次验证创建的 user 行 `username IS NULL`。

**规格引用** §6.3.1、§7.1、§7.5

> ✅ **已交付，两条验收标准都实测通过**（2026-09-06）。
> 容器内 `1543 tests / 7638 assertions` 全绿、**0 skip**；PHPStan level 8 无错，
> `deptrac` 0 violation / 0 uncovered、selftest 四个场景全过，
> `schema:validate` 与迁移 up/down 往返全绿，Spectral 0 warning。
> 覆盖率 `Module/Identity/Application` **97.6%**、`Module/Identity/Domain` 92.8%、
> `Shared/Domain` 98.6%、整体 91.3%（门槛 85 / 85 / 70）。
>
> `verifyOtp` 的**契约一个字没改** —— 它早在 T-007 就写好了。改的是 `requestOtp`
> 的 description（见下）。
>
> ### ⚠️ 开工第一天发现的事：**这个系统当时没有任何注册路径**
>
> T-103 交付的行为是「未注册邮箱 → decoy challenge + **不发信**」，
> 而 §5.2 / §6.3.1 / 契约三处都写着「首次验证成功即注册」，且没有 signup 端点。
> 两者放在一起就是：新用户永远收不到码 → 永远走不到 verify → **永远无法注册**。
> M1 的出口条件在第一步就走不通。
>
> 它没有在任何用例里显形：既有用例要么只测 request（那里没有注册的概念），
> 要么自己往库里塞一个 user 再断言。教训写在 T-103 的更正块里 ——
> 「两条路径不可区分」被测得很细，而「这两条路径合起来能不能完成产品流程」没人测。
>
> 修复是 [ADR-0014](../adr/0014-otp-always-sends-a-code.md)：**对任意邮箱都真发码**。
> `RequestOtpService` 因此**不再注入 `UserRepositoryInterface`** ——
> 防枚举从「配平两条路径的做功」升级成「服务端在那条路径上根本没查」，
> 更强，也不再需要人肉维护。连带删掉了 T-103 那行「返回值被丢弃的 `encrypt()`」。
>
> 代价是明知并接受的，写在 ADR 的 Consequences 里：§3.1 那句「用户无法让系统向
> 任意第三方邮箱发信」不再成立，准确表述变成「只能向任意邮箱发一封 OTP，
> 每邮箱每天 10 封封顶」。§7.5 的三个窗口一个都没放宽。
>
> ### 攻击面搬到了 verify，那才是本任务真正的难点
>
> ADR-0014 之后，攻击者能对任意邮箱拿到一个**真实的** `challenge_id`
> （信进了受害者的收件箱，他看不到），再用错码来问「这个邮箱注册过吗」。
> 要挡的是这一对：
>
> - 「**未注册**邮箱的 challenge + 错码 → 401」
> - 「**已注册**邮箱的 challenge + 错码 → 401」
>
> 好消息是这两条在 `VerifyOtpService` 里**根本不是两条**：拒绝路径不查 `users`、
> 不加密、不发信，做的事只有 `hmac(code)` ×1 + `findById` + `attempts` 的 UPDATE。
> 三处强制点：
> - `VerifyOtpServiceTest::testEveryRejectionShapeDoesTheSameAmountOfWork()` ——
>   六种拒绝形状（码错 / 过期 / 已消费 / 次数耗尽 / 不存在的 id / 遗留哑挑战）
>   的往返账单逐项相等。
> - **那条布尔链的次序是安全约束**：`hash_equals` 永远执行，`isDecoy()` 永远排在它后面。
>   反过来写会让哑挑战少一次 Vault HMAC，而「是哑挑战」恰好等价于
>   「这个邮箱在上个版本里没注册过」。`testADecoyIsRejectedEvenWhenTheCodeHashMatches()` 钉它。
> - `OtpEnumerationResistanceTest` 新增的 verify 三层断言（形状 / 库层足迹 / 墙钟中位数）。
>
> ### 相对任务卡的三处偏差（都不是范围蔓延）
>
> - **`audit_log(login_success)` 没写。** 那张表归 T-401（M4），本任务不提前建。
>   替代是 `login_total{result=registered|success|rejected}` 计数器
>   （与 T-103 的 `otp_request_total` 同形）。交接：审计落点在 `VerifyOtpService`
>   事务提交之后那一行。
> - **新设备登录只发信，不发推送。** FCM 整条链路归 T-306（M3），
>   它还需要 `push_token` 的下发（T-105）与一个尚不存在的推送 Port。
>   邮件是两条渠道里更重要的那条 —— §7.2 T02 的缓解就落在这封信上，
>   而推送发到的正是可能已被控制的那些设备。
> - **「这不是我」链接指向设备管理落地页**（`{APP_PUBLIC_BASE_URL}/l/devices`），
>   不是带令牌的一次性撤销 URL。后者需要 T-105 的撤销端点与令牌存储。
>   同理 `approximate_region` 没有 GeoIP 可用，填的是本地化的「Unbekannt / Unknown」——
>   **不编一个假地区**：模板正文写着「Region 是粗略估计」，填假值会把那句话变成谎话，
>   而这封信的全部价值在于可信。
>
> ### 落地时踩到、值得记住的五个坑
>
> - **PHP 的 OpenSSL 扩展签不了 EdDSA。** `openssl_sign()` 没有对应的 `OPENSSL_ALGO_*`，
>   `openssl_pkey_get_details()` 对 Ed25519 也不给裸密钥字节。能签的是 `ext-sodium`，
>   而它要 32 字节**裸 seed**。好在 Ed25519 的 PKCS#8 编码是**唯一且定长**的
>   （48 字节 = 16 字节固定头 + seed），所以那段转换是定长切片，不需要 ASN.1 解析器。
>   ⚠️ 别把它「泛化」成一个 DER 解析器 —— 那正是 CVE 的产地。
> - **切错位置不会有任何症状。** 把内层 OCTET STRING 的两字节头也算进去，仍然得到
>   32 字节，`sodium` 也照样接受，只是推出来的公钥不同 —— 于是线上签出的每一枚
>   token 谁也验不过，而所有「自己签自己验」的单测全绿。
>   钉它的是两条独立验签：单测用 `sodium_crypto_sign_verify_detached()` 重算一遍，
>   集成测试拿 **Vault 里那份 `public_key`** 验一遍。
> - **`attempts` 必须饱和，不能一直加。** 上限用完之后调用方**仍然要**写库
>   （「次数耗尽」这种拒绝必须与「码错了」做同样多的功），于是这一列会被一条已经
>   死掉的挑战反复写。不封顶的话它是无界的，而它是 SMALLINT ——
>   足够多的源 IP 打同一个 `challenge_id` 能把它顶过 32767，那时 PG 拒绝 UPDATE，
>   于是那条挑战上的每一次请求都变成 **500**。现在 `recordAttempt($max)` 到上限即饱和。
> - **成功路径不能调 `settle()`。** 它必然越过 120 ms 预算（建三张表 + 签 JWT +
>   入队一封信），硬填会让 `MonotonicTimeBudget` 在**每一次正常登录**上打一行
>   "Constant-time budget overrun" —— 而那行是 §3.8 的安全告警，被正常流量淹掉
>   之后就再也没有信号价值了。「200 与 401 耗时不同」本身不是泄漏：走到 200
>   需要先拿到正确的 6 位码。
> - **JWT 密钥读不到要收敛成 503，不能用 `VaultClient` 的默认映射。** 那个映射把
>   4xx（含 404）归 500，对 transit 是对的；但签名密钥读不出来的后果是
>   「所有人都登不进去」，处置永远是一个运维动作，与 ADR-0004 的手工 unseal 同构。
>   500 会让客户端不重试，且把一次可预期的运维窗口报成 §14.4 的「5xx 率高」告警。
>
> ### 手工验证（本地 compose + Mailpit，2026-09-06）
>
> **未注册**邮箱 → `otp/request` 202 → Mailpit **收到**德语码信
> 「153430 ist dein N-Cards-Anmeldecode」（这正是本任务修正的核心行为，
> 上个版本这里是 0 封）→ `otp/verify` 200 →
> `users` 行 +1 且 **`username IS NULL`**、`locale=de`、`status=active`；
> `devices` 1 行（android / Pixel 8 / 15，未撤销）；
> `sessions` 1 行，`refresh_token_hash` **32 字节**、`previous_token_hash` NULL、
> `expires_at = 2026-12-05`（+90 天）；challenge `attempts=1`、`consumed_at` 非空。
>
> access token 的 header 是 `{"alg":"EdDSA","typ":"JWT","kid":"ef107cb92107c2ed"}`，
> kid 与 Vault KV 里的逐字相同；claims 恰好 `sub/sid/did/jti/iat/exp`，`exp-iat=900`；
> 用 KV 里那份 `public_key` 走 `sodium_crypto_sign_verify_detached()` → **true**。
>
> 同一条 challenge 再 verify → 401 且 `detail` 是那句唯一的
> "The verification code is not valid."；同一邮箱 60 秒内重发 → 429 +
> `Retry-After: 60` + `X-RateLimit-Remaining: 0`。
>
> **第二台设备**登录 → 200，Mailpit 收到德语提醒信
> 「Neue Anmeldung bei N-Cards: Samsung Galaxy S24」，正文里
> `Zeitpunkt: 2026-09-06T21:41:29Z`、`Region: Unbekannt`、
> 链接 `https://app.n-cards.de/l/devices`。
> **第一台设备（注册那次）没有发信** —— 刚注册的账号第一次登录必然是「新设备」，
> 给它发一封「检测到新设备登录」是纯噪声，而噪声会训练用户忽略这封信。
>
> ### ⚠️ 留给后续任务的六条
>
> - **`ncards.otp.verify_budget_ms` 的 120 与 `request_budget_ms` 的 150 都是保守猜测。**
>   校准方法写在 `config/packages/ncards_otp.yaml` 里，判据是 staging 的 p99。
>   T-405 接告警时应该盯 "Constant-time budget overrun" 那行 warning。
> - **`infra/vault/policies/ncards-app.hcl` 新开了一条可读路径**
>   （`secret/data/ncards/jwt/current`），也是这份清单里唯一一条让密钥材料离开 Vault 的。
>   RCE 之后的增量风险是「**驱离攻击者之后仍能伪造任意用户的 token**」——
>   **T-404 的 key-rotation runbook 必须写明：任何一次应用主机失陷的响应都要包含
>   一次 JWT 密钥轮换**（与 §5.3 的 6 个月常规轮换无关）。
> - **`is_decoy` 列与 `OtpChallenge::decoy()` 已无生产写入方**，但没删：
>   部署窗口内库里还有上个版本建的哑挑战，verify 必须继续对它们返回 401
>   （寿命 10 分钟）。删列走 §13.5 的 expand–contract，**归 T-113**。
> - **`otp_challenges` 的数据分级变了**：从「只有哈希」变成「含加密的个人数据」。
>   §8.2 的 ROPA 已补一行；**§8.4 的数据导出（T-402）要把它算进去**。
> - **`SigningKeyProviderInterface` 只有 `currentKey()`，没有 JWK Set。**
>   T-108 接鉴权器时要记住 §5.3 重叠期的语义是「**验两把、签一把**」——
>   把它改成返回一组密钥再让签名方随手取第一个，会让轮换当天签出一半旧一半新的
>   token，且没有任何症状。`VaultKvSigningKeyProvider::publicKeyFromSpkiPem()`
>   就是取验签材料的入口。
> - **`APP_PUBLIC_BASE_URL` 是新环境变量**（`.env` / `.env.test` / `env.j2` /
>   两个 group_vars 都已加）。T-106 的 Magic Link 复用它。
>   ⚠️ 它指向**客户端**域（App Links `https://app.n-cards.de/l/*`），
>   不是 API 域；staging 指向 `app.staging.n-cards.de`，理由见 group_vars 的注释。

---

## T-105 Refresh 轮换与重放检测、logout、设备管理

**预估** 2 人日 · **依赖** T-104

**交付物**
- `POST /auth/token/refresh`（轮换后的新令牌对）、`POST /auth/logout`
- `GET /v1/me/devices`（含当前设备标记）、`DELETE /v1/me/devices/{id}`（远程登出）、`PUT /v1/me/devices/{id}/push-token`

**实现要点**
- 每次刷新签发新 refresh token，旧 token **立即失效**，`previous_token_hash` 记录。
- 收到**已使用过的** refresh token → 判定令牌被窃：撤销该会话家族全部令牌 + `audit_log(reuse_detected)` + 告警 + 给用户发安全提醒邮件。
- 服务端**不做** access token 黑名单（15 分钟窗口可接受），但会话撤销后 refresh 立即失效。
- `revoked_reason` 取值：`logout` / `reuse_detected` / `user_revoked` / `account_deleted`。
- 限流：refresh 按 session 60/h。

**验收标准** 集成测试：重放旧 refresh token → 整个会话家族被撤销且发出提醒邮件；远程登出某设备后该设备的 refresh 立即失效。

**规格引用** §5.2、§7.1、§7.5

> ✅ **已交付，两条验收标准都在真栈上实测通过**（2026-09-07）。
> 容器内 `1687 tests / 8327 assertions` 全绿、**0 skip**；覆盖率
> `Module/Identity/Application` 98.3% / `Identity/Domain` 92.8% / 整体 90.8%
> （门禁 85/85/70）。`composer migration:check` 三步全绿 ——
> **本任务不产生迁移**，T-101 的 DDL 已经覆盖全部需求。
>
> ### 两条验收标准的实测形状
>
> - **重放** —— `SessionLifecycleTest::testReplayingAnOldRefreshTokenRevokesTheWholeFamilyAndQueuesTheAlert()`：
>   登录 → 轮换一次 → 拿旧令牌再刷 → 401 + `sessions.revoked_reason = 'reuse_detected'`
>   + 提醒信入队 + **刚签发的那枚新令牌也一起失效**（后者才是「家族」二字的含义）。
>   与它成对的是 `testAReplayedTokenLooksExactlyLikeAnUnknownOne()`：
>   被窃检测**不能**从响应里看出来，否则攻击者就有了一个「我这枚令牌是不是刚被换掉的」
>   的免费预言机。
> - **远程登出** —— `testRemotelyRevokingADeviceInvalidatesItsRefreshTokenImmediately()`：
>   同一账号两台设备，踢掉 B → B 的 refresh 当场 401、A 不受影响、
>   B 的 `push_token` 已清空、B 不再出现在设备列表里。
>
> 手工验证走了一遍真链路（compose 栈 + Mailpit，curl）：
> `otp/request` → 收到真码 → `verify` → `refresh` ×1 → 重放 → 401 +
> 库里 `reuse_detected` + 德语安全提醒信实发；两台设备 → `GET /v1/me/devices`
> （`is_current` 正确、响应体无 `push_token`）→ `PUT .../push-token` 204 →
> `DELETE` 204 → B 的 refresh 401 → `logout` 204 → 无 Bearer 的 logout 401。
>
> ### ⚠️ 相对任务卡的三处偏差
>
> - **鉴权器从 T-108 提前到本卡，这不是范围蔓延，是依赖顺序错了。**
>   `logout` 与三个 `/me/devices` 都要 Bearer，而仓库里没有任何 access token
>   验签器（没装 security-bundle，`SigningKeyProviderInterface` 只有 `currentKey()`）。
>   文档三处（该接口的类注释、两个 Anonymous 解析器、T-104 的交接清单）都把它写给了
>   T-108 —— 而 T-108 依赖 T-107、T-107 依赖 T-104，排在本卡**之后**。
>   不建它，本卡一个端点都交付不了。落地为 **ADR-0015**，连带兑现了 T-004 留下的
>   两条「待接入」：§7.5 的「全部写接口 user 300/min」从此真的按 user 生效，
>   幂等键也按 user 隔离（此前全局按 IP —— 两个用户撞同一个键时后者会拿到前者的响应）。
> - **`audit_log(reuse_detected)` 没写。** 那张表归 T-401（M4），与 T-104 对
>   `audit_log(login_success)` 的处理是同一个决定。替代是
>   `token_refresh_total{result=rotated|rejected|reuse_detected}` 与
>   `session_revoked_total{reason}` 两个计数器，外加一行 `logger->critical()`
>   （§14.4 的告警按级别筛）。交接：审计落点在 `RefreshTokenService::handleReuse()`
>   撤销事务提交之后那一段。
> - **`push-token` 只落库，不接 FCM。** 推送链路归 T-306（M3）。端点先存在，
>   是为了 T-306 上线当天不至于一个令牌都没有 —— 与 T-104「只发信、不发推送」同一个做法。
>
> ### 落地时踩到、值得记住的六个坑
>
> - **⚠️ 免鉴权白名单必须按路由名，不能按路径前缀。** 免鉴权的端点里有三个在
>   `/v1/auth/` 下，于是「前缀一律放行」看起来完全等价 —— 但
>   `POST /v1/auth/logout` **也**在那个前缀下，而它是 auth 组里唯一需要 Bearer 的。
>   写成前缀的后果是任何人都能撤销任何会话，**而所有测试照常绿**（logout 本来就 204）。
>   `AuthenticationCoverageTest` 拿白名单与契约里的 `security: []` 逐条对账。
> - **`revokeAllForDevice()` 不能写成批量 DQL `UPDATE`。** 那会绕过
>   `Session::revoke()`，于是「首个 reason 胜出」失效 —— 一条已因 `reuse_detected`
>   撤销的会话会被远程登出改写成 `user_revoked`，这个账号曾经发生过令牌被窃
>   就再也查不出来。ORM 3 又拿掉了 `clear($entityName)`，身份映射也没有便宜的补救。
>   改成逐行水合 + `revoke()`：一台设备上的会话是个位数量级，付得起。
> - **`findByRefreshTokenHash()` 要加 `PESSIMISTIC_WRITE`。** 轮换是读-改-写，
>   两个并发刷新不加锁会丢失更新 —— 而先提交的那枚新令牌已经发回客户端了，
>   它从此在库里不存在，客户端下次刷新 401 且没有任何日志能解释。
> - **合法客户端的重试会被误判成被窃。** 服务端轮换完、响应在路上丢了，
>   客户端拿旧令牌重试 → 命中 previous → 会话被撤销 + 一封安全警报，
>   而实际上什么都没发生。唯一的缓解是契约给这个端点挂的 `Idempotency-Key`
>   （中间件回放同一对令牌）。**不要**在服务端「宽容一点」——
>   那等于给被窃令牌开一个可用窗口。客户端侧的对应约束是 T-150 的 Mutex 串行化。
> - **`validation_failed` 在本仓库映射到 400，不是 422**（`ErrorCode::httpStatus()`）。
>   422 留给 `username_invalid` / `limit_exceeded` / `idempotency_key_reused`。
>   另外 `{}` 走的是 `malformed_request` 而不是字段校验：`json_decode('{}', true)`
>   给出空 PHP 数组，而 `array_is_list([])` 为 true，于是
>   `AbstractApiController::decodeBody()` 先一步判它「不是 JSON 对象」。
> - **`refresh_replay` 邮件的文案与实现对不上。** T-102 起草它时撤销范围还没实现，
>   草稿写的是「**所有**设备已登出」，而实际范围是一条会话家族（别的设备不受影响）。
>   已改成「一台设备」。这不是措辞偏好：一个两台设备的用户读到「所有设备已登出」、
>   转头发现另一台还登着，从此就不会再认真读这封信 —— 而 §7.2 T02 把它列为
>   「邮箱被接管」唯一能被用户察觉的信号。
>
> ### 留给后续任务的四条
>
> - **T-106（Magic Link）**：`POST /v1/auth/magic/consume` 落地时要往
>   `AuthenticationListener::PUBLIC_ROUTES` 加一行（契约里它已经是 `security: []`）。
>   `AuthenticationCoverageTest` 只对**已注册**的路由对账，所以在那之前不会红。
>   响应体直接复用 `Application/Session/SessionIssued`（本卡把它从 `Application/Otp/`
>   挪过来了，因为它已经有两个生产者）。
> - **T-108（onboarding 拦截器）**：`auth_logout` 必须进那三个白名单之一 ——
>   契约明写「`onboarding_incomplete` 的用户也可以登出」，否则中途放弃注册的人
>   无法登出。鉴权器本身已经在跑，T-108 只需要在它之后加 username 状态机。
> - **T-404（key-rotation runbook）**：轮换要按「验两把、签一把」写 ——
>   换 `ncards/jwt/current` **之前**先把旧的那份写到 `ncards/jwt/previous`，
>   24 小时后删掉。policy 里那条 read 已经加好了（ADR-0015 记了它的代价）。
>   `AppRolePolicyTest::testAppRoleCanReadThePreviousJwtKeyPathEvenThoughItIsAbsent()`
>   靠 **404 与 403 的区别**证明 policy 配对了。
> - **T-405（指标导出）**：本卡新增 `token_refresh_total{result}` 与
>   `session_revoked_total{reason}`，与既有的 `login_total` / `otp_request_total` 同形。
>   `result=reuse_detected` 那条值得一条告警规则 —— 它是 §7.1 里唯一被称为
>   「令牌被窃」的事件。

---

## T-106 Magic Link

**预估** 1 人日 · **依赖** T-104

**交付物** 邮件内 Magic Link → `https://app.n-cards.de/l/magic/*` 落地页 + `POST /v1/auth/magic/consume`；`assetlinks.json` 托管。

**实现要点（这是最容易实现错的一处）**
- 企业邮件安全网关（Microsoft Defender、Barracuda）会**自动 GET 邮件里的所有链接**。若 `GET` 即消费，用户还没点开就已失效。
- 因此：`GET` 只渲染"点击继续登录"按钮，**不消费令牌**；实际消费走 `POST`；对 `HEAD` 与预取（`Purpose: prefetch`）**不做任何状态变更**。
- App Links 域名下**只剩 Magic Link 一种用途**（好友邀请落地页已随 C2 整体删除），但 `assetlinks.json` 仍需保留。

**验收标准** 集成测试断言 `GET` 与 `HEAD` 不改变 `consumed_at`；`POST` 消费一次后重复 POST 返回 401。

**规格引用** §6.2、§7.1

> ✅ **已交付，两条验收标准都实测通过**（2026-09-07）。
> 容器内 `1712 tests / 8515 assertions` 全绿、**0 skip**；PHPStan level 8 无错，
> `deptrac` 0 violation / 0 uncovered、selftest 四个场景全过，
> `schema:validate` 与迁移 up/down 往返全绿，Spectral 0 warning。
> 覆盖率 `Module/Identity/Application` **97.4%**、`Identity/Domain` 92.8%、
> `Shared/Domain` 98.3%、整体 90.8%（门槛 85 / 85 / 70）。
>
> 契约**一个字没改**（`consumeMagicLink` 早在 T-007 就写好了），只给两个端点的
> `description` 补了说明。取舍全部记在
> [ADR-0016](../adr/0016-magic-link-delivery-and-landing-page.md)。
>
> ### 四个规格没回答、而答案会长成不同系统的问题
>
> - **① 链接从哪封信来？** 契约里没有「请求 Magic Link」的端点，
>   而 `magic_token_hash` 与 `code_hash` 在**同一行**上 —— 令牌只能是
>   `otp/request` 顺手签的。于是选了**同一封信**，`MailTemplate::MagicLink`
>   连同它的六个模板**退休**（§3.1 的外发邮件从四封变三封）。
>   发两封会让 §7.5 的「每邮箱 10 封/天」实际变成 20 条消息，而 ADR-0013 的
>   域名邮箱配额至今没有实测；更要紧的是一秒内到两封 Critical 信会训练用户
>   忽略这一类邮件，而 §7.2 T02 把它列为「邮箱被接管」唯一能被察觉的信号。
> - **② 落地页由谁提供？** 这张卡之前 `app.n-cards.de` 在整个仓库里**没有任何
>   服务端** —— Caddyfile 只有 `api.` 一个站点块，T-104 的 `/l/devices` 与
>   T-105 的 `/l/security` 在 DNS 层就打不开。新增 `infra/caddy/site/` +
>   第二个站点块，**只 `file_server`，没有 `reverse_proxy`**。
> - **③ 谁来 POST？** 契约的 `device.platform` 取值域只有 `android`、`device.id`
>   是安装级 UUID、refresh token 按 §7.1 只存 EncryptedSharedPreferences ——
>   **浏览器构造不出合法请求**。所以落地页的按钮是一次 `intent://` 交接，
>   由 App 发那个 POST。这一条规格没写，但契约已经把答案钉死了。
> - **④ 要不要耗时填充？** 不要，见下。
>
> ### ⚠️ 验收标准的前半条被改写了（这是本卡最该被 review 的一处）
>
> 「集成测试断言 `GET` 与 `HEAD` 不改变 `consumed_at`」在 PHPUnit 里**写不出来**：
> 落地页不在后端。取而代之是三个强制点，合起来比原文更强（原文只覆盖后端）
> 但分散在三处：
>
> 1. `tests/Api/RouteInventoryTest::testTheBackendServesNothingUnderTheAppLinksPath()`
>    —— 后端不注册**任何** `/l/` 路由。这是最接近原意的一条：
>    「GET 消费了令牌」在结构上无法发生，而不是靠某条用例守着。
> 2. `tests/Api/MagicConsumeEndpointTest::testOnlyPostIsRouted()` ——
>    本端点自己对 GET/HEAD 返回 405 且 `consumed_at` 不变（第二道保险：
>    万一哪天有人「顺手」把 API 地址放进模板）。
> 3. `scripts/ci/smoke-app-site.sh` —— 在**真栈**上 curl `GET` / `HEAD` /
>    `Purpose: prefetch`，并断言响应体里**不含令牌**（含了就说明这一页变成了
>    服务端渲染，那整个 ADR-0016 的论证就不成立了）。
>
> ### 相对任务卡的三处偏差（都不是范围蔓延）
>
> - **退休了一份 T-102 已交付的模板**（上面 ①）。那段「为什么这个链接不消费令牌」
>   的德语注释是那份模板里最有价值的东西，已原样搬进 `otp_code.html.twig`。
> - **`assetlinks.json` 托管做了，内容是空的 `[]`。** 仓库里没有 release keystore，
>   拿不到 `sha256_cert_fingerprints`。空列表是合法 JSON，效果是 App Links 验证
>   不通过 → 点链接落到 Web 落地页而不是直接拉起 App。**功能不坏，少一步。**
>   由 Ansible 按环境渲染（staging 与 prod 的签名证书不同，而 `infra/` 是同一棵
>   被 rsync 到两台机器的子树，所以不能提交一份写死的 JSON）。
> - **Android 侧不在本卡**：`intent-filter` + `autoVerify` + 那次消费调用归
>   T-151，验证清单归 M4。理由是 Android 连认证 UI 都还没有（T-150/T-151 未开工），
>   而指纹要等 release keystore。⚠️ **两边缺一边，链接都不会拉起。**
>
> ### 落地时踩到、值得记住的五个坑
>
> - **⚠️ 部分索引会让 `migration:check` 永久变红。**
>   `CREATE UNIQUE INDEX … WHERE magic_token_hash IS NOT NULL` 在功能上更好
>   （这一列大多数行是 NULL，而 PG 的普通唯一索引把每个 NULL 逐个存进索引），
>   但 **DBAL 读不回索引定义里的 WHERE 子句** —— `schema:validate` 从此永远报
>   「不同步」，`schema:update --dump-sql` 每次吐同一对 DROP/CREATE。
>   实测确认后改成普通 UNIQUE。这张表短命，代价可以忽略。
> - **⚠️ 测试库是 `ncards_test`，不是 `ncards`**（`when@test` 的 `dbname_suffix`）。
>   只对 dev 库跑迁移的话，唯一约束那条用例会「静默通过」——
>   它断言的 `UniqueConstraintViolationException` 根本不会抛，因为索引不在那个库里。
>   本卡就是这么先绿后红了一次。
> - **⚠️ XML 注释里不能出现 `--`。** 在 `OtpChallenge.orm.xml` 的注释里写了
>   `schema:update --dump-sql`，Doctrine 报的是
>   「The document has no document element」——**完全指不到那一行**。
> - **⚠️ `findByMagicTokenHash()` 必须带 `PESSIMISTIC_WRITE`，调用方必须包事务。**
>   与 T-105 的 `findByRefreshTokenHash()` 是同一个坑，但后果更直接：
>   两个并发 POST 各自在事务外读到 `consumed_at IS NULL`、各自通过
>   `consume()` 的判空、各自 UPDATE 同一行 —— **一个令牌换到两个会话**，
>   而库里只留下一条看起来完全正常的记录。
>   `SessionIssuer` 内部那个 `run()` 因此变成一个 SAVEPOINT（`use_savepoints: true`）。
> - **⚠️ 覆盖率是按 `#[CoversClass]` 归属的。** 把 `issueSession()` 那一段从
>   `VerifyOtpService` 抽成 `SessionIssuer` 之后，`Identity/Application` 的覆盖率
>   从 98.3% 掉到 **61.8%** —— 而**测试一条都没红**：那 94 行照样在跑，
>   只是没有任何测试声明覆盖它。给三个测试补上 `#[CoversClass]` 即回到 97.4%。
>   下一个做提取重构的人会撞同一堵墙。
>
> ### 手工验证（本地 compose + Mailpit，2026-09-07）
>
> ```
> POST /v1/auth/otp/request      → 202
>   Mailpit 收到 **1 封**（不是 2 封）德语信，
>   主题「142632 ist dein N-Cards-Anmeldecode」（码仍在主题里），
>   正文同时有 6 位码与 http://localhost:8081/l/magic/<43 字符 token>
>   库里该行 magic_token_hash 非空、32 字节，consumed_at = NULL
>
> GET   /l/magic/<token>          → 200 text/html
> HEAD  /l/magic/<token>          → 200
> GET   + `Purpose: prefetch`     → 200
>   三次之后 consumed_at 仍然 = NULL              ← 验收标准前半条
>   响应体里 grep 令牌 → 0 处（令牌只由浏览器侧 JS 从路径读）
>   Caddy 访问日志里这三条都是 file_server，没有一条到 app
>
> POST /v1/auth/magic/consume     → 200 + Session（expires_in=900）
>   users +1 且 **username IS NULL**、locale=de、status=active
>   JWT claims 恰好 sub/sid/did/jti/iat/exp
>   consumed_at 非空
> POST 同一个 token 再一次        → 401 token_invalid
>                                   「The sign-in link is not valid.」  ← 验收标准后半条
> ```
>
> `scripts/ci/smoke-app-site.sh http://localhost:8081` 13 条断言全绿
> （落地页三种方法、两个安全头、assetlinks 的三条、两个既有落地页、
> 以及「这个域下 `/v1/*` 返回 404」）。
>
> ⚠️ **本机 :80 被系统 Apache 占着**，所以 compose 的 caddy 是用
> `HTTP_PORT=8080` 起的（仓库里的默认值仍是 80，没动）。App Links 站点块在
> :8081 上，与 `backend/.env` 的 `APP_PUBLIC_BASE_URL` 对齐。
>
> ### ⚠️ 留给后续任务的五条
>
> - **上线前置条件（仓库外）**：`app.n-cards.de` 与 `app.staging.n-cards.de` 的
>   **A 记录必须先建**，Caddy 才签得出证书。首签前把 `ACME_CA` 指向
>   Let's Encrypt 的 staging 目录排练（`infra/caddy/README.md` 有流程）。
>   `deploy.yml` 里那一步带 `if`，域名没就位时**跳过而不是红** ——
>   但一旦填了 `app_site_url`，它就必须绿。
> - **T-151（Android onboarding）**：`AndroidManifest.xml` 要加
>   `intent-filter` + `android:autoVerify="true"`（host `app.n-cards.de`，
>   path prefix `/l/`），并把 release keystore 的 SHA-256 填进两个 group_vars 的
>   `ncards_android_cert_fingerprints`。**两边缺一边，链接都不会拉起。**
>   消费调用是 `POST /v1/auth/magic/consume {token, device}`，
>   响应与 `otp/verify` 是同一个 `Session` schema，客户端可以复用同一段解析。
> - **T-405（指标）**：`login_total{result}` 现在**聚合两条登录入口**
>   （otp verify 与 magic consume）。语义仍自洽（「一次登录尝试的结果」），
>   但分不出用户走的哪条。要分就加一个 `method` 标签 —— 那会改变既有序列的形状，
>   所以留给那张卡决定。`email_send_total{template}` 的 `magic_link` 取值已退休。
> - **T-113（清理）**：`otp_challenges` 现在多了一列有值的 `magic_token_hash`，
>   清理逻辑不变（按 `expires_at` / `consumed_at` 删整行）。删 `is_decoy` 列时
>   顺带确认 `uq_otp_challenges_magic_token_hash` 没被牵连。
> - **`base64UrlEncode()` 现在有 5 份拷贝**（`SessionIssuer`、`RequestOtpService`、
>   `RefreshTokenService`、`Ed25519AccessTokenSigner`、`Cursor` 各一份，
>   每份带各自语境的注释）。本卡照既有惯例又加了一份，没有收敛 ——
>   真要收敛的话，落点是 `Shared\Domain\Encoding`，而那会动到 T-004/T-104/T-105
>   三张已交付的卡，不该顺手做。

---

## T-107 Username 值对象与一次性设定端点

**预估** 1.5 人日 · **依赖** T-104, T-006（先决 Q9）

**交付物** `Identity/Domain/ValueObject/Username.php` + `POST /v1/me/username`。

**实现要点**
- 字符集 `^[a-z0-9_]{3,20}$`；归一化 `trim` + `toLowerCase`，**必须用 `Locale.ROOT` 语义**（土耳其语的 `I→ı` 会破坏匹配）。
- 保留词黑名单（配置文件维护）：`admin`、`support`、`ncards`、`help`、`root`、`system`、`info`、`kontakt`、`datenschutz`，德语场景补 `impressum`、`hilfe`、`konto`（Q9）。
- **一次性写入**：已非空 → `409 username_immutable`。**不存在 PATCH/PUT 对应端点——不可变是靠"没有这个端点"保证的**。
- 唯一冲突 → `409 username_taken`（这是**有意为之**的枚举面：注册时必须告诉用户名字被占了，否则功能不可用）。
- 校验失败 → `422 username_invalid`。
- 限流：按 user **10 次总计**（成功一次后该端点永久 409）。

**验收标准** §13.4-6 的 username 不变性测试全绿：`Anna_B ` 与 `anna_b` 视为同一个且第二次返回 `username_taken`；保留词被拒；设定后 `POST` 与 `PATCH /v1/me`（含 `username` 字段）**均**返回 409（不静默忽略）。

**规格引用** §3.8、§5.2、§6.2、§7.5、§13.4

> ✅ **已交付**（2026-09-07）。`composer qa` 全绿；容器内 `vendor/bin/phpunit`
> **1906 tests / 9443 assertions，0 failure、0 skip**；`migration:check` 三步全绿
> （up → down 往返 → `schema:validate` "The database schema is in sync"）；
> 覆盖率整体 90.2%、`Identity/Domain` 93.3%、`Identity/Application` 93.7%（门槛 85%）；
> `npm run lint:api` 无告警。
>
> 接之前，这张卡的两半在仓库里已经等了很久：`User::assignUsername()` 从 T-101 起
> 就写好了却没有调用方，`UserRepositoryInterface::findByUsername()` 同理，
> `LimitEnforcer` 上那三个 username 访问器的注释里逐字写着「留给 T-107」，
> 契约里 `Username` / `User` 两个 schema 齐备但 `paths` 下没有 `/me/username`。
> 也就是说**任何人注册完都卡在 onboarding**，钱包一张卡都建不了。
>
> ### 落地时冒出来的三个规格没回答的问题，合起来是 [ADR-0017](../adr/0017-username-assignment-and-lifetime-attempt-counter.md)
>
> §7.5 的「按 user 10 次总计」那一行同时踩到三件事：
>
> 1. **它存哪。** 印在速率限制表里，但 T-006 落地那天就已经把它排除在
>    `rate_limiter.yaml` 之外了（那个文件的页脚与 `RateLimitPolicyCoverageTest`
>    都逐字登记着这条豁免）：§8.2 的 ROPA 规定限流计数保留 **24 小时**，
>    而这个计数要跨越账号生命周期。→ 新增 `users.username_attempts`
>    （`Version20260907170000`，带 DEFAULT 的扩展列，§13.5 只需发一次）。
>    §17.1 与 §5.2 已回改。
> 2. **用尽返回什么。** 429 在契约里**强制**带 `Retry-After`，而这个计数
>    永不恢复 —— 只能编一个假秒数，而 Android 对 429 的处理是自动退避重试，
>    用户会看着一个永远转圈的按钮。→ `422 limit_exceeded`，新增
>    `SystemLimit::UsernameAttemptsPerUser`。§7.5 的那一行已从速率表移进限额表。
> 3. **哪些请求消耗它。** §7.5 给的理由是「用于试探占用情况」，而格式非法的
>    名字探不到任何占用。→ **只有走到唯一性检查的请求消耗**：`username_invalid`
>    与 `username_immutable` 都不消耗。反面的代价是具体的：客户端预校验
>    （T-010/T-151）与服务端正则一旦漂移，用户按十次「确认」就再也进不了钱包，
>    而 username 不可变、T-108 的拦截器又会挡住注销路径 —— 没有任何自助出路。
>
> ### 相对任务卡的一处偏差
>
> **验收标准后半条移交 T-108。** 「设定后 `POST` 与 `PATCH /v1/me`（含 `username`
> 字段）**均**返回 409」——`PATCH /v1/me` 是 T-108 的交付物，本卡一行都没有它。
> 强制点已经就位且是共用的：`User::assignUsername()` 在 `username` 已非空时抛
> `username_immutable`，T-108 接 PATCH 时复用同一条不变量即可，不需要新写判断。
> 沿用 T-106 显式改写验收标准的先例。前半条（`POST` 返回 409、不静默忽略）
> 由 `UsernameEndpointTest::testASecondAssignmentIsRejectedAsImmutable()` 在真栈上验。
>
> **Q9 起草完成、待产品确认**，不阻塞：12 个词在
> `config/packages/ncards_username.yaml`，精确匹配。
>
> ### 落地时踩到、值得记住的五个坑
>
> - **⚠️ `mb_strtolower()` 会把 §3.8 点名的土耳其语坑请回来。** 规格要求
>   `Locale.ROOT` 语义，直觉是用「更 Unicode 安全」的那个 —— 但它做完整折叠，
>   `İ`（U+0130）会变成 `i̇`（两个码位），一个本该被字符集直接拒掉的输入
>   就此长度也变了。PHP 8.2 起 `strtolower()` 已经不受 locale 影响、只映射
>   ASCII A–Z，那恰好就是 `Locale.ROOT`。值对象里有注释挡着「顺手改」。
> - **⚠️ 两次 flush 不能合并。** 计数必须在查重**之前**独立落库：Doctrine 在
>   flush 失败时会关闭 EntityManager，挤在一起的话撞唯一约束那一路的计数会被
>   一起丢 —— 于是这条限流恰好对**唯一真正在试探的人**失效，而所有测试照常绿。
>   与 T-104 把 `otp_challenges.attempts` 放在事务外保存是同一条。
>   `InMemoryUserRepository` 现在记调用顺序，就是为了让这条约束有测试守着。
> - **唯一冲突的翻译不能写进 `save()`。** `save()` 抛裸
>   `UniqueConstraintViolationException` 是它的既有契约（两条集成用例钉着），
>   而 `uq_users_email_hash` 的冲突是 T-104 的并发注册问题 —— 顺手翻成 409
>   只会让它更难查。新增 `saveNewUsername()`，只认 `uq_users_username` 这一个
>   约束名，其余原样冒泡。翻译必须在 Infrastructure：deptrac 不让 Application
>   看见 Doctrine。
> - **`json_encode([])` 编出来的是 `[]` 不是 `{}`，而 `{}` 也进不了校验。**
>   `AbstractApiController::decodeBody()` 用 `array_is_list()` 判顶层是不是
>   JSON 对象，而 `array_is_list([])` 恒为 true —— 于是空对象在那一层就成了
>   `malformed_request`。「省略必填字段」的用例要用一个**非空**对象来构造。
>   这是 T-004 的既有行为，本卡只是把它钉住（都是 400，分类不同）。
> - **⚠️ 保留词自己必须是合法的 username。** 不合法的保留词是死代码：值对象
>   先做字符集校验，一个像 `n-cards`（人读形态，ADR-0002）的词永远匹配不到
>   任何输入，看起来在保护品牌、实际一次都不会生效，而且没有任何测试会红。
>   `UsernameRulesTest` 逐条断言这一点 —— Q9 定稿时最容易踩的就是这个。
>
> ### 交给 T-108 的三件事
>
> - `me_username_set` **必须**进 onboarding 拦截器的白名单（与 `auth_logout`、
>   `me_get` 并列）。漏了的话唯一的出口被拦截器自己堵死。
> - `PATCH /v1/me` 收到 `username` 字段时调 `User::assignUsername()` 即得 409，
>   **不要**静默忽略。
> - `Application/Me/UserProfile` 与 `UsernameController::body()` 里那段 `user`
>   搬运直接复用，别写第三份 —— 契约里 `User` 只有一个 schema。

---

## T-108 onboarding 拦截器与 `/v1/me`

**预估** 1 人日 · **依赖** T-107

**交付物** `username IS NULL` 状态机的全局强制 + `GET /v1/me` + `PATCH /v1/me`（`locale`、通知偏好）。

**实现要点**
- `username IS NULL` 的用户处于 `onboarding_incomplete`：除 `GET /v1/me`、`POST /v1/me/username`、`POST /v1/auth/logout` 外，**所有** `/v1` 端点返回 `403 username_required`。
- **必须由单一的 Kernel 事件监听器统一拦截**，不得逐端点判断（漏一个就是权限洞）。
- `GET /v1/me` 返回 `"onboarding_complete": false`，客户端据此路由。
- `PATCH /v1/me` **已无 `display_name`**（C10 移除）。

**验收标准** 参数化测试遍历全部 `/v1` 端点，断言 onboarding 未完成的用户只能访问白名单三个。

**规格引用** §5.2、§6.2、§6.3.1

> ### 落地记录（2026-09-08）
>
> 拦截器、`GET /v1/me`、`PATCH /v1/me` 已落地。三个决策进了
> [ADR-0018](../adr/0018-onboarding-interceptor-placement-and-the-first-inverted-shared-port.md)，
> 这里只记它没覆盖的东西与踩到的坑。
>
> ### 相对任务卡的两处偏差
>
> - **`PATCH /v1/me` 只做 `locale`，通知偏好延后。** 任务卡与 §6.2 都写了
>   「locale、通知偏好」，但通知偏好在**整个仓库**没有任何数据模型：§17.1 的
>   `users` DDL 没有、`User.orm.xml` 没有、契约的 `User` schema 没有、
>   `Notification` 模块源码里也没有 —— 规格里唯一的出现是 §12.2 模块职责表里的
>   那四个字。M1 里也没有任何用户可关的通知（OTP 信与新设备提醒信都是安全类，
>   本来就不该可关）。在没有消费者的情况下发明一个偏好模型，等于把一列 JSONB 与
>   一份 schema 钉进 `/v1`，而 §13.6 禁止在 `/v1` 内删字段；反过来新增可选字段
>   是允许的。§6.2 那一行已回改并标注延后。
> - **`PATCH` 收到 `username` 时不调 `User::assignUsername()`**（T-107 的移交笔记
>   是这么写的）。理由见 ADR-0018 决定五：那个方法对一个**未完成** onboarding 的
>   调用者会真的把值写进去，绕过校验、查重与 10 次计数 —— 也就是开出第二条
>   username 写入路径，而它唯一的守卫恰好是本卡的白名单。改成 payload 第一行按
>   键是否存在直接抛，与实体共用 `User::USERNAME_IMMUTABLE_DETAIL`。
>   T-107 移交的验收标准后半句（「设定后 `PATCH /v1/me`（含 username）也返回 409」）
>   落在 `MeEndpointTest::testPatchRejectsAUsernameFieldAsImmutable()` 与
>   `testPatchWithAUsernameFieldChangesNothing()`。
>
> ### 落地时踩到的五个坑
>
> - **⚠️ `SessionLifecycleTest` 会在拦截器落地当天集体变红。** 它的 `login()` 走
>   真实 `otp/verify`，建出的用户 `username` 恒为 NULL，而文件里有 11 处打
>   `/v1/me/devices*` —— 全部变成 `403 username_required`，而那个症状会把人引向
>   「是不是鉴权坏了」。修法是给 `RequiresOtpStack` 加一个 `uniqueUsername()`，
>   让那个 `login()` 在 verify 之后补一次 `POST /v1/me/username`。
>   **按响应里的 `onboarding_complete` 判断，不能无条件设** —— 同邮箱第二次登录
>   （造「同一用户的两台设备」）时用户已经有 username 了，再设一次是 409。
>   `UsernameEndpointTest` 不动：它要的正是那个中间态。
> - **验收测试的预期必须取自规格，不能取自 `EXEMPT_ROUTES`。** 初版拿实现的常量
>   当预期，于是「表里多一行」这个方向被完全吞掉（多出来的那行既是实现也是预期，
>   测试恒绿）—— 而那正是权限洞的那一种。改成文件里一个 `SPEC_EXEMPT` 常量，
>   与 `AuthenticationCoverageTest` 拿契约对实现是同一个做法。
>   顺带：phpstan level 8 会把 `assertSame(['a','b'], SomeClass::CONST)` 判成
>   `alreadyNarrowedType` 恒真并报错 —— 对着常量自比的断言在这里根本编译不过，
>   这个门禁刚好挡住了那种没有价值的测试。
> - **两次 mutation 验证过测试不是空转**（值得对每一条「唯一强制点」都做一次）：
>   把白名单改成路径前缀 `/v1/me` → 4 条红（`PATCH /me` 与三个设备端点全泄漏）；
>   删掉 `me_username_set` → 1 条红，且失败文案直接说「多半是 EXEMPT_ROUTES 里
>   少了它」。
> - **`probe_*` 夹具不需要测试专用配置口子**，不像 `ncards_auth.yaml`。免鉴权路由
>   在 `AuthenticationListener` 写 `AUTH_CONTEXT` **之前**就返回了，所以永远不带
>   身份走到本拦截器 —— 「`null === $auth` 即放行」是结构性的。同一行顺带保住了
>   `POST /v1/auth/token/refresh`（§5.2 的三条清单里没有它，但挡住它的话，
>   卡在 onboarding 的用户 15 分钟后连令牌都换不了，于是连那三条也够不着）。
> - **`PATCH /v1/me {}` 是 `400 malformed_request`，不是 200 no-op。**
>   `AbstractApiController::decodeBody()` 用 `array_is_list()` 判顶层，而
>   `array_is_list([])` 恒为 true —— T-107 的落地记录已经登记过这个坑。
>   为 PATCH 单独放宽会动到每个端点，而一个什么都不改的 PATCH 只可能是客户端 bug。
>   契约的 `MeUpdate` 因此写 `required: [locale]`，测试钉住了这个行为。
>
> ### 顺手收掉的一处重复
>
> `UsernameController::body()` 提成了 `Identity\Http\UserBody::of()`（T-107 的
> `UserProfile` 注释指定的那件事）。`OtpVerifyController` /
> `MagicConsumeController` / `TokenRefreshController` 里那三份同形搬运**没动** ——
> 它们搬的是 `SessionIssued`（另一个 DTO），合并要先改那个 DTO 的形状，
> 会牵动三个控制器、三个服务与它们的测试。这件事登记在 `UserBody` 的类注释里。
>
> **无迁移**：`users.locale` 从 `Version20260905101500` 起就在，`changeLocale()`
> 从 T-101 起就写好了并标着「T-108」。
>
> 验证：容器内 1982 tests / 9942 assertions，0 failure、**0 skip**；
> 整体行覆盖 89.9%，`Identity/Application` 94.1%；deptrac 0 violation
> （反转端口不需要改 `deptrac.yaml`）；`schema:validate` 同步；Spectral 0/0。

---

## T-109 Wallet：cards 模型与 CRUD

**预估** 2.5 人日 · **依赖** T-005, T-104

**交付物** `cards` 表 + `GET /v1/cards`（游标分页）、`POST /v1/cards`、`GET /v1/cards/{id}`、`PATCH /v1/cards/{id}`、`DELETE /v1/cards/{id}`。

**实现要点**
- **`sort_order` 与 `is_pinned` 不在 `cards` 上**（在 `card_members` 上，见 T-110）。这是共享模型的必然结果，最容易漏掉。
- `barcode_value_encrypted` / `note_encrypted` 走 Vault Transit；列表查询**必须**用 `BatchDecryptor` 一次批量解密（禁止循环调 Vault）。
- `barcode_value_fingerprint BYTEA`：HMAC(payload)，用于重复卡检测，不可逆。
- `encryption_scheme TEXT NOT NULL DEFAULT 'server_v1'`（为二期 E2EE 预留）。
- **创建幂等**：body 含客户端生成的 UUIDv7 `id`；`id` 已存在且属同一用户 → `200` + 现有实体；属他人 → `409 id_conflict`。
- **更新乐观锁**：`If-Match: "<revision>"`；匹配 → `revision += 1` + `200`；不匹配 → `409 {"code":"revision_conflict","current":{…}}`。
- **删除**：软删（`deleted_at`，90 天后硬删）+ 墓碑。
- 权限：`PATCH`/`DELETE` 对**非 owner 一律 `403 insufficient_role`**，即使请求体合法、revision 正确。

**验收标准** 契约测试全绿；集成测试覆盖幂等创建、revision 冲突、批量解密性能基准（200 张卡 bootstrap P95 ≤ 700 ms）。

**规格引用** §5.2、§5.3、§5.4.3、§6.2、§9.1

> ### 落地记录（2026-09-08）
>
> `cards` 表 + 五个端点已落地，Wallet 模块从五个 `.gitkeep` 变成一个完整模块。
> 核心决策进了
> [ADR-0019](../adr/0019-cross-module-foreign-keys-via-post-generate-schema.md)，
> 这里只记它没覆盖的东西。
>
> ### 那颗雷拆了，而且是第四条路
>
> T-101 留下的 `cards.owner_id → users(id)` 跨模块外键，ADR-0011 列了三条出路
> 并写明「哪一条都不是顺手能定的」。落地时走的是**第四条**：外键照建在库里，
> 但**不进 ORM 元数据** —— `Card` 只有 `Uuid $ownerId` 这一个普通列，外键由
> `Shared\Infrastructure\Doctrine\CrossModuleForeignKeys` 在 Doctrine 的
> `postGenerateSchema` 上补进生成的 schema。
>
> 三条代价一条都不用付：库层外键在、deptrac **0 violation**、`skip_violations`
> 仍然是空的。而且它比 `skip_violations` 方案更硬 —— 拿不到 `$card->getOwner()`，
> 就写不出 `$card->getOwner()->getUsername()`，§4.2 规则 5 从此结构性成立。
>
> 载荷验证过：把那个监听器上的 `#[AsDoctrineListener]` 注释掉，
> `schema:validate` 当场报「不同步」；恢复即绿。
>
> ### ⚠️ 部分索引能用，T-106 的结论在 DBAL 4 上不成立
>
> `Version20260907140000` 的文件头记着「DBAL 读不回索引定义里的 WHERE 子句」，
> 因此 T-106 那条唯一索引放弃了谓词。重测下来**读得回**，之前撞墙的原因是
> `Index::samePartialIndex()` 拿两侧的 `where` 做 `===` 比较，而 PG 存回来的是
> 规范化结果、**带一对外层括号**：
>
> ```
> .orm.xml 里写 `deleted_at IS NULL`   → 不同步
> .orm.xml 里写 `(deleted_at IS NULL)` → 同步 ✅
> ```
>
> 所以 §17.1 的 `idx_cards_owner … WHERE deleted_at IS NULL` **原样落地，没有偏离规格**。
>
> ⚠️ **T-110 需要这条结论**：`uq_card_single_owner` 的谓词是不变量本身、去不掉，
> 按旧结论它根本落不了地。照 `Card.orm.xml` 的形状写即可。完整论证见 ADR-0019 末尾。
>
> ### 相对任务卡的三处增补，与一处刻意的缺席
>
> - **契约补了 `POST /v1/cards` 的 `200`。** 任务卡与 §5.4.3 都写「同一个 `id`
>   重复提交 → 200 + 现有实体」，而 `docs/api/openapi.yaml` 只声明了 `201` ——
>   是契约漏了。按 §13.1 同一个 PR 里补上，顺带给 `201` 声明了 `Location` 头。
>   ⚠️ 幂等重放**不应用请求体**：一个迟到的离线 outbox 条目会把用户后来改过的
>   标题静默覆盖回去，而建卡请求不带 revision，没有任何冲突检测会发现。
> - **三条长度限额提前到本卡。** T-111 才是「§7.5 落地」，但 `cards.title` 在库层
>   带 `CHECK (char_length(title) <= 100)` —— 不在应用层挡住就是一个 **500**，
>   而不是契约写的 `422 limit_exceeded`。`enforceLength()` 与那三个值 T-006 都已备好，
>   每处一行。`cards_per_user`（500）的强制点也一并建好，T-111 补它的边界测试。
>   ⚠️ `merchant_label` 的 100 字符**不是** §7.5 的额度（那张表里没有它），
>   所以它是 `400 validation_failed` + `too_long`，理由记在 `CardFields` 的注释里。
> - **`Shared\Http\Concurrency\IfMatch`。** `If-Match` 的解析此前整个仓库都没有。
>   放 `Shared.Http` 是因为它解析的是一个标准 HTTP 头，与 `CursorPaginator` 解析
>   `?cursor=&limit=` 同类。缺失/`*`/弱验证器一律 `400 validation_failed` ——
>   为什么不是 `428 Precondition Required`，见那个类的注释。
>
> **刻意缺席**：契约 `Card` 里的 `sort_order` / `is_pinned` / `member_count` /
> `owner_username` **一个都不发**（它们本来就不在 schema 的 `required` 里）。
> 前三个来自 `card_members`（T-110），第四个要跨模块读 Identity 而 T-109 阶段
> owner 恒为调用者本人。**没有**补常量占位：客户端会把 `sort_order: 0` 当成用户
> 真实的排序存进本地库，T-110 上线后本地与服务端不一致且没有任何信号提示要重拉。
> `my_role: "owner"` / `can_edit: true` 照发 —— 那两个在单人钱包阶段是**真的**，不是占位。
>
> ### 顺手收掉的一处重复
>
> `RequiresOtpStack` 多了 `registerOnboardedUser()`。T-108 之后每个「登录后打某端点」
> 的用例都要先设 username，`SessionLifecycleTest::login()` 已经写过一份，
> cards 的用例是第二份 —— 第三份出现之前提到 trait 里。
> 拿注册中间态当被测对象的 `UsernameEndpointTest` / `OnboardingCoverageTest`
> **不**用它，它们要的正是那个没有 username 的状态。
>
> ### 三张卡的边界没有被挪动
>
> `card_members` 一行都没建（T-110），墓碑记录一行都没写（T-201 的
> `ChangeLogSubscriber`），`PUT /v1/cards/{id}/placement` 没有实现（T-110）。
> `DELETE` 只写 `deleted_at`，90 天硬删也还没有清理任务。
>
> ### ⚠️ 一处「测不出东西」的地方，登记在这里
>
> `encryption_scheme` 一期只有 `server_v1` 一个值，所以**任何**在单元层对它的断言
> （比枚举实例、比 `->value`）对静态分析都是恒真的 —— phpstan 会直接把它报成
> `staticMethod.alreadyNarrowedType`，而它确实什么都证明不了。
> 那两条断言因此被删掉，真正的保证分给了两个够得着的地方：
> 库里的 `DEFAULT 'server_v1'` 由 `WalletSchemaTest::columns()` 对着
> `information_schema` 断言；枚举 ↔ TEXT 的往返由 `DoctrineCardRepositoryTest`
> 读**真列**（`SELECT encryption_scheme …`）来比。
> 代价是 `EncryptionScheme::default()` 那两行没有覆盖（Domain 55/57）。
> 二期加第二个 case 时这条约束自动消失。
>
> 验证：容器内 **2146 tests / 10766 assertions**，0 failure、**0 skip**；
> 整体行覆盖 90.6%，`Module/Wallet/Domain` 96.5%、`Module/Wallet/Application` 98.7%
> （门槛 85%）；phpstan level 8 clean；deptrac **0 violation / 0 skipped violation**，
> 自检 4 个场景全过；`schema:validate` 在 down/up 往返之后同步；Spectral 0/0。

---

## T-110 card_members 与 placement 端点

**预估** 1 人日 · **依赖** T-109

**交付物** `card_members` 表 + partial unique index + `PUT /v1/cards/{id}/placement`。

**实现要点**
- 建卡时**同时**写入一行 `card_members(role='owner')`——单人钱包阶段也必须有，否则 M3 的共享要回头补数据。
- ```sql
  CREATE UNIQUE INDEX uq_card_single_owner
    ON card_members (card_id) WHERE role = 'owner' AND left_at IS NULL;
  ```
- `placement`（`sort_order` / `is_pinned`）是**成员私有字段**，**不走 revision 锁**；owner 与 viewer 均可调用——这是 viewer 唯一的写入端点。
- `left_at` 是成员墓碑字段，M3 会重度使用，此处先建好。

**验收标准** 集成测试断言：尝试插入第二行 owner 被索引拒绝；两个成员各自的 placement 互不影响。

**规格引用** §5.2、§6.2

> ### 落地记录（2026-09-09）
>
> `card_members` + `PUT /v1/cards/{id}/placement` 已落地。核心决策进了
> [ADR-0020](../adr/0020-card-members-belongs-to-sharing-and-wallet-reads-it-through-three-ports.md)，
> 这里只记它没覆盖的东西。
>
> ### 这张表归 Sharing，不归 Wallet —— 于是 M1 里立起了第二个模块
>
> 任务卡在 M1、依赖 T-109，读起来像是「往 Wallet 里加一张表」。但 §4.2 的
> 模块图逐字把「卡成员（owner/viewer）」划给 **Sharing**，而 M3 的三张卡
> （T-303 级联撤销、T-304 邀请接受、T-305 成员管理）**全都**在这张表上写。
> 归 Wallet 会让那三张卡反过来经 Wallet 的 Port 写「自己的」表，
> Sharing 变成空壳。
>
> 所以 T-110 实际交付的是 **Sharing 模块的第一批代码**：实体（本仓库第一个
> 复合主键实体）、值对象、仓储、三个端口、两个 DTO、一个服务。
> **1 人日的估算按「加一张表 + 一个端点」算，实际接近 2.5** —— 清单里没有
> 一项超出任务卡的交付物，只是「建一张表」在这个仓库里的真实成本
> （新模块的 `Domain` / `Application` 各要过 85% 覆盖率门槛，外加一篇 ADR）。
>
> ### ⚠️ ADR-0019 的「两条」是错的，实际是三条
>
> 那个计数只有在 `card_members` 归 Wallet 时才成立（那样 `card_id → cards`
> 是模块内部外键）。归 Sharing 之后 `card_id → cards` 也跨模块，
> 而且它是清单里**第一条指向另一个模块业务表**（而不是 `users`）的外键。
> ADR-0019 的「影响」行与 Consequences 已随本卡修订，末尾补了一节「T-110 补记」。
>
> ### 部分索引：ADR-0019 的判断成立，谓词一次就对
>
> `uq_card_single_owner`（`WHERE role = 'owner' AND left_at IS NULL`）原样落地，
> `schema:validate` **第一次就同步**。做法是先建表再拿真值，而不是照着
> `Card.orm.xml` 猜：
>
> ```sql
> SELECT indexdef FROM pg_indexes WHERE tablename = 'card_members';
> ```
>
> PG 规范化后是 `((role = 'owner'::text) AND (left_at IS NULL))`。
> ⚠️ 是 `role` 不是 `(role)::text` —— 那一列是 **TEXT** 不是 VARCHAR，
> 计划阶段猜的就是错的那个形式。抄错的症状是 dump-sql 反复吐 DROP + CREATE。
>
> ⚠️ 顺带确认了一件 ADR-0019 第 3 条容易被误读的事：**三条外键列不需要各补一条
> `<index>` 声明。** 那一条管的只是「迁移里真的建了的索引」；DBAL 给外键列自动
> 补的 `IDX_<hash>` 在 ORM 侧与库侧内省时**都会**生成、同名互相抵消
> （`Table::_addForeignKeyConstraint()` 自己的注释说明了后者）。
> 所以 XML 里只声明了 `uq_card_single_owner` 与 `idx_card_members_user` 两条。
>
> ### 三个窄端口，而不是一个胖端口
>
> `CardOwnershipRegistrarInterface` / `CardMembershipReaderInterface` /
> `CardPlacementWriterInterface`，由**一个类**实现。合并的诱惑很大（三个方法、
> 同一张表），但它们的**前置条件互相矛盾**：registrar 必须在事务内
> （`isInTransaction()` 断言 + 抛 `\LogicException`），reader 绝不该在，
> writer 两者都不要求。而且 reader 要注进 `CardViewAssembler` ——
> 那个类不该拿到 `registerOwner()` 这种写能力。口径同 Notification 的三个端口。
>
> ### placement 端点留在 `Wallet.Http`，判据是 `CardBody`
>
> 「写的是 Sharing 的表，端点也该归 Sharing」这个直觉由端口满足即可。
> 真放过去要克隆两个类：`CardView`（装**明文码值**，它「只能由
> `CardViewAssembler` 构造」的不变量当场作废）与 `CardBody`
> —— 而后者的类注释写着它存在的全部理由就是「组装代码有第二份的话，
> 加字段时必然漏一处，且漏掉的那个端点的契约测试照样是绿的」。
> **T-110 正是给 `Card` 加两个字段的那次提交**，五个既有端点一行没改，
> 那句注释第一次被兑现。
>
> ### ⚠️ 留下一处「两个真相来源」，收口在 T-305
>
> 角色来自 `card_members`（assembler），可见性仍来自 `Card::isOwnedBy()`
> （`CardQueryService::get()`）。M1 里恰好一致只因为没有 viewer。
> 于是出现一个真实的不对称：**viewer 能 `PUT …/placement`，却不能 `GET` 同一张卡。**
>
> 落地时这一条把 `CardPlacementEndpointTest` 里那条「两个成员各自 placement
> 互不影响」用例弄红过一次（viewer 侧 `GET` 返回 403，读回来是 null）。
> 处理方式不是去改 placement，而是：用例改成读 `PUT` 的 200 响应体
> （那本来就是「调用者视角的卡」），并**新增一条把已知的错断言下来**的用例
> `testAViewerStillCannotGetTheCardUntilT305()`。T-305 落地时它会红 ——
> 那时该改的是它，不是回头改 placement。修法写在 `CardQueryService` 的类注释里：
> 把检查**挪到**成员表上，而不是再加一次查询。
>
> ### 相对任务卡的三处增补
>
> - **迁移带数据回填，且刻意包含软删的卡。** 不回填的话 T-109 期间建的每张卡
>   都变成「没有成员行的孤儿」。含软删是因为 `DELETE` 只写 `deleted_at`、
>   那个 id 仍被占用，而建卡的幂等重放会把它原样返回。目标是让
>   `cards → card_members` 是**全函数**，于是「卡有、owner 行没有」从一种状态
>   降级成一个 bug —— `CardViewAssembler::assertComplete()` 直接抛（500 + 日志），
>   不回退到任何默认角色（三条备选都更糟，写在那个方法的注释里）。
> - **`sort_order` 的 int32 范围校验。** 列是 PG 的 `INTEGER`，而
>   `{"sort_order": 9223372036854775807}` 是一个**合法的 JSON 整数**，
>   `is_int()` 收得下，然后在 flush 时变成一个 **500** —— 而契约写的是 400。
>   一个字段级校验漏掉就直接送 500 的洞，朴素实现一定会踩。
>   `CardFields::requiredInt32()` + `out_of_range`。
> - **`CardMember::leave()`。** 任务卡说「`left_at` 此处先建好」，而只建列不建
>   写入口的话 phpstan level 8 会报「never assigned」（baseline 必须保持为空）。
>   建全它正好也是任务卡要的 —— T-303 的级联撤销直接用它。
>
> **刻意缺席**：`member_count` 与 `owner_username` 仍然不发。前者 §5.2 要求
> 只对 owner 可见（C11）且 M1 阶段恒为 1，连同那条按角色裁剪的逻辑留给 T-305；
> 后者要跨模块读 Identity。`CardView` 里那句「不要补常量占位」的警告**留着**，
> 现在是为这两个字段服务的。
>
> **`DELETE /v1/cards/{id}` 不动 `card_members`**：软删一张卡不给 owner 行写
> `left_at`，因为 §5.2 要求卡墓碑的 audience 取**删除前**的成员快照。
> 这一条写进了迁移文件头与 `DeleteCardService` 的类注释，免得 T-201 有人来「修」它。
>
> ### ⚠️ 一处工具层面的坑，登记在这里
>
> `Sharing/Application` 第一次跑覆盖率是 **64.5%**（门槛 85%），而那些代码
> 明明被测到了。原因不在代码：**纯数据 DTO（只有提升构造器）在这套 pcov 配置下
> 本来就是 0 覆盖** —— `CardView`（T-109）与 `Identity` 的 `UserProfile`
> 一直是 0/1 与 0/8，只是它们所在目录的分母够大，没人注意到。
>
> 真正的异常只有 `CardMembershipMap` 的 0/10：它只被 `CardMembershipServiceTest`
> **间接**走过，而 php-code-coverage 只给测试**直接指名**的类记账。
> 补一个 `CardMembershipMapTest`（`#[CoversClass]`）就回到 10/10。
> 那个测试本来也该有 —— `missingFrom()` 的去重与保序在服务那一层看不出来。
>
> 排查中试错过的三条（都不是原因，免得下次重走）：清 `var/.phpunit.cache`、
> 关 `opcache.enable_cli`、调大 `pcov.initial.files`。
> ⚠️ 另外：`var/` 是容器里的**独立卷**，在宿主机 `rm` 它不生效；
> 而 `/app` 是**只读**挂载，所以 `php-cs-fixer fix` 与 `composer migration:check`
> （容器里没有 bash）都要在宿主机跑。
>
> 验证：容器内 **2250 tests / 11096 assertions**，0 failure、**0 skip**；
> 整体行覆盖 90.1%，`Module/Sharing/Domain` 100%、`Module/Sharing/Application` 96.8%
> （门槛 85%）；phpstan level 8 clean（baseline 仍为空）；deptrac **0 violation /
> 0 skipped violation**，自检 4 个场景全过；`schema:validate` 在 down/up 往返之后
> 同步；Spectral 0/0（契约未改 —— placement 端点 T-109 时就已声明）。

---

## T-111 系统限额落地

**预估** 0.5 人日 · **依赖** T-006, T-109

**交付物** 在写入路径强制 §7.5 限额：每用户卡数 500、payload ≤ 1024 字节、note ≤ 2000 字符、title ≤ 100 字符。超限 → `422 limit_exceeded`。

**实现要点** **共享卡不计入 viewer 的 500 卡限额**（Q11）——否则 owner 可通过共享消耗他人配额，是一种滥用面。计数只统计 `owner_id = :user`。

**验收标准** 单测覆盖每个限额的边界值（499/500/501）。

**规格引用** §7.5、§17.5 Q11

---

## T-112 `GET /v1/config`

**预估** 0.5 人日 · **依赖** T-004

**交付物** `{min_supported_client, latest_client, maintenance:{active, message_key, retry_after}, feature_flags:{}}`，**无需认证**。

**实现要点**
- 结合 `X-Client` header 判定强制升级：低于 `min_supported_client` → `426 client_too_old`。
- `maintenance.message_key` 是**键不是文案**——面向用户的德语文案由客户端本地化生成。
- 计划维护窗口每周二 03:00–04:00 CET，提前 24 小时通过此端点下发。

**验收标准** 契约测试；用旧 `X-Client` 请求任意端点返回 426。

**规格引用** §3.10、§6.2、§9.2

---

## T-113 Scheduler 与清理任务

**预估** 1 人日 · **依赖** T-101, T-107

**交付物** `scheduler` 容器（Symfony Scheduler）+ 每日任务：
- 删除 `username IS NULL` 且 `created_at < now() - 7 days` 的僵尸注册行（他们只是一条邮箱哈希，无关联数据，可安全物删）
- 清理过期/已消费的 `otp_challenges`
- 清理 30 天前的 `request_ip_hash`

**验收标准** 集成测试断言各清理任务的边界（第 7 天 vs 第 8 天）；`worker` 与 `scheduler` 在 compose 中分别可独立重启。

**规格引用** §5.2、§8.2、§14.2

---

## T-150 令牌存储与网络认证层

**预估** 1.5 人日 · **依赖** T-009, T-010

**交付物** `data:auth` Repository + OkHttp `Authenticator`/`Interceptor`：附加 Bearer、401 `token_expired` → **静默刷新后重试一次**、401 `token_invalid` → 清空本地会话跳登录。

**实现要点**
- Access token 存内存 + `EncryptedSharedPreferences`；refresh token **仅存** `EncryptedSharedPreferences`。
- 刷新必须串行化（Mutex），避免并发 401 触发多次轮换——**轮换重放检测会把用户踢下线**。
- 错误映射对 `code` 分支，**不得解析 `detail`**。

**验收标准** 单测：并发 5 个 401 只触发一次刷新；`token_invalid` 会清空本地会话。

**规格引用** §6.1、§7.1、§7.3

---

## T-151 Onboarding 流程

**预估** 2.5 人日 · **依赖** T-150, T-107

**交付物** `feature:onboarding`：语言/隐私说明 → 邮箱输入 → 6 位码输入（含重发倒计时 60s）→ **username 设定页** → 进入空钱包 → 引导"添加第一张卡"。

**实现要点**
- username 设定页**不可跳过、不可返回**；由 `GET /v1/me` 的 `onboarding_complete: false` 路由过来。
- **必须**二次确认对话框，文案明确：`Dieser Name kann später nicht geändert werden.`——这是本项目为数不多的不可逆用户操作，和删号同级对待（R13）。
- 输入时**实时显示字符集规则**；客户端也做 `trim` + `toLowerCase(Locale.ROOT)` 归一化（与服务端一致）。
- 页面**必须**提示："其他人可通过该名字找到你，请勿使用真实姓名或邮箱"（GDPR 数据最小化 + 用户知情）。
- 注册页链接 AGB / Nutzungsbedingungen。

**验收标准** Compose UI Test 覆盖 J1 旅程；`username_taken` / `username_invalid` 各有对应的本地化错误提示。

**规格引用** §1.4 J1、§3.8、§6.3.1、§8.6、§16 R13

---

## T-152 `core:barcode` 渲染与格式映射

**预估** 1.5 人日 · **依赖** T-008

**交付物** ZXing `MultiFormatWriter` → `BitMatrix` → Bitmap 渲染器 + **唯一**的 `BarcodeFormat` 枚举（ML Kit ↔ ZXing ↔ 展示名的映射集中在 `core:model`）。

**实现要点**
- **必须按目标 `View` 尺寸生成**（不要生成小图再放大——边缘模糊会导致扫码枪读不出）。
- **必须强制白底黑码**，无论 App 主题深浅。深色模式下的条码是扫码失败的经典原因。
- 1D 码 `margin` 至少 10 模块宽（静区），否则部分扫码枪拒读。
- 渲染在 `Dispatchers.Default`，结果缓存于内存 LruCache（key = format + value + size）。
- **禁止**在各处散落 `when` 做格式映射。
- 支持码制：EAN-13/8、UPC-A/E、Code 128/39/93、ITF、Codabar、QR、Aztec、PDF417、DataMatrix。

**验收标准** 单测覆盖全部码制的映射往返；真机上用扫码枪/另一台手机实测能读出 EAN-13 与 QR。

**规格引用** §1.3、§10.1

---

## T-153 钱包列表

**预估** 2 人日 · **依赖** T-009, T-152（Q7 调色板）

**交付物** `feature:wallet`：Room `Flow` 驱动的卡片列表、搜索、置顶、拖拽排序、颜色/首字母图标、空状态、`sync_state` 徽章。

**实现要点**
- **离线优先铁律**：UI **永远**从 Room 的 `Flow` 读取，**绝不**直接消费网络响应渲染。
- `UiState` 用 sealed interface（`Loading`/`Content`/`Empty`/`Error`），**禁止**多个独立布尔标志。
- 排序/置顶写入的是**成员私有**的 placement，不参与 revision。
- 调色板色值需逐个校验对比度 ≥ 4.5:1（Q7）。
- 性能预算：温启动首帧 P95 ≤ 400 ms；200 张卡滚动 P99 帧耗时 < 16.6 ms。

**验收标准** ViewModel 单测（Turbine）；200 张卡的滚动无掉帧；德语长词（`Benachrichtigungseinstellungen`）不截断。

**规格引用** §4.3、§9.1、§10.4、§11.2

---

## T-154 卡详情与全屏条码页

**预估** 2 人日 · **依赖** T-152, T-153

**交付物** 卡详情页 + **独立 `Activity`** 的全屏条码页。

**实现要点**
- 亮度 = `1.0f`；`FLAG_KEEP_SCREEN_ON`；**`onPause` 必须恢复原亮度**（否则用户会投诉耗电）。
- 支持横屏放大 1D 码。
- **无障碍硬要求（不是可选项）**：码值必须以**可选中的大字号文本**显示在条码下方，带 `contentDescription`，使 TalkBack 用户能让系统朗读、口述给收银员。这是银发用户与视障用户的关键功能。
- 性能预算：Widget 点击 → 条码完全渲染 P95 ≤ 800 ms（冷启动 ≤ 1.8 s）。
- SHOULD：设置项"允许截屏"（默认允许；关闭时加 `FLAG_SECURE`）。

**验收标准** Macrobenchmark 达标；TalkBack 能读出码值；退出后亮度恢复。

**规格引用** §9.1、§10.2、§11.2

---

## T-155 手动新增/编辑卡

**预估** 1.5 人日 · **依赖** T-153

**交付物** `feature:cardedit`：标题、商家名、颜色选择、码制选择、码值手输、备注（≤ 2000 字符）、`expires_on` 日期。

**实现要点**
- **写入顺序是铁律**：先写 Room（乐观更新，`sync_state = PENDING`）→ 再入 outbox → SyncEngine 异步推送。网络失败**不得回滚 UI**，只更新 `sync_state` 并显示细微徽章。
- 客户端生成 **UUIDv7** 作为主键——离线创建的实体从一开始就有稳定 ID，无需"临时 ID → 服务端 ID"重映射。
- `expires_on` 一期仅本地展示与排序，**不做推送提醒**（提醒是二期，字段先有以避免二期改表）。

**验收标准** 飞行模式下新增卡立即可见且带待同步徽章（J4 旅程）。

**规格引用** §1.4 J4、§3.13、§4.3

---

## T-156 相机扫描（CameraX + ML Kit）

**预估** 2 人日 · **依赖** T-152, T-155

**交付物** `feature:scan`：CameraX Preview + `ImageAnalysis(STRATEGY_KEEP_ONLY_LATEST)` + ML Kit 全码制识别。

**实现要点**
- **必须用 bundled 模型**（`com.google.mlkit:barcode-scanning`），不用 `play-services-mlkit-*`（后者首次使用需下载模型，弱网/无 GMS 设备会失败）。约 +2.5 MB APK，值得。
- **连续 3 帧识别到同一 `(format, rawValue)` 才确认**——防误识别，这是关键。
- 确认后：触觉反馈 + 声音 → 停止分析 → 进入编辑页。
- **必须**提供手电筒开关（超市光线差）与手动输入回退入口（ITF/Codabar 等磨损卡识别率低）。
- 相机权限被拒 → 直接进入**图片录入或手动输入**页，**不做无意义的挽留弹窗**。

**验收标准** 真机扫描 EAN-13/QR/Code 128 成功；权限拒绝路径不出现死胡同。

**规格引用** §10.1、ADR-09

---

## T-157 从图片录入（Photo Picker）

**预估** 2 人日 · **依赖** T-156

**交付物** `feature:imageimport`：`PickVisualMedia(ImageOnly)` → 降采样解码 → ML Kit 静态图识别 → 0/1/多码分支。

**实现要点**
- **必须用 Photo Picker**，**不得**申请 `READ_MEDIA_IMAGES` / `READ_EXTERNAL_STORAGE`——用户只授予单张图片的临时访问，这让隐私声明可以写"我们无法访问你的相册"。
- **图片绝不落盘、绝不上传、绝不进日志**（含 Sentry 附件与面包屑）。这是 ADR-12（不做图片存储）与 ADR-13 能共存的唯一前提。解码后立即 `bitmap.recycle()`，不持有 Uri。
- **必须降采样**：`ImageDecoder + setTargetSampleSize`，长边 ≤ 2048 px。4000×3000 直接 Bitmap 化在 2 GB RAM 低端机上会 OOM。2048 px 既保证 1D 码最小模块宽 ≥ 2 px，又把内存控制在 ~16 MB。
- `InputImage.fromBitmap(bitmap, rotationDegrees = 0)`——ImageDecoder 已应用 EXIF 旋转，再传角度会转两次。
- 解码在 `Dispatchers.Default`，超过 5 秒视为失败。
- **必须复用与相机路径完全相同的** `BarcodeScanner` 实例与格式映射入口，**禁止为图片路径写第二套 `when` 分支**。
- 结果分支：0 个 → "未识别到条码" + `[换一张图片] [手动输入]`（**不做无意义的重试引导**）；1 个 → 直接进编辑页预填；≥2 个 → 在缩略图上按 `boundingBox` 画框让用户点选（按面积降序，最大的通常是主码）。
- SHOULD（可裁剪，见 README）：`ACTION_SEND` + `image/*` intent-filter，支持从其他 App 分享图片直达本流程。

**不做**：图片裁剪/旋转编辑器、批量导入、从 PDF 导入（§1.3 范围蔓延）。

**验收标准** §13.4-8 图片解码测试：单码图、多码图、无码图、4000×3000 超大图（不 OOM）、EXIF 旋转 90° 的图；断言与相机路径产出**相同**的 `BarcodeFormat` 与 `rawValue`。

**规格引用** §1.4 J1b、§7.3、§10.1、§13.4、ADR-13

---

## T-158 强制升级墙与维护横幅

**预估** 0.5 人日 · **依赖** T-150, T-112

**交付物** 启动时拉 `GET /v1/config`；`426 client_too_old` → 全屏强制升级墙；`maintenance.active` → 顶部横幅（文案本地化生成）。

**实现要点** 升级墙是**不可绕过**的全屏拦截（离线优先架构下用户可能长期不更新，这是唯一的推动手段）。维护期间**离线读卡不受影响**——这是离线优先架构的额外收益，横幅文案要体现。

**验收标准** 用低版本 `X-Client` 启动出现升级墙；维护横幅可关闭但下次启动重现。

**规格引用** §3.10、§9.2
