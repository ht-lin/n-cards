# PHPStan baseline。
#
# §13.3：baseline **只允许缩小，不允许增长**。
#
# 新代码不得往这里加条目 —— 报错就在 PR 里修掉。本文件只用于将来某次
# PHPStan 或 Symfony 大版本升级时，把一次性涌现的存量报错临时收拢，
# 并且必须在同一个 PR 的描述里写明清理计划。
#
# 当前为空，这是期望状态。

parameters:
    ignoreErrors: []
