Your sign-in code for N-Cards:

{{ code }}

The code is valid for {{ expires_in_minutes }} minutes and can only be used once.

On your phone you can skip the typing — this link signs you in:

{{ magic_link_url }}

The code and the link belong to the same sign-in: once you have used one of them, the
other stops working.

Didn't request a sign-in? Just ignore this email. Without the code and without the link,
nobody can sign in with your address.

We will never ask you for this code by email or by phone.

--
N-Cards
This message was sent automatically. Replies are not read.
