# Symfony 的**默认值**文件：只放非密钥的默认配置，随仓库入库。
#
# 加载顺序（后者覆盖前者）：
#   .env                默认值（本文件，入库）
#   .env.local          本地覆盖（不入库）
#   .env.$APP_ENV       环境默认值（.env.test 入库，其余不入库）
#   .env.$APP_ENV.local 本地环境覆盖（不入库）
# 真实环境变量优先于以上所有文件。
#
# ⚠️ 绝不在本文件（或任何入库文件）里写生产密钥 —— §14 / §7.4。
# staging 与 prod 的真实值由 sops(age) 加密后随 Ansible 下发（T-012），
# 容器内以真实环境变量注入（T-003）。
#
# 根 .gitignore 默认忽略 .env 与 .env.*，本文件与 .env.test 是两处显式放行，
# 见 .gitignore 中「Symfony dotenv」一段的说明。

###> symfony/framework-bundle ###
APP_ENV=dev
# 开发用占位值，不具备任何安全意义；staging/prod 必须由环境变量覆盖。
APP_SECRET=dev-only-not-a-secret
APP_SHARE_DIR=var/share
###< symfony/framework-bundle ###

###> symfony/routing ###
# 非 HTTP 上下文（CLI 命令）生成 URL 时使用的基地址。
DEFAULT_URI=http://localhost
###< symfony/routing ###

###> doctrine/doctrine-bundle ###
# 本地裸机（不经 compose）跑 composer test 时用的默认值。密码是占位符，
# 没有任何安全意义 —— 与上面的 APP_SECRET 同一口径。
#
# 在容器里，compose 用 infra/compose/.env 的 POSTGRES_* 拼出真实的 DATABASE_URL 注入
# （见 infra/compose/docker-compose.base.yml）；staging/prod 由 sops(age) 下发（T-012）。
# 驱动与 server_version 已在 config/packages/doctrine.yaml 里钉死，这里不重复。
DATABASE_URL="postgresql://ncards:ncards@127.0.0.1:5432/ncards"
###< doctrine/doctrine-bundle ###

###> ncards/backing-services ###
# T-003 起在 compose 内网可达。这里是「compose 注入什么」与「应用读什么」的唯一对照表。
#   REDIS_URL  → T-004 的幂等键（已消费）与 T-006 的限流
#   VAULT_ADDR → T-005 的 Transit 加密门面（已消费）
REDIS_URL=redis://127.0.0.1:6379
VAULT_ADDR=http://127.0.0.1:8200
###< ncards/backing-services ###

###> ncards/vault-auth ###
# T-005：应用怎么向 Vault 证明自己（§3.3 / §7.4 / §17.4）。
#
# 二选一，判据是「配没配 VAULT_ROLE_ID」，不是 APP_ENV：
#
#   dev / test      VAULT_TOKEN = compose dev 模式的 root token。
#                   root token 没有 policy 限制，只在本地与 CI 用。
#   staging / prod  VAULT_ROLE_ID + VAULT_SECRET_ID，走 AppRole，
#                   受 infra/vault/policies/ncards-app.hcl 的最小权限约束。
#                   由 sops(age) 加密后随 Ansible 下发（T-012），**且不下发 VAULT_TOKEN**。
#
# 选择逻辑在 Shared\Infrastructure\Vault\VaultTokenProviderFactory，那里有完整理由。
#
# ⚠️ VAULT_SECRET_ID 是**凭据**。绝不写进本文件或任何入库文件（§14 / §7.4）。
# 下面两行留空是刻意的 —— 它们的存在只是为了让「应用会读这两个变量」有据可查，
# 空值时工厂会回落到 VAULT_TOKEN。
VAULT_TOKEN=dev-only-root-token
VAULT_ROLE_ID=
VAULT_SECRET_ID=
###< ncards/vault-auth ###

###> ncards/http-kernel ###
# T-004 的 HTTP 横切层。

# ⚠️ 没有这一行，Symfony 会**忽略** Caddy 发来的 X-Forwarded-For 与 X-Forwarded-Proto，
# 于是 Request::getClientIp() 对每个请求都返回 Caddy 容器的 IP、isSecure() 在 TLS
# 后面恒为 false。后果：T-004 的幂等 IP 维度会把所有匿名客户端塞进同一个桶；
# 到了 T-006 更严重 —— §7.5 的 `POST /auth/otp/request` IP 20/h 与
# `GET /v1/users/lookup` IP 100/h 会把全世界当成一个 IP，等于对登录端点的拒绝服务。
#
# `private_ranges` 覆盖 compose 的内网网段；生产同样是「Caddy 与 app 在同一 Docker 网络」
# 的拓扑（§4.1），所以这个值在三个环境通用。
TRUSTED_PROXIES=private_ranges

# §6.1 的强制升级基线。默认值放行一切 —— 真正的基线由 T-112 随 /v1/config 一起定，
# 在那之前把任何客户端挡在门外都是纯粹的误伤。
MIN_SUPPORTED_CLIENT="android/0.0.0 (1)"

# RFC 9457 的 `type` URI 前缀。**所有环境同值**，理由见 config/services.yaml 的注释。
PROBLEM_TYPE_BASE_URI=https://api.n-cards.de/problems
###< ncards/http-kernel ###

###> symfony/messenger ###
# T-004 只用 `sync://` transport（见 config/packages/messenger.yaml），不需要 DSN。
#
# recipe 默认给的是 `doctrine://default?auto_setup=0`，那会连带一张
# `messenger_messages` 表和一个迁移 —— 属于「第一个真实异步消费者」的决定，
# 由 T-1xx 做。到时候取消下面一行的注释，并在 messenger.yaml 里加 async transport。
# MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0
###< symfony/messenger ###
