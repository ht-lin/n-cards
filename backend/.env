# Symfony 的**默认值**文件：只放非密钥的默认配置，随仓库入库。
#
# 加载顺序（后者覆盖前者）：
#   .env                默认值（本文件，入库）
#   .env.local          本地覆盖（不入库）
#   .env.$APP_ENV       环境默认值（.env.test 入库，其余不入库）
#   .env.$APP_ENV.local 本地环境覆盖（不入库）
# 真实环境变量优先于以上所有文件。
#
# ⚠️ 绝不在本文件（或任何入库文件）里写生产密钥 —— §14 / §7.4。
# staging 与 prod 的真实值由 sops(age) 加密后随 Ansible 下发（T-012），
# 容器内以真实环境变量注入（T-003）。
#
# 根 .gitignore 默认忽略 .env 与 .env.*，本文件与 .env.test 是两处显式放行，
# 见 .gitignore 中「Symfony dotenv」一段的说明。

###> symfony/framework-bundle ###
APP_ENV=dev
# 开发用占位值，不具备任何安全意义；staging/prod 必须由环境变量覆盖。
APP_SECRET=dev-only-not-a-secret
APP_SHARE_DIR=var/share
###< symfony/framework-bundle ###

###> symfony/routing ###
# 非 HTTP 上下文（CLI 命令）生成 URL 时使用的基地址。
DEFAULT_URI=http://localhost
###< symfony/routing ###

###> doctrine/doctrine-bundle ###
# 本地裸机（不经 compose）跑 composer test 时用的默认值。密码是占位符，
# 没有任何安全意义 —— 与上面的 APP_SECRET 同一口径。
#
# 在容器里，compose 用 infra/compose/.env 的 POSTGRES_* 拼出真实的 DATABASE_URL 注入
# （见 infra/compose/docker-compose.base.yml）；staging/prod 由 sops(age) 下发（T-012）。
# 驱动与 server_version 已在 config/packages/doctrine.yaml 里钉死，这里不重复。
DATABASE_URL="postgresql://ncards:ncards@127.0.0.1:5432/ncards"
###< doctrine/doctrine-bundle ###

###> ncards/backing-services ###
# T-003 起在 compose 内网可达。这里是「compose 注入什么」与「应用读什么」的唯一对照表。
#   REDIS_URL  → T-004 的幂等键（已消费）与 T-006 的限流
#   VAULT_ADDR → T-005 的 Transit 加密门面（已消费）
REDIS_URL=redis://127.0.0.1:6379
VAULT_ADDR=http://127.0.0.1:8200
###< ncards/backing-services ###

###> ncards/vault-auth ###
# T-005：应用怎么向 Vault 证明自己（§3.3 / §7.4 / §17.4）。
#
# 二选一，判据是「配没配 VAULT_ROLE_ID」，不是 APP_ENV：
#
#   dev / test      VAULT_TOKEN = compose dev 模式的 root token。
#                   root token 没有 policy 限制，只在本地与 CI 用。
#   staging / prod  VAULT_ROLE_ID + VAULT_SECRET_ID，走 AppRole，
#                   受 infra/vault/policies/ncards-app.hcl 的最小权限约束。
#                   由 sops(age) 加密后随 Ansible 下发（T-012），**且不下发 VAULT_TOKEN**。
#
# 选择逻辑在 Shared\Infrastructure\Vault\VaultTokenProviderFactory，那里有完整理由。
#
# ⚠️ VAULT_SECRET_ID 是**凭据**。绝不写进本文件或任何入库文件（§14 / §7.4）。
# 下面两行留空是刻意的 —— 它们的存在只是为了让「应用会读这两个变量」有据可查，
# 空值时工厂会回落到 VAULT_TOKEN。
VAULT_TOKEN=dev-only-root-token
VAULT_ROLE_ID=
VAULT_SECRET_ID=
###< ncards/vault-auth ###

###> ncards/http-kernel ###
# T-004 的 HTTP 横切层。

# ⚠️ 没有这一行，Symfony 会**忽略** Caddy 发来的 X-Forwarded-For 与 X-Forwarded-Proto，
# 于是 Request::getClientIp() 对每个请求都返回 Caddy 容器的 IP、isSecure() 在 TLS
# 后面恒为 false。后果：T-004 的幂等 IP 维度会把所有匿名客户端塞进同一个桶；
# 到了 T-006 更严重 —— §7.5 的 `POST /auth/otp/request` IP 20/h 与
# `GET /v1/users/lookup` IP 100/h 会把全世界当成一个 IP，等于对登录端点的拒绝服务。
#
# `private_ranges` 覆盖 compose 的内网网段；生产同样是「Caddy 与 app 在同一 Docker 网络」
# 的拓扑（§4.1），所以这个值在三个环境通用。
TRUSTED_PROXIES=private_ranges

# §6.1 的强制升级基线。默认值放行一切 —— 真正的基线由 T-112 随 /v1/config 一起定，
# 在那之前把任何客户端挡在门外都是纯粹的误伤。
MIN_SUPPORTED_CLIENT="android/0.0.0 (1)"

# RFC 9457 的 `type` URI 前缀。**所有环境同值**，理由见 config/services.yaml 的注释。
PROBLEM_TYPE_BASE_URI=https://api.n-cards.de/problems
###< ncards/http-kernel ###

###> symfony/messenger ###
# T-102 是 T-004 注释里点名的「第一个真实异步消费者」，于是这一行被取消注释了。
# 承载的是 `email` / `email_failed` 两条 transport（见 config/packages/messenger.yaml）。
#
# 选 doctrine 而不是 redis 的理由（ADR-0012）：丢一封 OTP 信 = 该用户登不进去，
# 而本栈的 Redis 是单容器、无 AOF、§8.2 给它的定位是「限流计数保留 24 小时」——
# 一次重启就会把在途的登录信吃掉。Postgres 那边本来就有备份与 WAL（T-406）。
#
#   auto_setup=0    表由迁移建（§13.5 / CONTRIBUTING §5：所有 DDL 走迁移）。
#                   留着 auto_setup 的话 transport 会自己偷偷 CREATE TABLE，
#                   绕过那道门，且 staging 与本地的表结构可能悄悄分叉。
#
# ⚠️ 与之配套的 `use_notify: false` 在 messenger.yaml 的 `options:` 里，**不在这里** ——
# 它是 transport 选项而不是 DSN 参数，写进 DSN 会被
# `Connection::buildConfiguration()` 当成未知选项直接抛异常。
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0
###< symfony/messenger ###

###> ncards/mail ###
# T-102 邮件通道（§3.1 / §3.2 / §8.3）。
#
# ⚠️ **MAILER_DSN 在 staging/prod 是凭据**（含服务商的 SMTP 用户名与口令），
# 与 VAULT_SECRET_ID 同一口径：由 sops(age) 加密后随 Ansible 下发（T-012），
# 绝不写进本文件或任何入库文件。
#
# 本地默认 `null://null` —— **不发信，直接丢弃**。这是刻意的：
# 开发机上跑 composer test 不该往真实邮箱发东西，而 §3.1 的「用户无法让系统
# 向任意第三方发信」在开发环境同样不该靠自觉。想在本地肉眼看信，
# 起 compose 的 dev profile（Mailpit）并改成 smtp://mailpit:1025，
# 见 infra/compose/README.md。
MAILER_DSN=null://null

# 发件人。域名来自 ADR-0002（`n-cards.de`）。
# SPF / DKIM / DMARC 必须配在**这个地址的域**上，否则几乎必进垃圾箱（§3.2）——
# 记录模板与推进节奏见 docs/runbooks/email-dns.md。
MAIL_FROM_ADDRESS=no-reply@n-cards.de
MAIL_FROM_NAME=N-Cards

# §14.4 的 `email_send_total{provider,template,result}` 里那个 provider 标签。
#
# Q3 已决（2026-09-05，docs/adr/0013-mail-via-domain-mailbox.md）：发信走
# n-cards.de 的**域名邮箱**，托管方 dogado GmbH（德国），不采购专业 ESP。
# 换选型时改的就是这一行 + MAILER_DSN 两个变量，MailSenderInterface 以上
# 一行代码没动 —— 那道抽象墙（§3.2）的第一次真实检验。
MAIL_PROVIDER=dogado

# §3.1 / §7.5 最后一行的「全局日发信量阈值告警 + 自动熔断非关键邮件（保留 OTP）」。
# 两个阈值的取值理由见 config/packages/ncards_mail.yaml。
###< ncards/mail ###

###> ncards/public-links ###
# T-104：信里那些指回 App 的链接的基地址（§7.1 的「新设备登录提醒」）。
#
# ⚠️ 与上面的 DEFAULT_URI **不是**一回事。DEFAULT_URI 是 Symfony 路由在 CLI 里
# 生成本服务 URL 时的兜底（api.n-cards.de 那一侧）；这一条指向的是**客户端**：
# Android 注册了 `https://app.n-cards.de/l/*` 的 App Links（§7.1 / assetlinks.json），
# 装了 App 的用户点信里的链接会直接拉起 App，没装的落到 Web 落地页。
# 两个域名不同、归属不同、改动理由也不同，共用一个变量迟早会有人改错一边。
#
# 结尾**不带**斜杠 —— 拼接方一律写成 `$baseUrl.'/l/…'`。
#
# T-106 的 Magic Link 复用同一个变量（`/l/magic/<token>`）。
#
# ⚠️ 这个值必须与 Caddy 的 `CADDY_APP_SITE_ADDRESS` 指向**同一个地方**：
# 那边决定落地页在哪儿，这边决定邮件里写哪儿。对不上就是死链，而且不会有任何
# 测试变红 —— 症状只出现在真人点开那封信的时候。
#
# ⚠️ 本地默认改成 localhost:8081（compose 把 App Links 站点块映射在那儿），
# 否则本地 Mailpit 收到的信里是生产域名，点开去的是**生产**落地页。
# 生产/staging 的真值由 Ansible 的 ncards_app_public_base_url 注入，不看这里。
APP_PUBLIC_BASE_URL=http://localhost:8081
###< ncards/public-links ###
